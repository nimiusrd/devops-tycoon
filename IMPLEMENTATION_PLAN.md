# 実装計画

DevOps Tycoon の実装計画。本書は [`SPEC.md`](./SPEC.md) の企画内容を、実際に作る順序・成果物・検証方法へ落とし込んだものである。SPEC の章番号を随所で参照する（例: 第22章）。

> 方針: SPEC 第21章「MVPスコープ」を段階の骨格とし、第22章「技術構成」をアーキテクチャの前提とする。**シミュレーション層を最初から描画から分離し、seed付き決定論で実装する**ことを全フェーズ共通の規律とする（第22.3〜22.5）。

---

## 0. 現状とゴール

### 0.1 現状

- リポジトリには企画書（`SPEC.md`）と HTML/PNG のモックアップ（`mockups/`）のみが存在し、アプリケーションコードはまだない。
- モックアップ（`main-screen` / `org-screen` / `dept-screen` / `industry-screen` / `drilldown`）は**デザイン・レイアウトの正**として維持する（第22.2）。

### 0.2 ゴール

- 「AI導入が開発組織の生産性に与える影響」を、seed付き決定論の確率モデルで再現するWebゲームを段階的に構築する。
- 各 MVP の終わりに「遊べる/検証できる」状態を作り、次フェーズへ積み増す。
- ロジックは GPU 不要の Vitest で厚くテストし、実ピクセル/操作は Playwright で薄くテストする二段構え（第22.5）。

---

## 1. 技術スタックとアーキテクチャ方針

SPEC 第22章に準拠する。

| 領域 | 採用技術 | 役割 |
| --- | --- | --- |
| 言語 | TypeScript | 全レイヤ共通 |
| ビルド/Dev | Vite | 開発サーバ・バンドル |
| UIシェル | React | HUD・アクションバー・カード・ツリー・マップ・リザルト・パンくず |
| UI演出 | Framer Motion | 画面遷移・マイクロインタラクション |
| 盤面描画 | DOM/SVG（MVP1〜2）→ PixiJS + pixi-viewport（MVP3以降） | タスク粒・アイソメ盤面・フロー・ヒートマップ・延焼 |
| 状態管理 | Zustand（ラン/メタ状態）＋ XState（フェーズ遷移） | 予測可能な状態とフロー管理 |
| シミュレーション | 純TS・固定タイムステップ・seed付きPRNG | 確率モデル本体（描画から分離・決定論） |
| 重い試算 | Web Worker（+Comlink） | what-if 計算・モンテカルロ等 |
| 静的グラフ | Recharts または visx | リザルト/診断の図 |
| 保存 | localStorage（MVP）→ IndexedDB（idb/Dexie） | セーブ・メタ進行・リプレイ |
| テスト | Vitest（ロジック）＋ Playwright（実ブラウザ） | 第22.5 の二段構え |
| バックエンド | 当面なし（ローカル擬似） | 将来: ランキング共有・デイリーラン配信 |

### 1.1 レイヤ分離の原則（最重要）

```text
React (UIシェル)  ── 状態を持ち、表示する。Zustand + XState。
        │ 読む（一方向）
Simulation (純TS) ── 固定タイムステップ＋seed付きPRNGで状態を更新。描画を一切知らない。
        │ 読まれる（一方向）
Renderer (DOM/SVG → Pixi) ── 状態を読んで描くだけ。双方向バインドしない。
```

- **React は状態を持ち、Renderer は状態を読んで描くだけ**（第22.2）。
- **sim は描画を知らない**。`step(dt)` で状態を進める純粋な関数群とし、入力（介入アクション等）はイベントキュー経由で受ける。
- 乱数は seed付き PRNG（`mulberry32` 程度）に一本化し、`?seed=` で再現可能にする（第22.3）。
- `window.game` に `pause() / step(ms) / loadState(seed, scenario)` を露出し、E2E から状態を固定できるようにする（第22.5）。

### 1.2 ディレクトリ構成（初期案）

```text
/
├─ SPEC.md
├─ IMPLEMENTATION_PLAN.md      ← 本書
├─ mockups/                    ← デザインの正（維持）
├─ index.html
├─ vite.config.ts
├─ package.json
├─ tsconfig.json
└─ src/
   ├─ sim/                     ← シミュレーション（純TS・描画非依存）
   │  ├─ rng.ts               ← seed付きPRNG（mulberry32）
   │  ├─ types.ts             ← ドメイン型（Task, Lane, OrgState 等）
   │  ├─ model/               ← 確率モデル（coding/review/rework/incident）
   │  ├─ engine.ts            ← 固定タイムステップのループと step(dt)
   │  └─ scenarios.ts         ← 難易度プリセット（第16章）
   ├─ state/                   ← Zustand ストア + XState マシン
   │  ├─ runMachine.ts        ← マップ→スプリント→ドラフト→進化（第3章）
   │  └─ stores/
   ├─ render/                  ← 描画アダプタ（DOM/SVG → 後でPixi）
   │  ├─ Board.tsx            ← 盤面（タスク粒の流れ）
   │  └─ adapters/            ← レンダラ差し替え用インターフェース
   ├─ ui/                      ← React UI（HUD/アクションバー/カード/ツリー…）
   ├─ data/                    ← カード・レリック・イベント・ボス定義（データ駆動）
   ├─ game.ts                  ← window.game フック（pause/step/loadState）
   └─ main.tsx
   tests/
   ├─ unit/                    ← Vitest（sim 不変条件・ビューモデル）
   └─ e2e/                     ← Playwright（視覚回帰・操作）
```

---

## 2. フェーズ別計画

各フェーズは SPEC 第21章の MVP に対応する。フェーズ末で「動く成果物」と「テスト」を必ず残す。

### フェーズ 0: 基盤セットアップ（MVP の前提）

SPEC に明記はないが、第22章を成立させるための土台。

- **タスク**
  - Vite + React + TypeScript プロジェクトの初期化
  - Zustand / XState / Framer Motion の導入
  - Vitest セットアップ（`src/sim` を最優先でテスト可能に）
  - Playwright セットアップ（既存 `mockups/` 撮影構成を視覚回帰の基盤として流用 / 第22.5）
  - Lint/format（ESLint + Prettier）、CI（テスト実行）
  - `src/sim/rng.ts`（mulberry32）と seed の一元管理、`?seed=` パラメータ
  - `window.game` フックの骨組み（`pause/step/loadState`）
- **成果物**: `npm run dev` で空のシェルが立ち上がり、`npm test` が通る。
- **テスト**: `rng.ts` の決定論（同一 seed で同一列）を Vitest で検証。

---

### フェーズ 1: スプリントシミュレーション（MVP 1）

> SPEC 第21章 MVP1 / 第4.1〜4.2章 / 第4.6章

**目的**: AIなし / AIあり の違いを、タスク粒の流れと結果表示で体感できる最小ループ。

- **sim 層**
  - ドメイン型: `Task`（定型/通常/複雑/AI/手戻り/障害/高価値/負債）、`Lane`（Backlog▸Coding▸Review▸Rework▸Done）、`OrgState`（第5章のリソース）
  - 工程モデル: Coding 速度、Review 容量、Rework 率、Incident 率を確率で算出
  - **AI 導入フラグ**で Coding 加速 → Review 渋滞が増える因果を実装（第2章のコア）
  - 固定タイムステップの `step(dt)`、seed 付きで決定論
- **状態/UI**
  - 基本ステータス表示（出荷ポイント/開発速度/レビュー耐性/品質/シニア体力/AI依存度/技術的負債/士気/炎上リスク）（第4.2章）
  - 盤面: タスク粒が `Backlog → Coding → Review → Rework → Done` を流れる（DOM/SVG、`main-screen` モックアップ準拠）
  - 種類ごとの見た目（第4.1章の表）
  - スプリントリザルト画面（第4.6章の項目: Done/Delivered/Rework/Incidents/Senior HP 等）
- **成果物**: 1スプリントを最後まで自動進行し、AIあり/なしを切り替えて結果差を観察できる。
- **テスト（Vitest 中心）**
  - 不変条件: 「AI依存度↑ → Rework 傾向↑」（第22.5 の代表例）
  - seed 再現性: 同一 seed で同一リザルト
  - 「状態→見た目」マッピング（粒の色/サイズ）を純関数で検証

---

### フェーズ 2: 能動操作とカード（MVP 2）

> SPEC 第6章 / 第4.3章 / 第7章 / 第18.2章

**目的**: 「眺める」から「捌く」へ。介入アクション・集中力・コンボ・カードドラフトを追加。

- **能動操作（第6章）**
  - マネジメント集中力 ⚡（毎スプリント回復、スプリント中は回復しない）
  - 介入アクション: 割り込みレビュー / PR分割 / 緊急対応🔥 / タスク差配 / AIスロットル / 残業号令 / アンドン⏸ / ペアモブレビュー（第6.1の表＝コスト・CD・副作用）
  - 介入アクションバー UI（⚡コスト・CD・Ready 表示 / 第4.3章）
  - リアルタイム演出メーター: 渋滞メーター・炎上タイマー（第6.3章）
  - コンボと連携ゲージ（第6.2章）
- **カード/デッキ（第7章）**
  - データ駆動のカード定義（第7.2章の例を `src/data/cards`）、レアリティ
  - スプリント後のドラフト（3枚→1枚）、手札配布、発動
  - カード強化（コスト減/効果増）の枠組み（ショップ/休息は MVP3 で接続）
- **演出（第18.2章）**
  - 数字ポップ、`COMBO xN`、渋滞ヒートマップ、延焼/鎮火、スイープ（Framer Motion + DOM/SVG）
- **成果物**: スプリント中に介入して結果が変わる。ドラフトでデッキが育つ。
- **テスト**
  - Vitest: 集中力消費/CD/副作用、コンボ倍率、カード効果の状態反映
  - Playwright: 介入アクション発動 → 渋滞が捌ける操作 E2E（第22.5）

---

### フェーズ 3: 周回・育成・診断（MVP 3）

> SPEC 第3章 / 第4.4〜4.5章 / 第8〜17章

**目的**: ローグライクの周回構造を完成させる。1ラン＝1四半期。

- **フロー（XState）**: マップ → スプリント → リザルト → ドラフト → 進化 の入れ子（第3章）
- **ランマップ（第4.4章）**: 分岐ルート、ノード種別（通常/高負荷/イベント◇/ショップ$/休息☾/ボス★）
- **ボススプリント（第10章）**: 大型リリース/本番大規模障害/セキュリティ監査/経営レビュー（特別ルール）
- **組織進化ツリー（第4.5章 / 第11章）**: 進化ポイント割り振り、5ブランチ（開発力/レビュー体制/品質保証/AI活用/文化）
- **組織文化レリック（第8章）**: 恒久パッシブ（心理的安全性、ポストモーテム文化 等）
- **ランダムイベント（第9章）**: 良い/悪い/ネタ + 分岐選択イベント（トレードオフ）
- **組織タイプ診断（第13章）**: Healthy Acceleration / Review Hell / AI Overproduction / Rework Spiral / Senior Sacrifice / Documentation Kingdom
- **称号と勝敗（第4.6 / 第14 / 第15章）**: 称号付与、勝利条件（通常/健全/AI導入成功/経営/現場幸福/カオス/ノーダメ）、敗北条件
- **メタ進行（第17章）**: ボス撃破で解放、localStorage 保存
- **難易度（第16章）**: Easy/Normal/Hard/Nightmare + 試練モディファイア
- **画面ステート演出（第18.3章）**: 健全=昼 / Review Hell=赤く夜 等
- **描画移行の検討点**: 粒数・ノード数が増えるため、**ここで盤面描画を PixiJS へ移植**する判断ポイント（第22.4）。sim 分離済みなら移植コストは小さい。
- **成果物**: 1ランを最初から最後（ボス）まで通しでプレイでき、結果で解放が進む。
- **テスト**
  - Vitest: 進化ツリー/レリック/イベント効果の状態反映、診断ロジック、勝敗判定、seed 固定のラン再現
  - Playwright: 島/ノードクリック → 画面遷移、ボス突破フロー

---

### フェーズ 4: キャラクター育成（MVP 4）

> SPEC 第12章

- メンバー個体（実装力/レビュー力/AI習熟/スタミナ）とトレイト（AI職人/巨大PR製造機 等）
- 成長（ジュニア→ミドル→シニア昇格）、編成（誰をどのレーンに/AIを誰に）
- スタミナ管理と離脱（休職）リスク、表情演出（疲れ顔/ガッツポーズ）
- **成果物**: 採用カードが「未来の主力育成」の選択になる。
- **テスト**: Vitest で成長/昇格/スタミナ閾値、編成が結果に与える影響。

---

### フェーズ 5: 組織スケール（巨大組織対応 / MVP 5）

> SPEC 第4.7〜4.11章

- ズーム階層（業界 ▸ 全社 ▸ 部署 ▸ 現場）とパンくず移動（第4.7章）
- 全社マップ（部門ゾーン・チーム島・共通基盤ハブ・チーム間フロー・全社レバー / 第4.8章, `org-screen`）
- 部署ビュー（チーム＝小パイプライン・依存の連鎖炎上 / 第4.9章, `dept-screen`）
- 業界ランキング（シーズン制リーダーボード・リーグ・ベンチマーク / 第4.10章, `industry-screen`）
- ドリルダウン/ズームアウトのカメラ遷移（pixi-viewport、クロスフェード / 第4.11章, `drilldown`）
- **描画前提**: PixiJS + pixi-viewport を本格採用。下位の事象を上位に集約し、上位レバーで下位制約を緩める設計（第4.7章）。
- **成果物**: 現場↔部署↔全社↔業界を地続きにズームして采配できる。
- **テスト**
  - Vitest: アイソメ投影/depth sort/カリング、状態→見た目、カリング数/プール再利用の数値検証（第22.5）
  - Playwright: 視覚回帰（seed＋一時停止で固定フレーム）、ドリルダウン操作

---

## 3. 横断的な実装規律

### 3.1 決定論とテスト容易性（第22.3 / 22.5）

- 乱数は seed付き PRNG に一本化、`?seed=` で再現。
- レンダラは「状態を読んで描くだけ」＝同一状態なら同一フレーム＝スクショ安定。
- `window.game.pause()/step(ms)/loadState(seed, scenario)` を全フェーズで維持。
- スプライト生成は依存注入にし、テストでモック差し替え可能に。

### 3.2 避けること（CIが脆くなる / 第22.5）

- Node 上で実 WebGL を回さない（実ピクセル検証は Playwright に集約）。
- FPS を CI で直接 assert しない（カリング数・プール再利用・生成スプライト数の上限を数値検証）。

### 3.3 データ駆動

- カード（第7章）・レリック（第8章）・イベント（第9章）・ボス（第10章）・難易度（第16章）は `src/data/` の宣言的定義に寄せ、追加・バランス調整をコード変更なしで行える形にする。

### 3.4 段階的描画移行（第22.4）

- MVP1〜2: DOM/SVG（モックアップ準拠）で素早く。ただし sim は最初から分離・seed付き。
- MVP3 以降: 粒数/ズームが増える時点で PixiJS へ移植。React/TS/Framer Motion/Recharts は役割を限定して継続。

---

## 4. マイルストーンと順序

| # | マイルストーン | 対応 MVP | 完了の目安 |
| --- | --- | --- | --- |
| M0 | 基盤セットアップ | 前提 | dev 起動・Vitest/Playwright 稼働・PRNG 決定論テスト緑 |
| M1 | スプリントが回る | MVP1 | AIあり/なしで結果差・リザルト表示・不変条件テスト緑 |
| M2 | 捌けるスプリント | MVP2 | 介入/集中力/コンボ/ドラフトで結果が変わる |
| M3 | 1ラン通し | MVP3 | マップ→ボス→解放まで通しプレイ・診断/称号/勝敗 |
| M4 | メンバー育成 | MVP4 | 個体育成・編成が戦術になる |
| M5 | 巨大組織 | MVP5 | 4階層ズーム・全社/部署/業界・カメラ遷移 |

依存関係: M0 → M1 → M2 → M3 →（M4・M5 は M3 以降で並行可能だが、M5 は描画移行を前提とするため M3 の Pixi 移植後に着手するのが安全）。

---

## 5. リスクと留意点

- **世界観の制約（第2.1章）**: イベント/ボス/敗北/称号/演出は「現実の開発組織で起こりうる範囲」に留める。SF的飛躍・実在企業/個人の貶めはしない。実装時のレビュー基準にする。
- **描画移行のタイミング**: 早すぎる Pixi 投資は過剰（第22.4）。MVP1〜2 は DOM/SVG で素早く、粒数が破綻し始める MVP3 で移植する。
- **バランス調整コスト**: 確率モデルのチューニングは Web Worker でのモンテカルロ試算（第22.3）を用意し、データ駆動定義で回す。
- **状態の複雑化**: フェーズ遷移は XState に集約し、ラン/メタ状態は Zustand に分離して、肥大化を防ぐ。

---

## 6. 次の一手（着手順）

1. フェーズ0: Vite+React+TS 初期化、Vitest/Playwright、`rng.ts`、`window.game` 骨組み。
2. フェーズ1: `sim` のドメイン型と工程モデル、AIあり/なしの因果、リザルト画面、不変条件テスト。
3. 以降、M2 → M3 → M4/M5 と積み増す。

各フェーズ末に「動く成果物＋テスト」をコミットし、SPEC の章番号で追跡可能にする。
