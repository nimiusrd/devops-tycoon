# 残課題バックログ

実装済みの手順ではなく、現在の未充足・保留事項を管理する。完了項目は追跡可能な最小限の要約だけを残し、詳細はGit履歴とテストを参照する。

## 1. 現在の課題

| ID | 課題 | 優先度 | 状態 | SPEC |
| --- | --- | --- | --- | --- |
| RI-64 | 独立状態を持つ複数チーム運営 | 高 | 完了 | 第4.7〜4.11 |
| RI-67 | オンボーディングとシニア燃え尽きの断絶 | 高 | 未着手 | 第14〜16 / 19〜20 |
| RI-68 | 四半期レビュー Delivery KPI のスケール不整合 | 高 | 未着手 | 第4.6.1 / 15 |
| RI-69 | スプリント上部操作バーと盤面の重なり | 中 | 未着手 | 第4 |
| RI-70 | モバイルのスプリント操作性 | 中 | 未着手 | 第4 |
| RI-71 | favicon 未設定による 404 | 低 | 未着手 | — |
| RI-72 | 難易度カーブと、常に正解／常に不正解な手がある構造 | 高 | 未着手 | 第19.1（F-1・F-7）/ 第16 |
| RI-73 | Nightmare は AI 依存を意識しない方針で第1スプリント敗北が確定する | 高 | 未着手 | 第19.1（F-8・F-9）/ 第15・16 |
| RI-74 | スプリントが規定帯をほぼ全面的に下回る | 高 | 未着手 | 第3.1 / 第19.1（F-4） |
| RI-75 | 勝利種別が「重アクションを使ったか」でしか分岐しない | 高 | 未着手 | 第14 / 第19.1（F-10） |
| RI-76 | AI 導入が既定 ON で、既定のまま進むのが有利 | 高 | 未着手 | 第2 / 第19.1（F-1・F-2・F-10） |
| RI-77 | カードが使い方を変えても勝率を動かさない | 高 | 未着手 | 第7 / 第6 / 第19.1（F-2） |
| RI-78 | 予算枯渇・信頼枯渇が予兆なく終わり、敗因ラベルが実態と一致しない | 中 | 未着手 | 第15 / 第19.1（F-6・F-8・F-9） |
| RI-79 | 評価が無介入と熟練を区別しない | 中 | 未着手 | 第4.6 / 第19.1（F-6） |
| RI-80 | ドラフトのマリガンが無い | 中 | 未着手 | 第7 / 第19.1（F-12） |
| RI-81 | 敗北画面に「次の一手」が無い | 中 | 未着手 | 第13 / 第19.1（F-6） |
| RI-82 | 目標修正の選択が結果をほとんど変えない | 中 | 未着手 | 第4.6.1 / 第19.1（F-2 第4層） |
| RI-83 | 条件を揃えると介入の寄与がほぼ消える | 高 | 未着手 | 第6 / 第19.1（F-5） |
| RI-84 | レビュー凍結が選択不能な判定イベントでしか確定しない | 高 | 未着手 | 第4 / 第19.1（F-4・F-8） |
| RI-85 | Q1 の進化解放が分散し、ビルドの方向が確定しない | 中 | 未着手 | 第11 / 第19.1（F-11） |
| RI-86 | セキュリティ軸が存在しない | 中 | 未着手 | 第19.1（F-10 のビルド表） |
| RI-87 | インフラコスト軸が存在しない | 中 | 未着手 | 第19.1（F-10 のビルド表） |
| RI-34 | ローカル完結の将来拡張 | 低 | 保留（要判断） | 第23 |

RI-67〜71 は 2026-07-20 のプレイテストで洗い出した。RI-72〜87 は 2026-07-26 に
SPEC 第19.1「面白さの定義と判定基準」を定めたうえで、全難易度 × 26方針 × 10seed = 1,040ラン（`npm run playtest`、メタ解放は初見相当）を回して洗い出した。
経緯・再現条件・実測は[playtest-findings.md](./playtest-findings.md)を参照する。

### RI-64 独立状態を持つ複数チーム運営

完了。各チームは `TeamRunState` として永続し、非選択は粗粒度 sim・選択中は詳細スプリント、全社／部署／チーム施策と入り込みコスト、任意チームへのドリルダウン、保存／リプレイを接続した。詳細はGit履歴と `tests/unit/orgscale-engine.test.ts` / `tests/e2e/org-scale.spec.ts` を参照。

### RI-67 オンボーディングとシニア燃え尽きの断絶

Easy でも無介入・素直プレイだと第1四半期スプリント2でシニア燃え尽き（`seniorBurnout`）敗北する。敗北条件は抽象値 `org.seniorHp <= 1`（`src/sim/outcome.ts:57`）で、メンバー個別 stamina ではない。`org.seniorHp` は毎 tick `REVIEW_HP_REGEN`=0.7（炎上中は半減）で回復し、主な減少要因はレビュー1件ごとの一律消費 `REVIEW_HP_COST`=1.6（`reviewOne`、`src/sim/sprint.ts:252`）と、猶予切れ炎上の自動鎮火 `INCIDENT_HP_COST`=12（`advanceBurning`、`src/sim/sprint.ts:340`）。スプリントは `isDrained` まで進むが、手戻り・鎮火タスクは Review へ再投入されるためレビュー回数はタスク数固定ではなく手戻り・炎上率で増減する。第1スプリント実測（同一 seed）では、自動鎮火回数と HP 終値が 無介入 3回/42、緊急対応のみ 1回/62、緊急対応＋andon 0回/74、編成＋介入 1回/55。最も確度の高い（単一条件変更の）要因は**炎上を自動鎮火 −12 の前に緊急対応 −2 で鎮火すること**。andon は流入を一時停止して既存キューを捌く猶予を作る補助。AIスロットルはレビュー1件あたりの点火率と手戻り率を下げ、品質・PR分割は手戻り率のみ下げて再レビューを減らす（点火率 `incidentProbability` は testCoverage・aiAssisted・カード効果のみ参照し品質値・`task.split` は参照しない）。編成（review 増員）は review 容量だけでなく Coding 担当減で `foldFormationEffects` の複数因子が連動するため単一機序に帰属できないが、A（編成のみ）は無介入と同じく即死しており燃え尽き回避の主手段ではない。チュートリアル（`src/ui/tutorial.ts`）は介入バー・レビュー渋滞・コンボのみで、燃え尽き回避の主手段を教えない。実測と経緯は[playtest-findings.md](./playtest-findings.md)参照。

受入条件:

- 初見プレイヤーがシニア燃え尽きを回避する最大手段（炎上を自動鎮火前に緊急対応で鎮火）へ導かれる。andon は既存キューを捌く猶予、AIスロットルは点火率と手戻り率を下げる、品質・PR分割は手戻り率のみ下げて再レビューを減らす、編成はスループット策であり燃え尽き回避の主手段ではない、と位置づけを区別する。
- チュートリアルまたは盤面・HUD の警告で、抽象値としてのシニア体力と燃え尽きリスクを事前に伝える。
- Easy の序盤（第1四半期）で、標準的な操作理解のプレイが2スプリントで即敗北しない。
- 難易度別の `org.seniorHp` 収支・レビュー負荷の妥当性を自動操作モデルまたはユニットテストで検証する。

### RI-68 四半期レビュー Delivery KPI のスケール不整合

四半期レビューで Delivery 目標（概ね1スプリント相当）に対し、四半期累計 `quarterTotals.delivered` を比較しているため、看板KPI「出荷ポイント＝勝利条件の進捗」が常に自明達成となる（実測: Q2 目標20 vs 実績1693）。目標生成は `src/sim/run/quarterReview.ts` の `deliveryTarget`。

受入条件:

- `deliveryTarget` と判定値の単位（1スプリント平均 vs 四半期累計）を一致させる。
- Delivery 目標が四半期を通じて意味のある達成／未達の分岐を生む。
- 実際に効く制約（Morale・Quality・Incident・燃え尽き）が勝敗条件として表示で伝わる。
- 代表 seed で目標と実績の分布が妥当な範囲に収まることを検証する。

### RI-69 スプリント上部操作バーと盤面の重なり

デスクトップ幅（1440px、DOM レンダラ `?renderer=dom` で実測）で、上部の操作バー（`通常スプリント / ❚❚ / 1x-2x / 渋滞メーター / 炎上タイマー / COMBO`）が盤面領域の上端と重なり見切れる。DOM 経路の盤面は Pixi キャンバスではなく DOM 要素で描画されるため、レンダラ非依存のヘッダ・盤面コンテナのレイアウト問題として扱う（Pixi でも別途再現確認）。対象は `src/ui/SprintScreen.tsx` 周辺。

受入条件:

- 主要なデスクトップ幅で操作バーと盤面が重ならない。
- `npm run gallery` またはE2E視覚回帰で重なりの再発を検知する。

### RI-70 モバイルのスプリント操作性

390px 幅で横スクロール・崩れは無いが、縦積みKPIカードの下に盤面とアクションバーが来るため、時限スプリント中に盤面と介入ボタンへスクロールが必要になる。

受入条件:

- 狭幅ではKPIを折り畳み／要約し、盤面とアクションバーを1画面に収める。
- 狭幅でスプリント中に介入バーへ到達できることをE2Eで検証する。

### RI-71 favicon 未設定による 404

`index.html` に favicon 宣言が無く `public/` にも実体が無いため、`/favicon.ico → 404` が毎ロード時にコンソールへ出る。

受入条件:

- `<link rel="icon">` とアイコン配置で 404 が出ない。
- ブラウザタブにアイコンが表示される。

### RI-72 難易度カーブと、常に正解／常に不正解な手がある構造

難易度別の勝率（26方針 × 10ラン = 各260ラン）は easy 69% / normal 38% / hard 10% / nightmare 0%。SPEC 第19.1 F-7 が目標とする「初勝利5ラン前後＝勝率20%前後」に合うのは hard（初見相当 2/10）だけで、easy は完全放置（介入もカードも使わない）でも 7/10 勝てる。nightmare は260ラン全敗（RI-73）。

F-1 の違反は2方向にある。**単一介入の `onlyFirefight`（緊急対応のみ）が全26方針中トップの 19/40**（全難易度合計）で、複合方針の `skilled` 9/40・`skilledNoHire` 13/40 を上回る。難易度別でも easy 10/10・normal 6/10・hard 3/10 と、複合方針（6/10・3/10・0/10）より強い。緊急対応は「打てるときに打てば得」で状況によって正解が入れ替わらず、他の介入を選ぶ理由が薄い。なお全介入を 1 tick ごとに撃つ `probe` は 10/40 と弱く、乱打が損になる関係自体は成立している。

統制実験（同一 seed、条件を1つずつ外す）では、**採用が一貫して勝率を下げる**（全40ランで `skilled` 9 vs `skilledNoHire` 13。難易度別は easy 6/7、normal 3/5、hard 0/1）。採用方針を全経路（専用フェーズ・ショップ・休息・即時採用イベント）へ適用し、採用・復職の双方でベンチのメンバーを実働レーンへ配置した上での結果である。`budgetExhausted` 敗北は1,040ラン中19件。F-1 の観点では、緊急対応が「常に打てば得」であるのと対称に、**採用が「常に見送るのが正解」**になっている。一方カードは使い方を変えても勝率を動かさない（無差別 16 / 不使用 16 / 状況選択 15）。詳細は RI-77。

受入条件:

- 初見相当（`naive`）の勝率が概ね 20% 前後になる難易度が、導入難易度（easy / normal）側に存在する。
- 完全放置（介入もカードも使わない）で勝ち越せる難易度が無い。
- 単一介入だけを繰り返す方針が、状況に応じて介入を選ぶ方針を上回らない。緊急対応にも
  不利になる状況（打つべきでない盤面）が存在し、同一難易度の中で有利／不利が共存する。
- 採用が**離職・休職のリスクを下げる方向**で作用する。人数を増やすことでレビュー負荷と
  スタミナ消耗が分散し、`onLeave` や燃え尽きの発生率が下がる。単なる出力の上積みではなく、
  「今の人数で回し続けるか、コストを払って壊れにくくするか」という選択にする（方針決定 2026-07-26）。
- 各手段のコスト（採用の予算・カードの集中力）は F-1 の求める代償として維持する。求めるのは
  代償の消去ではなく、**代償に見合う状況が特定できること**である。
- 難易度別の勝率レンジと、上記の「有利／不利になる条件」の両立を `npm run playtest` で検証し、回帰を検知する。

### RI-73 Nightmare は AI 依存を意識しない方針で第1スプリント敗北が確定する

nightmare は26方針260ランすべて敗北し、うち215ランが `aiDependency`。敗北までのスプリント数 p50 は 1 で、他の敗因（2〜24）と比べて突出して短い。回避経路は2種類ある。依存度を下げる経路（`onlyThrottle` / `noAi`）と、**リテラシーを上げて敗北条件そのものを外す経路**（`onlyPair` は依存度100のまま最終リテラシー 62 で `aiDependency` 敗北 0/10）である。とくに `pairReview`（「詰まったPRをペアで処理。AI Literacy が上がる」）による回避は SPEC 第19章の主題に最も合致する。それ以外の方針は最終リテラシー 25.0・依存度 100 で 10/10 が第1スプリント敗北する。回避経路はいずれも勝率 0/10。敗北条件は `org.aiDependency >= 95 && org.aiLiteracy <= 30`（`src/sim/outcome.ts:63-65`）で、nightmare の初期値 `aiDependencyBase`=55 / `aiLiteracy`=25（`src/data/difficulties.ts`）はリテラシー側を最初から満たしている。`aiDependency` は AI 割当タスク1件ごとに `AI_DEP_PER_TASK`=2.2 加算され（`src/sim/sprint.ts:193`）、`taskCountMul`=1.2 のタスク量で第1スプリント中に 95 へ到達する。問題は回避不能なことではなく、**回避に必要な方針が事前知識を要し、外すと第1スプリントで終わる**こと。SPEC 第19.1 F-8（詰みの確定を作らない）違反であり、初手で終わるため「負けて学ぶ」も成立しない。

依存度を下げる手段は既に存在する（カード `ai-guideline` の `aiDependencyAdd: -10`、レバー `aiGuideline` / `teamAiThrottle` の `aiDependencyDelta: -10`、`aiThrottleDept` の -8）。一方で AI を抑えない方針の最終依存度は全難易度で上限に張り付く。カードはドラフトの引き次第、レバーは四半期予算を消費するため、既定方針では低下手段が実質機能しない。リテラシー経路（ペアレビュー）も、チュートリアルにもリザルトにも現れず発見の手掛かりが無い。したがって不足しているのは手段の有無ではなく、既存手段の効果量・入手機会・発見性である。

受入条件:

- Nightmare で、事前知識のない方針が第1スプリントで敗北しない。
- AI 依存敗北の回避経路（依存度を下げる／リテラシーを上げる）がプレイ中に発見できる。とくに `pairReview` によるリテラシー上昇が敗北条件を外すことを、チュートリアルまたは敗北時の提示で伝える。
- 既存の依存度低下手段（`ai-guideline` カード・AI 系レバー）の効果量・入手機会・発見性を、1スプリントあたりの上昇量と釣り合う水準へ見直す。新規手段の追加はこの見直しで足りない場合に限る。
- 通常プレイで `aiDependency` が上限に張り付かず、意思決定の入力として機能する。
- AI 依存敗北が、盤面・HUD で数スプリント前から予兆として見える。
- 難易度別の `aiDependency` 推移と敗北到達スプリントをユニットテストで検証する。

### RI-74 スプリントが規定帯をほぼ全面的に下回る

SPEC 第3.1 の規定は通常60〜120秒（絶対下限30秒）・ボス90〜180秒。高負荷スプリント（`elite`）も「スプリント1本」の目標が要るため通常帯で判定した。**ボススプリントは easy 99.4% / normal 96.3% / hard 91.9% が規定下限（90秒）を下回り**、p50 は easy 32.0秒 / normal 40.8秒 / hard 43.5秒。山場であるはずのボスが通常スプリントとほぼ同じ長さで終わっている。通常スプリントも easy で 97.3% が規定下限（60秒）を下回り、**59.6% は絶対下限の30秒すら割る**（p50 25.2秒、p10 10.9秒）。normal は 91.5% が下回り 45.9% が30秒未満、hard は 77.9% / 21.9%。p50 が規定帯に入るのは hard の高負荷スプリント（83.6秒）と nightmare（104.7秒）だけで、**初見が最初に触れる easy が最も規定から外れている**。既存のペーシング回帰テスト（`tests/unit/sprintTempo.test.ts`）は無介入経路で通常スプリントの絶対下限・p50・p90 を検査しているが、対象は normal のみ。ボスは上限しか検査しておらず、下限（90秒）と高負荷スプリントは未検査。

受入条件:

- 全難易度で通常スプリントの p50 が第3.1 の 60〜120秒帯に入り、絶対下限30秒未満が生じない。
- ボススプリントの p50 が 90〜180秒帯に入る。通常スプリントより明確に長い。
- 高負荷スプリント（`elite`）も時間目標の対象として検証する。
- ペーシング統計検証（RI-66）を全難易度・全スプリント種別・下限と上限の両方へ拡張する。

### RI-75 勝利種別が実質2種で、最も受動的なプレイが最上位勝利を取る

1,040ラン中366勝の内訳は `healthy` 208（57%）/ `noDamage` 157（43%）/ `happiness` 1 で、`aiSuccess` / `management` / `chaos` / `normal` は0件。`evaluateWinType`（`src/sim/outcome.ts:103-118`）が固定の優先順ラダーで、先頭の判定が `!usedHeavyActions && totals.spread === 0` のため、残業もアンドンも使わない受動的なプレイほど「やり込み枠」であるはずのノーダメージ勝利になる。実測でも勝利種別は「重アクションを使ったか」だけで分岐している。組織診断も同様に偏る（`seniorSacrifice` 588 / `reviewHell` 394 / `documentationKingdom` 29 / `aiOverproduction` 19 / `healthyAcceleration` 9 / `reworkSpiral` 1）。ただしビルド方針ごとに見ると診断は明確に分かれており（`noAi` は `documentationKingdom` 10 / `healthyAcceleration` 5、`aiFullBet` は `reviewHell` 12 / `seniorSacrifice` 28）、内部状態は分岐しているのに勝利種別へ反映されていない。SPEC 第19.1 F-10 が求める「ビルドによって目指す勝ち筋が変わる」が成立していない。

受入条件:

- **勝利種別は自動判定のまま維持する**（プレイヤーによる宣言制は採らない。方針決定 2026-07-26）。
  直すのは判定側で、`evaluateWinType` の固定優先順ラダーがビルドの違いを潰している点。
- ノーダメージ勝利が、受動的なプレイでは到達しない難度になる。
- 組織診断がビルドで分かれている（`noAi` は `documentationKingdom`、`aiFullBet` は `reviewHell` 中心）
  のに勝利種別が2種へ潰れている状態を解消する。診断とビルド指標を判定へ反映させる。
- 代表 seed × 複数ビルドで、勝利種別が3種以上に有意に分かれることを検証する。

### RI-76 AI 導入が既定 ON で「AI を入れるか」の意思決定が存在しない

初期ロスターは `aiAssigned: arch.preferred === 'coding'`（`src/sim/member/roster.ts:135`）でコーダーに最初から AI が配られており、既定プレイの AI 利用率は 82.5%、最終 AI 依存度は 100。明示的な「AI 全配布」方針（`aiFullBet`）は既定とほぼ同じ結果になる。一方 AI 配布を全解除すると別のゲームになる（easy スプリント平均で 出荷 334.5→291.4、AI 利用率 82.6%→0%、最終 AI 依存度 100→33.4）。因果自体は正しく効いている。

選択の入口は存在する。新規ランは第1スプリント前に必ず `SetupScreen`（見出し「編成 — スプリント開始前に配置とAIを決める」）を通り、`FormationScreen.tsx:172` の「🤖 AI配布中」ボタンから `setMemberAi` で解除できる。したがって課題は場の不在ではなく次の3点である。(1) 既定が ON で、既定のまま進むのが有利なため、選び直す動機が無い。(2) 解除側の代償が一方的に大きい。`noAi` は全難易度合計 9/40 で、`budgetExhausted` 敗北25件のうち14件を占める。AI で稼げないぶん予算を維持できず、AI 導入が実質必須になっている。(3) 導入が編成画面の on/off だけで段階が無く、「どこまで広げるか」を四半期を通じて問い直す機会が無い。

受入条件:

- 開始時の既定値が、そのまま進めるだけで有利な状態でなくなる（既定のまま／解除の双方に成立条件がある）。
- AI を入れない選択が、目標未達による敗北へ一直線に繋がらない（別の勝ち筋が成立する）。
- AI の導入度合い（誰に配るか・どこまで広げるか）が、ラン中に複数回問い直される段階的な意思決定になる。
- 開始時 AI の全面撤廃は取らない。`SetupScreen` / `FormationScreen` の既存の編成上の意思決定を重複して作り直さない。
- AI あり／なしの結果差が代表 seed で検証されている（`tests/unit/helpers/aiAdoptionSeeds.ts` の枠組みを利用する）。

### RI-77 カードが集中力だけを消費し、勝率を動かさない

当初「介入すべき状況が盤面に現れない」としていたが、これは誤りだった。毎 tick 全8種の介入を試す `probe` 方針で発動可能回数の上限を測ると、成立回数は easy p50=7 / hard p50=8 / nightmare p50=8 で、いずれも SPEC 第3.1 の「1スプリントあたり 3〜8回」の帯に収まる。介入の機会自体は足りている。

実プレイ相当の方針が p50=1〜3 に留まる原因は集中力の奪い合いである。カードだけを外す統制条件（`skilledNoHire` → `skilledNoCards`。採用条件は両方 `skip` で揃えてある）では、集中力不足による介入失敗（`no-focus`）が完全に消え、介入成立数が 1.1〜2.2 倍になる（easy ok=213 / no-focus=258 → ok=350 / no-focus=0。normal 269/718 → 582/0。hard 446/2006 → 476/0）。にもかかわらず勝率は変わらない（easy 8/10→9/10、normal 5/10→5/10、hard 3/10→2/10。全40ランでは 16 と 16）。つまりカードは共通資源である集中力を大量に消費しながら、結果に寄与していない。SPEC 第19.1 F-2 が求める「スプリント間の投資が結果を変える」が第2層で成立していない。

対象不足（`no-target`）はアクションによって大きく異なる（単一介入方針で測った、試行のうち `no-target` で失敗した割合）。`firefight` は easy 89.5% / normal 86.9% / hard 82.2%、`assignTask` は easy 78.9% / normal 90.2% / hard 94.9% / nightmare 98.2%、`interruptReview` は 41.7〜57.5%、`splitPr` は 45.8〜60.7%。一方 `overtime` / `andon` / `pairReview` / `aiThrottle` は対象を要さず 0%。とくに `assignTask` は難易度が上がるほど空振りし、`firefight` も easy で約9割が空振りする。

受入条件:

- カードの発動が勝率・組織状態に有意な差を生む（統制条件との比較で確認できる）。
- カードと介入が集中力を共有する構造を残す場合、どちらを取るかの正解が難易度で固定されない。
  共有をやめる場合は、カード専用のコスト資源を導入して介入の集中力予算と切り離す。
- 対象を要する4種（`firefight` / `assignTask` / `interruptReview` / `splitPr`）について、対象の発生頻度と撃ちたくなる頻度が釣り合う。とくに `assignTask` が高難易度でほぼ空振りする状態を解消する。
- 上記を `npm run playtest` の統制方針（`skilledNoCards` 等）で検証し、回帰を検知する。

### RI-78 予算枯渇・信頼枯渇が予兆なく終わり、敗因ラベルが実態と一致しない

敗因ごとの進行速度は明確に異なり（敗北までのスプリント数 p50 が `aiDependency` 1 / `seniorBurnout` 2 / `moraleCollapse` 2 / `techDebt` 3 / `reviewFreeze` 7 / `budgetExhausted` 11 / `reorgRequired` 18 / `trustExhausted` 24）、この点は F-9 として良い。問題は予兆とラベルの2点である。

第一に、`budgetExhausted` / `trustExhausted` / `reorgRequired` は直前スプリントのシニアHP 54〜63・士気 98〜100 のまま終わる。盤面上は健全に見えるのに終了するため、F-6（次の一手が分かる）と F-8（詰みの確定を作らない）を満たさない。

第二に、**`trustExhausted` ラベルが3種類の原因を1つに潰している**。`loseReasonForOutcome`（`src/sim/run/quarterReview.ts:464`）は `missed_crisis` と `shutdown` の**両方**を `trustExhausted` に変換する。1,040ランでの発火条件の内訳は、`missed_crisis` が `trust<=15` のみ19件 / `budget<=5` のみ7件 / `missedCount>=4` のみ0件、`shutdown` が `seniorHp<=5 かつ missedCount>=2` 2件。つまり `trustExhausted` 28件のうち信頼由来は19件で、**残り9件は予算切れ（7件）とシニア枯渇（2件）**である。予算やシニアが原因のランに信頼回復を促しても解決しない。

受入条件:

- 予算・信頼が危険域に入ったことが、数スプリント前から HUD で警告される。
- 危険域に入った後に、予算・信頼を回復・延命する打ち手が存在する。
- `missed_crisis` と `shutdown` の発火条件（信頼 / 予算 / KPI未達件数 / シニア枯渇）が敗因ラベルと表示に反映され、
  プレイヤーが実際の原因を特定できる。
- 敗因別に、直前スプリントの盤面指標が危険域に入っていることを `npm run playtest` で検証する。

### RI-79 スプリント評価が S に偏る

1,040ラン・7,652スプリント超の評価分布は S が61%台、A が21%台で、S と A が82%を占める。同一難易度・同一スプリント番号で無介入 `passive` と熟練 `skilledNoHire` の中央値を比べても、8条件中5条件で一致し、差が出るのも1段階のみ。RI-79 の受入条件「無介入の評価中央値が熟練方針より明確に低い」を満たさない。

受入条件:

- 無介入方針の評価中央値が、熟練方針より明確に低い。
- 評価分布が特定ランクへ半数以上偏らない。

### RI-80 ドラフトのマリガンが無い

`DraftScreen` の選択肢は「3枚から1枚選ぶ」か「スキップ」のみで、コストを払って引き直す手段が無い。カードプールも9枚（`src/data/cards.ts`）で、うち4枚はメタ解放が必要なため初期プールは実質5枚。SPEC 第19.1 F-12 が求める「事故で負けた」→「コストを払ってでも引き直すか」への変換が成立していない。RI-77 のとおりカードは現状ほぼ結果に寄与していないため、マリガン導入はカード自体の効果量見直し（RI-77）とセットで扱う。

受入条件:

- ドラフトでコスト（予算・集中力・信頼など）を払って引き直せる。
- 引き直しコストが、引き直すかどうかを迷う水準に設定されている。
- カードプールが、ドラフト3枚の組み合わせに意味が出る規模まで拡張されている。

### RI-81 敗北画面に「次の一手」が無い

`RunResultScreen` が敗北時に示すのは敗因ラベル＋一文の説明（`LOSE_LABEL`、`src/ui/RunResultScreen.tsx:26-52`）、組織タイプ診断、メタ進行ポイントで、次のランで何を変えるかは提示されない。教訓文（`FAILURE_LESSONS`、`src/sim/diagnosis.ts:64-81`）は初回敗北でも表示される（`recordIfFinished` が `applyRunReward` 経由で失敗診断を `collectedDiagnoses` へ加えてから画面を更新するため、`failureInCollection` は初回時点で true）。問題は対応範囲と粒度で、教訓は敗因（`LoseReason` 10種）ではなく組織タイプ診断に紐づき、しかも `FAILURE_LESSONS` が対応するのは失敗診断4種のみ。診断が `healthyAcceleration` / `documentationKingdom` のまま敗北すると `failureEntry` が存在せず教訓が一切出ない。また `budgetExhausted` / `trustExhausted` / `aiDependency` / `reorgRequired` で負けても表示は診断由来の一般論で、その敗因に対する次の一手にならない。SPEC 第19.1 F-6 は「初見は負けて学ぶ」の前提条件であり、学びが敗因に噛み合っていない。

受入条件:

- 敗因（`LoseReason`）に対応した「次のランで変える具体的な一手」が表示される。
- 診断が失敗4種以外でも、敗北時に必ず教訓が表示される。
- 教訓が現実の開発現場への示唆としても読める文言になっている（SPEC 第20章）。

### RI-82 目標修正の選択が結果を変えない

目標修正だけを固定する統制方針（**全6選択肢**、各40ラン。うち実際に該当の修正が提示され選べたのは各17ラン。修正を選んだ四半期は 1.3〜1.5 で揃っており事前状態は比較可能）の勝率は `extend_deadline` 52.9% / `request_budget` 52.9% / `cut_scope` 58.8% / `pause_ai_rollout` 64.7% / `reorg_teams` 70.6% / `quality_pivot` 76.5%。効果がまったく異なる選択肢（スコープ削減 / 予算追加 / AI 停止 / 組織リセット）を並べても 52.9〜76.5%（最大差は17ラン中4勝ぶん）に収まり、n=17 では有意と言い切れない。目標修正フェーズ自体は1,040ラン中409ラン（39.3%）で発生しており選択機会は十分にある。SPEC 第19.1 F-2 が求める「四半期レビューの目標修正でジレンマが成立する」を満たさない。

なお当初の集計では `quality_pivot` の勝率を 83.3% としていたが、これは統制していない方針のランが提示順の先頭である `cut_scope` 群へ大量に混入した結果のアーティファクトだった。また「到達四半期」に終端の `quarterNumber` を使っていたため、修正を選んだ時期ではなく後続のラン長を見ていた。いずれも訂正した。

受入条件:

- 目標修正の選択肢ごとに、後続四半期の進み方（勝率・到達四半期・組織状態）が有意に分かれる。
- 各選択肢に、それが有利になる局面と不利になる局面がそれぞれ特定できる（F-1）。
- 選んだ代償が次四半期以降に現れることが、プレイヤーに追跡できる形で提示される。
- 統制方針での比較を `npm run playtest` で検証し、回帰を検知する。

### RI-83 条件を揃えると介入の寄与がほぼ消える

`noInterventionCtl`（`skilledNoHire` から介入だけを外し、カード・進化・採用を揃えた統制条件）との比較で、勝率は easy 8/10・normal **7/10**・hard 1/10（全40ランで16）。対する `naive` は 6/5/2（13）、`skilledNoHire` は 8/6/3（17）。**normal では介入しない方が勝っており**（7 vs 6）、全40ランでも熟練の介入との差は +1、初見の介入はむしろ 3 下回る。分散も同様で、同一スプリント番号の出荷 CV に方針間の一貫した差は出ない。

当初「介入は勝率を改善している（完全放置 7 → 熟練 16）」としていたのは、進化・カード・採用が同時に変わっていたためだった。介入だけを統制すると期待値でも分散でも寄与がほとんど無い。SPEC 第19.1 F-5 は「無介入は損ではなく不安定。介入の価値は結果の偏差を抑えること」と定義しているが、実装は期待値も分散も動かしていない。

受入条件:

- **実装を定義に寄せる**（F-5 の定義は変えない。方針決定 2026-07-26）。介入に下振れを潰す性質を
  持たせ、無介入の分散を大きくする。介入は期待値の底上げではなく不確実性の抑制として設計する。
- `noInterventionCtl` と介入方針の間に、同一スプリント番号の出荷 CV で有意な差が出る
  （介入側が明確に低い）。
- 介入しない方が勝つ難易度が無い。
- 判定を `npm run playtest` の統制方針で検証し、回帰を検知する。

### RI-84 レビュー凍結が選択不能な判定イベントでしか確定しない

敗北を確定させたフェーズを、ビートについては judgment（選択不能な判定）と decision（プレイヤーが選ぶ）に分けて記録した。全674敗の内訳は `beat:judgment` 279（41%）/ `sprint` 261（39%）/ `beat:decision` 89（13%）/ `quarterReview` 39（6%）/ `shop`・`recruit` 6。

敗因ごとに決着位置は分かれる。**`reviewFreeze`（183件）はほぼ100%が `review-freeze`(judgment) で確定**し、盤面で対処する機会がない。プレイヤーはスプリントを走り切った直後に、操作の余地がない画面で敗北を告げられる。一方 `seniorBurnout`（183件）は judgment 87 / **decision 65**（`giant-pr` 45、`ai-test-gen` 11 など）/ sprint 11 で、過半が自分の選択を経て確定する。`aiDependency`（215件）・`techDebt`・`moraleCollapse` はスプリント中、`trustExhausted`・`reorgRequired` は四半期レビューで決まる。

当初この所見は敗北の大半が「操作の余地がない画面」で確定するとしていたが、これはハーネスがビートの選択肢を常に先頭で取っていたためだった（`urgent-demo` の先頭は `seniorHp -10 / morale -15`）。状態を見て選ぶ方針へ変え、judgment と decision を分けて記録した結果、対象は `reviewFreeze` に絞られた。

受入条件:

- `reviewFreeze` の敗北が、スプリント中に予兆と対処の機会を伴って決着する。または判定イベントで確定する前のスプリントで明確な警告が出る。
- 敗因ごとの決着フェーズ分布（judgment / decision / sprint / quarterReview）を `npm run playtest` で検証し、回帰を検知する。

### RI-85 Q1 の進化解放が分散し、ビルドの方向が確定しない

SPEC 第19.1 F-11 は「どの方向で回すかが Q1 の途中で見え、以降はその方向を伸ばすか曲げるかの判断になる」ことを求める。Q1 の進化解放が特定ブランチへ寄っているか（同一ブランチ2ノード以上、かつ Q1 解放の過半）で判定すると、方向が確定したランは easy 8/260（3.1%）/ normal 5/260（1.9%）/ hard 22/260（8.5%）に留まり、残りのほぼ全ランが「複数ブランチへ1ノードずつ」という分散した解放になっている。進化ポイントの入手量に対してノードのコストが低く、方向を選ぶ前に一通り取れてしまう。方針側で解放順を AI 優先・品質優先に分けても、Q1 の時点では分岐が現れない。

当初この所見は F-11 を成立としていたが、それは「Q1 に1ノードでも解放すれば方向が決まった」と数えていたためだった。

受入条件:

- Q1 の終わりまでに、進化解放が特定ブランチへ寄るランが多数派になる。
- ブランチを選ぶことが、選ばなかったブランチを諦めることを意味する（コストか排他性）。
- Q1 の解放ブランチ分布を `npm run playtest` で検証し、回帰を検知する。

### RI-86 セキュリティ軸が存在しない

SPEC 第19.1 F-10 のビルド表は「セキュリティ軽視」を想定ビルドの1つに挙げているが、ゲーム内にセキュリティの概念が無い（`src/` で該当する状態値・カード・イベントは存在せず、`security` の語はボス名にのみ現れる）。検証を省いて速度を買い、事故の期待値と信頼の下振れを引き受けるというトレードオフが成立しない。実装する（方針決定 2026-07-26）。

受入条件:

- 組織状態にセキュリティ水準を持ち、低いほど事故（`incident`）の規模または信頼低下が大きくなる。
- 検証を省いて速度を取る選択が、短期の出荷増と引き換えに下振れリスクを増やす形で成立する。
- セキュリティ軽視ビルドが `npm run playtest` の方針として定義でき、他ビルドと結果が分かれる。

### RI-87 インフラコスト軸が存在しない

SPEC 第19.1 F-10 のビルド表は「自前エージェントハーネス（肥大）」と「同（最適化）」を想定ビルドに挙げているが、ゲーム内にインフラコストの概念が無く、予算は採用・ショップ・レバーでしか動かない。AI を使うほど継続コストが積み上がり、コスト最適化がそれを抑えるという構造が成立しない。実装する（方針決定 2026-07-26）。

なお試練には `frontierModelCostPerDependency`（AI依存度あたりの利用コスト）が既にあり、これを恒常的な軸へ広げる形が最小の実装になりうる。

受入条件:

- AI 利用量または自動化水準に応じた継続コストが予算を圧迫する。
- コスト最適化への投資が、初期は損だが長期では回収できる選択として成立する。
- 肥大ビルドと最適化ビルドが `npm run playtest` の方針として定義でき、結果が分かれる。

### RI-34 ローカル完結の将来拡張

外部API・共有バックエンドは対象外。着手時に個別IDへ切り出す。

- ツール別シナリオ
- 組織シナリオ共有（ローカルファイル／文字列による入出力）
- 組織診断ダッシュボードの時系列・部門比較
- 四半期レビューの高度化

カードデッキの研修方針、デイリーラン、AI導入失敗図鑑、レビュー地獄リプレイ、「なぜ燃えたか」は実装済み。社内LT／経営プレゼンモードと外部サービス連携は対象外。

## 2. 確定した判断

2026-07-20のSPEC監査で以下を確定した。

- Zustandは要件ではない。`RunEngine`を状態の正本とし、決定論、保存、リプレイ、UI同期を満たせばよい。
- 巨大組織は表示用の合成チームではなく、独立状態を持つ複数チームとして扱う。
- カード、レリック、進化ノードのSPEC内の具体例は方向性であり、実装データとの差を許容する。
- カードはラン外のコレクション画面で一覧・解放状況を確認できるようにする。

## 3. 完了項目の要約

| グループ | ID | 完了内容 |
| --- | --- | --- |
| 描画・演出 | RI-01〜RI-10, RI-59, RI-63 | 全社／部署／業界の等角表示、Pixi盤面、カメラ、粒・表情・延焼・報酬・音響 |
| 技術基盤 | RI-11〜RI-13, RI-39, RI-57, RI-58, RI-61 | Pixi既定化、分割ロード、Worker/Recharts、遷移契約、IndexedDB、セーブ、リプレイ |
| バランス | RI-14〜RI-19, RI-41, RI-56, RI-62, RI-66 | モンテカルロ許容レンジ、代表seed、介入効果、速度UIと通常／ボス／四半期／ランのペーシング統計 |
| メタ・コンテンツ | RI-20〜RI-32, RI-42, RI-65 | コレクション、診断演出、デイリー、採用・育成、試練、レリック、操作方式、カード図鑑 |
| ラン進行 | RI-33, RI-35〜RI-40 | 固定四半期トラック、イベント判定、契約テスト、通しテスト |
| UXフィードバック | RI-43〜RI-55, RI-60 | 効果タグ、what-if、HUD、介入演出・ログ・分析、チュートリアル |
| 組織スケール | RI-64 | 独立チーム状態、粗粒度 sim、入り込みコスト、チーム施策、保存／リプレイ |
| 第23章の実装済み拡張 | RI-34′, RI-34″, RI-34‴, RI-34⁗ | 炎上原因、失敗図鑑、レビュー地獄リプレイ、研修方針 |

RI-24とRI-25は「追加実装しない」と決定して完了した項目を含む。完了項目の具体的なPR、係数、当時の検討過程はGit履歴を参照する。

## 4. バックログ運用

- IDは`RI-NN`の連番とし、欠番を再利用しない。
- 状態は`未着手`、`進行中`、`保留（要判断）`、`完了`を使う。
- 1項目は原則1PRで完了できる大きさに分割する。エピックは受入条件と分割順を記載する。
- 完了時は詳細な実装日誌を追記せず、上の完了要約と[spec-mapping.md](./spec-mapping.md)を更新する。
