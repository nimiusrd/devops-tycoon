# プレイテスト所見（2026-07-20）

実機プレイで洗い出した改善課題の記録。バランス・オンボーディング・UI・ポリッシュを対象とし、
実行可能な項目は[remaining-issues.md](./remaining-issues.md)へ `RI-67`〜`RI-71` として登録した。

## 検証条件

- ビルド: `npm run dev`（ポート5174）／DOMレンダラ（`?renderer=dom`）
- seed: `review-hell`、難易度: Easy、試練なし
- `window.game`（第22.5）ファサードでフェーズを駆動し、タイトル→スプリント→リザルト→ドラフト→進化→ボス→四半期レビューまで通しプレイ
- プレイ方針を3通りで比較（無介入 / 介入のみ / 編成でレビュー分散＋介入）

## 結論の要約

コアループは良プレイで第2四半期に勝利でき、リザルト・組織診断・盤面演出・決定論の完成度は高い。
一方で、初見・受動プレイヤーの体験と、四半期レビューの看板KPIに明確な課題がある。

## 課題一覧

### RI-67 オンボーディングとシニア燃え尽きの断絶（優先度: 高）

Easy でも「素直に遊ぶ」と第1四半期スプリント2でシニア燃え尽き（`seniorBurnout`）敗北する。

同一 seed（`review-hell`・Easy）で、介入と編成を切り分けた統制実験の結果:

| プレイ方針 | 結果 |
| --- | --- |
| D 無介入（自動進行のみ） | Q1 sprint2 で `seniorBurnout` 敗北 |
| A 編成のみ（middle を review へ、介入なし） | それでも Q1 sprint2 で敗北 |
| B 介入のみ（緊急対応＋andon、編成そのまま） | 12スプリント以上を燃え尽きずに継続 |
| C 編成＋介入（B に編成変更を追加） | Q1 sprint2 で敗北 |

敗北条件は `org.seniorHp <= 1`（`src/sim/outcome.ts:57`）で、これはメンバー個別の stamina ではなく
**抽象的なグローバル値**である。`org.seniorHp` の主な減少要因は次の2つ:

- レビュー1件ごとの一律消費 `REVIEW_HP_COST`=1.6（`reviewOne`、`src/sim/sprint.ts:252`）。担当のランク・
  人数に関わらず同額を削る。
- 猶予切れの炎上を受動処理する**自動鎮火** `INCIDENT_HP_COST`=12（`advanceBurning`、`src/sim/sprint.ts:340`。
  リザルトの `シニアHP -12` の主因）。緊急対応（firefight）で先に鎮火すれば `FIREFIGHT_HP_COST`=2 で済む。

スプリントは全タスクが Done になるまで進む（`isDrained`、`src/sim/sprint.ts:480`）。実測では第1スプリントは
seed 固定 25 タスクを endTick 46〜71 で完了し、`maxTicks`=1500（`src/sim/scenarios.ts:46`）の打ち切りには
達しない。ただしレビュー**回数**はタスク数固定ではない: 手戻りタスクは Rework 経由で Review へ戻り
（`advanceRework`、`src/sim/sprint.ts:378`）、緊急対応で鎮火したタスクも Review へ戻る（`fire.lane='review'`、
`src/sim/actions.ts:151`）ため、レビュー試行数＝タスク数＋（手戻り・炎上の再レビュー）となり、手戻り率・炎上率に
応じて増減する。したがって**手戻り率**を下げる施策（品質・PR分割・AIスロットル。`reworkProbability` の quality・
`task.split`・aiAssisted・aiDependency 項）は再レビュー回数を減らし、`REVIEW_HP_COST` と炎上判定回数を**間接的に**
減らしうる。ただしこれは「レビュー1件あたりの点火率（`incidentProbability`）」を下げるのとは別で、点火率が参照するのは
testCoverage・`task.aiAssisted`・カード効果のみ（品質値・`task.split` は参照しない）。一方 `org.seniorHp` は
毎 tick `REVIEW_HP_REGEN`=0.7
（炎上中は `BURNING_REGEN_MUL`=0.5 で半減、`src/sim/sprint.ts:474`）で回復し、炎上確率 `incidentProbability` は
レビュー1件ごとに評価され**待ち行列長を入力に取らない**（`src/sim/model/process.ts`）。よって「待ち行列を短くすれば
点火が減る」は成り立たない（当初この所見が編成の待ち行列短縮を生存要因としていた点を、Codex レビューの指摘と
下記実測で訂正）。

第1スプリントの実測（同一 seed、条件別の自動鎮火回数と終了時 `org.seniorHp`）:

| 条件 | endTick | 自動鎮火(−12) | HP終値 |
| --- | --- | --- | --- |
| D 無介入 | 65 | 3 | 42 |
| A 編成のみ | 54 | 3 | 36 |
| 緊急対応のみ | 62 | 1 | 62 |
| B 緊急対応＋andon | 71 | 0 | 74 |
| C 編成＋介入 | 46 | 1 | 55 |

読み取れる実際の要因（直接測定できた範囲）:

- **HP を最も動かすのは自動鎮火（−12）の回数**で、これを直接減らすのが **緊急対応**（−12 になる前に −2 で鎮火。
  ただし鎮火後は Review へ戻り再レビュー1件ぶんの `REVIEW_HP_COST` は掛かる）。緊急対応のみで自動鎮火 3→1、
  HP 42→62 に改善し、これは条件を1つだけ変えた比較として最も確度が高い。andon はレビュー件数を減らすのではなく、
  流入を一時停止（`intake` の早期 return、`src/sim/sprint.ts:183`、通常 30 tick）して**既存キューを捌く猶予**を作り、
  同時炎上を抑えて緊急対応を間に合わせる補助（B で自動鎮火 0）。
- **編成（middle を Review へ）は単一要因ではない**。review 増員（`reviewCapacityMul`）だけでなく Coding 担当が減り、
  `foldFormationEffects`（`src/sim/member/roster.ts:251`）経由で `codingSpeedMul` / `codingSlotBonus` / `aiAdoptionShare`
  と AI 由来の確率補正が次スプリント生成に同時適用される。C（編成＋介入）が B（介入のみ）より早く燃え尽きた事実は
  確かだが、複数因子が連動するため B/C 差を「レビュー速度×回復」等の単一機序に帰属できない。確実に言えるのは、
  **編成変更はシニアHP保護の主手段ではなく（A は無介入と同じく即死）、燃え尽き回避を編成に頼らせるべきでない**こと。
  厳密な寄与分解には Coding 側を統制した比較が要る（RI-66 の統計検証と合わせて後追い）。
- AIスロットルは流入を止めず、新規タスクの `aiAssisted` を false にして AI 由来の手戻り・炎上確率
  （`reworkProbability` / `incidentProbability` の aiAssisted・aiDependency 項）を下げる。上記のとおり手戻り減は
  再レビュー減につながるため、HP 消費・炎上判定の**間接的な**緩和にはなる。

素直プレイが即死する構造は、シニア1人＝唯一のレビュアー（`src/data/members.ts` `STARTER_ARCHETYPES`）で
炎上が起きやすいうえ、チュートリアル（`src/ui/tutorial.ts`）が介入バー・レビュー渋滞・コンボの3点のみで、
**シニア体力（燃え尽き）と、燃え尽きを防ぐ主手段＝炎上を自動鎮火前に緊急対応で鎮火することを教えていない**ため。
Easy は導入難易度（「モダンなプロダクトチーム」）であり、この即死は体験を損なう。

改善方向（いずれか／組み合わせ）:

- チュートリアルに「シニア体力（燃え尽き）」と、それを防ぐ最大手段＝炎上を自動鎮火 −12 になる前に緊急対応で
  −2 鎮火することを追加する。補助として、andon（既存キューを捌く猶予）、AIスロットル（レビュー1件あたりの
  点火率と手戻り率を下げる）、品質・PR分割（手戻り率を下げて再レビューを減らす。点火率自体は下げない）も
  位置づける。編成での review 増員はスループット策であり燃え尽き回避の主手段ではない点を明確にする。
- `org.seniorHp` の収支（`REVIEW_HP_COST` / `INCIDENT_HP_COST` / `REVIEW_HP_REGEN`、`src/sim/model/process.ts` ほか）や、
  Easy 序盤の障害率を見直し、標準的な操作理解のプレイが2スプリントで即死しないようにする。
- 燃え尽きが近いときに盤面・HUD で明確に警告し、対処アクション（緊急対応・andon・スロットル）へ誘導する。

### RI-68 四半期レビュー Delivery KPI のスケール不整合（優先度: 高）

第2四半期レビューで **Delivery 目標20 に対し実績1693（約80倍超過）**、Quality/Morale/TechDebt も
すべて「超過」。目標生成（`src/sim/run/quarterReview.ts` の `deliveryTarget`、概ね1スプリント相当の
`minSprintDelivered` ベース）に対し、判定は四半期累計 `quarterTotals.delivered` を比較しているため、
看板KPIの「出荷ポイント＝勝利条件の進捗」が常に自明達成となり意味を失う。プレイヤーには
「目標20 → 実績1693 超過」が壊れて見える。

改善方向:

- `deliveryTarget` を四半期の実出力スケールに合わせて設定する、
  または判定・表示の単位（1スプリント平均 vs 四半期累計）を一致させる。
- 実際に効いている制約（Morale・Quality・Incident・燃え尽き）が勝敗条件として伝わる表示にする。

### RI-69 スプリント上部操作バーと盤面の重なり（優先度: 中）

デスクトップ幅（1440px、DOM レンダラ `?renderer=dom` で実測）で、上部の操作バー（`通常スプリント /
❚❚ / 1x-2x / 渋滞メーター / 炎上タイマー / COMBO`）が盤面領域の上端と重なり見切れる。1行への詰め込み過多。
DOM 経路の盤面は Pixi キャンバスではなく DOM 要素（`FlowArrows` / `TaskDot`）で描画されるため、これは
レンダラ非依存のヘッダ／盤面コンテナのレイアウト問題として扱う（Pixi レンダラでも別途再現確認する）。
対象: スプリント画面のヘッダ／盤面レイアウト（`src/ui/SprintScreen.tsx` 周辺）。

### RI-70 モバイルのスプリント操作性（優先度: 中）

390px 幅で横スクロール・崩れは無い（`documentElement.scrollWidth === innerWidth`）が、縦積みの
KPIカードの下に盤面とアクションバーが来るため、時限スプリント中に盤面のタスク流動と介入ボタンを
見るのにスクロールが必要。リアルタイム介入ゲームとして操作性が落ちる。
改善方向: 狭幅ではKPIを折り畳み／要約し、盤面＋アクションバーを一画面に収める。

### RI-71 favicon 未設定による 404（優先度: 低）

`index.html` に favicon 宣言が無く `public/` にも実体が無いため、`/favicon.ico → 404` が毎ロード時に
コンソールへ出る（`curl` で 404 を確認）。ブラウザタブのアイコンも空。`<link rel="icon">` の追加と
アイコン配置で解消できる。

## 参考: 既存の未充足（プレイでも該当を体感）

- RI-64 独立状態を持つ複数チーム運営（全社・部署ビューはプレイヤーチームからの合成）
- RI-66 ペーシング統計検証の補強

### RI-83 介入が結果の分散を下げない（優先度: 中 / F-5）

SPEC 第19.1 F-5 は「無介入は損ではなく不安定。介入の価値は期待値の底上げよりも**結果の偏差を抑えること**」と
定義している。ラン長の差が混入しない指標で測ると、この関係が成立していない。

easy の出荷の変動係数（CV）:

| 方針 | 1スプリントあたり出荷 CV | 第1スプリント出荷 CV |
| --- | --- | --- |
| `idle`（完全放置） | 26.6% | 10.6% |
| `passive`（無介入） | 23.4% | 10.6% |
| `naive`（初見相当） | 20.6% | 11.3% |
| `skilled`（上級者相当） | 23.7% | **12.7%** |
| `onlyFirefight` | 20.4% | 9.7% |

全ランが到達する第1スプリントで見ると、**介入する方針ほど分散がわずかに大きい**（`skilled` 12.7% が最大）。
1スプリントあたり出荷でも 26.6% → 20.6〜23.7% と、差はごく小さい。
normal / hard でも同様で、介入による明確な分散抑制は観測できない。

一方で勝率は改善している（RI-72 の表）。つまり現状の介入は**期待値を押し上げる手段**であって、
F-5 が想定する**不確実性を抑える手段**にはなっていない。

なお第1回の集計では累計出荷の CV が 57.9% → 38.6% と下がることを根拠に F-5 成立としていたが、
これは早期敗北したランほど累計出荷が機械的に小さくなるためで、
組織出力の安定性ではなくラン長の差を見ていた（Codex レビューの指摘により訂正）。

対応の方向は2つあり、どちらを取るかは設計判断である。

1. F-5 の定義を実装に合わせる（介入は期待値を上げる手段と位置づけ直す）。
2. 実装を F-5 に合わせる（介入に下振れを潰す性質を持たせ、無介入の分散を大きくする）。

受入条件:

- F-5 の定義と実装のどちらを動かすかを決め、SPEC 第19.1 に反映する。
- 実装側を動かす場合、無介入と介入で 1スプリントあたり出荷・第1スプリント出荷の
  変動係数に有意な差が出る。
- 判定を `npm run playtest` の CV 出力で検証し、回帰を検知する。

## 良かった点（回帰させないため記録）

- リザルト画面（ランク・タイムライン・「なぜ燃えたか」分析）
- Game Over の組織タイプ診断（例: Senior Sacrifice 型）
- isometric 盤面の演出とイベントティッカー
- seed 付き決定論、DOM/Pixi レンダラ切替

---


# プレイテスト所見 第2回（2026-07-26）— 面白さ定義に基づく判定

[SPEC 第19.1「面白さの定義と判定基準」](../SPEC.md)を先に定義し、その F-1〜F-12 に照らして
全難易度を回した記録。実行可能な項目は[remaining-issues.md](./remaining-issues.md)へ
`RI-72`〜`RI-82` として登録した。

## 検証条件

- 対象: `main`（`8629f5f`）
- 方法: `npm run playtest`（`tests/playtest/harness.ts`）。`RunEngine` の公開 API だけでランを自動プレイし、
  ラン結果・スプリント結果・介入の試行結果・四半期レビューの発火要因を全件 JSON へ記録する。
  集計は `npm run playtest:report`（`scripts/playtest-report.mjs`）。
- 母数: **4難易度 × 21方針 × seed `pt-1`〜`pt-10` = 840ラン**
- 方針は進化ツリーの解放順と採用経路まで区別する。初見相当（`naive`）は表示順に取り、
  上級者相当（`skilled`）はレビュー容量を優先する。採用方針は専用フェーズだけでなく
  ショップの採用枠と即時採用イベントにも適用する。
- **メタ進行は初見相当**（`PT_META=fresh`）。`game.startRun` と同じく `setUnlockedContent` で
  既定解放分のみをドラフト候補にする。全解放条件で回す場合は `PT_META=full`。
- 壁時計換算は `MS_PER_TICK_1X`=680ms（`src/ui/sprintTempo.ts`）を用いた 1x 基準

### プレイ方針（SPEC 第19.1.3 の観測に対応）

| 方針 | 内容 | ねらい |
| --- | --- | --- |
| `idle` | 介入もカードも一切使わない完全放置 | 下限ベースライン |
| `passive` | 介入なし・カードのみ発動 | F-5 の無介入ベースライン |
| `naive` | 初見想定。異常が目立ってから反応（炎上2件以上／レビュー12件以上）、600ms 刻み。進化はツリーの表示順に上から取る（どのブランチが有利かの知識を前提にしない） | F-7 の初見相当 |
| `skilled` | 上級者想定。閾値低め（炎上1件／レビュー6件）＋andon＋差配、300ms 刻み、採用あり（専用フェーズ・ショップ・即時採用イベントすべて）。進化はレビュー容量優先 | 熟練者の上限 |
| `skilledNoHire` / `skilledNoCards` | skilled から採用を外した条件／さらにカードも外した条件 | 交絡の切り分け |
| `onlyXxx`（8種） | 単一介入だけを打ち続ける | F-1 の固定強手検出、アクション別の対象不足 |
| `aiFullBet` / `noAi` / `reviewHeavy` | AI 全配布 / AI 配布なし / レビュー寄せ編成 | F-10 のビルド差分 |
| `adjCutScope` / `adjExtendDeadline` / `adjQualityPivot` | 目標修正だけを固定して選ぶ | F-2 第4層の統制比較 |
| `probe` | 全8種の介入を 1 tick ごとに試行（順序は tick ごとに回転） | 同時に撃ち続けた場合の成立数 |

`probe` は**発動可能回数の上限ではない**。8種を順に実際に発動するため、先行アクションが集中力と
盤面を消費して後続の機会を消す。アクション単体の対象不足は、順序の影響を受けない `onlyXxx` で見る。

## 結論の要約

**成立している基準**:

- **介入は勝率を改善している**。全40ランの勝利数は 完全放置 7 → 無介入 9 → 初見相当 12 →
  上級者相当（採用なし）16。乱打（`probe`）は 10 で損になる関係も成立している。
  ただしこれは**期待値の底上げ**であり、F-5 が要求する分散抑制とは別である（下記）。
- **F-9 敗因ごとに進行速度が明確に違う**。敗北までのスプリント数 p50 は
  `aiDependency` 1 / `seniorBurnout` 2 / `moraleCollapse` 2 / `techDebt` 3 /
  `reviewFreeze` 7 / `budgetExhausted` 11 / `reorgRequired` 18 / `trustExhausted` 30。

**成立していない基準**:

| 基準 | 未充足の内容 | 対応 |
| --- | --- | --- |
| F-1 | 緊急対応だけを打ち続ける方針が全21方針中で最強（22/40） | RI-72 |
| F-2 | 第2層（カード）が勝率を動かさず、第4層（目標修正）も選択で結果が変わらない | RI-77・RI-82 |
| F-4 | easy の通常スプリントは 56.7% が絶対下限30秒を割る。ボスは全難易度が規定下限割れ | RI-74 |
| F-7 | 勝率が easy 75% / normal 44% / hard 17% / nightmare 0%。初見の目標20%前後は hard のみ | RI-72 |
| F-8 | Nightmare は 210ラン全敗、うち165ランが第1スプリントの `aiDependency` | RI-73 |
| F-10 | 勝利種別が実質2種（`healthy` 153 / `noDamage` 131 / `happiness` 1、残り4種は0件） | RI-75・RI-76 |
| F-5 | 介入は勝率を上げるが、**結果の分散は下げていない**。F-5 は「介入の価値は分散抑制」と定義しているため、定義と実装がずれている | RI-83 |
| F-12 | ドラフトのマリガンが無い | RI-80 |

## 課題一覧

### RI-72 難易度カーブと、単一介入が複合方針を上回る構造（優先度: 高 / F-1・F-7）

難易度別の勝率（21方針 × 10ラン = 各210ラン）:

| 難易度 | 全体 | `idle` | `passive` | `naive` | `skilled` | `onlyFirefight` | 方針間レンジ |
| --- | --- | --- | --- | --- | --- | --- | --- |
| easy | **75%**（158/210） | 7/10 | 7/10 | 6/10 | 7/10 | **10/10** | 5〜10 |
| normal | **44%**（92/210） | 0/10 | 2/10 | 4/10 | 4/10 | **8/10** | 0〜8 |
| hard | **17%**（35/210） | 0/10 | 0/10 | 2/10 | 1/10 | 4/10 | 0〜4 |
| nightmare | **0%**（0/210） | 0/10 | 0/10 | 0/10 | 0/10 | 0/10 | 0 |

**F-7（初見相当の勝率20%前後）に合うのは hard（初見相当 2/10）だけ**。
easy は完全放置でも 7/10 勝て、nightmare は210ラン全敗（RI-73）。

**F-1 の違反**: 全40ラン合計の勝利数で見ると、単一介入の `onlyFirefight` が **22/40 で全21方針中トップ**。

| 方針 | 40ラン中の勝利 |
| --- | --- |
| `onlyFirefight`（緊急対応のみ） | **22** |
| `onlyAndon` | 19 |
| `reviewHeavy` | 17 |
| `skilledNoHire` / `skilledNoCards` | 16 |
| `naive` / `skilled` | 12 |
| `probe`（全部撃つ） | 10 |
| `passive` | 9 |
| `noAi` | 8 |
| `idle` | 7 |

緊急対応は「打てるときに打てば得」で、状況によって正解が入れ替わらない。
複合方針が単一介入に負ける以上、他の介入を選ぶ理由が薄い。

統制実験（同一 seed、条件を1つずつ外す。数値は勝率）:

| 条件 | easy | normal | hard |
| --- | --- | --- | --- |
| `skilled`（採用あり・カードあり） | 7/10 | 4/10 | 1/10 |
| `skilledNoHire`（採用なし・カードあり） | 8/10 | 5/10 | 3/10 |
| `skilledNoCards`（採用なし・カードなし） | 9/10 | 5/10 | 2/10 |

採用もカードも勝率を有意に動かしていない（n=10 の揺らぎの範囲）。
`budgetExhausted` 敗北は840ラン中18件に留まる。

### RI-73 Nightmare は AI 依存を意識しない方針で第1スプリント敗北が確定する（優先度: 高 / F-8・F-9）

nightmare は **21方針210ランすべて敗北**し、うち **165ランが `aiDependency`**。
敗北までのスプリント数 p50 は **1**（他の敗因は 2〜30）。

敗北条件は `org.aiDependency >= 95 && org.aiLiteracy <= 30`（`src/sim/outcome.ts:63-65`）で、
nightmare の初期値 `aiDependencyBase`=55 / `aiLiteracy`=25（`src/data/difficulties.ts`）は
リテラシー側を最初から満たしている。`aiDependency` は AI 割当タスク1件ごとに
`AI_DEP_PER_TASK`=2.2 加算され（`src/sim/sprint.ts:193`）、`taskCountMul`=1.2 のタスク量で
第1スプリント中に 95 へ到達する。

**回避経路は2種類ある**。依存度を下げる経路と、**リテラシーを上げて敗北条件そのものを外す経路**である。

| 方針 | `aiDependency` 敗北 | 平均継続スプリント | 最終 AI リテラシー | 最終 AI 依存度 |
| --- | --- | --- | --- | --- |
| `onlyPair`（ペアレビュー連打） | **0/10** | 2.6 | **62.2** | **100** |
| `onlyThrottle`（AIスロットル連打） | 0/10 | 2.5 | 43.3 | 60.8 |
| `noAi`（AI 配布を全解除） | 0/10 | 4.8 | 47.1 | 60.2 |
| `probe`（全介入を撃つ） | 2/10 | 3.6 | 49.5 | 98.2 |
| `skilled` / `naive` / `passive` ほか | 10/10 | 1.0 | 25.0 | 100 |

とくに `onlyPair` は**依存度100のまま**リテラシーを 25→62.2 へ上げて敗北を回避している。
`pairReview` の説明は「詰まったPRをペアで処理。AI Literacy が上がる」で、
これは SPEC 第19章の「AI は強い。しかし雑に使うと壊れる」に最も合致する対処である。

問題は回避不能なことではなく、**回避に必要な方針が事前知識を要し、外すと第1スプリントで終わる**こと。
初手で終わるため「負けて学ぶ」が成立しない。またリテラシー経路は
チュートリアルにもリザルトにも現れず、プレイヤーが発見する手掛かりが無い。

依存度を下げる手段も既に存在する（カード `ai-guideline` の `aiDependencyAdd: -10`、
レバー `aiGuideline` / `teamAiThrottle` の `aiDependencyDelta: -10`、`aiThrottleDept` の -8）。
不足しているのは手段の有無ではなく、既存手段の効果量・入手機会・発見性である。

### RI-74 スプリントが規定の半分以下で、難易度が低いほど短い（優先度: 高 / F-4・第3.1）

1x 換算の通常スプリント長:

| 難易度 | n | p10 | p50 | p90 | 30秒未満 | 60秒未満 |
| --- | --- | --- | --- | --- | --- | --- |
| easy | 1667 | 10.9秒 | **25.8秒** | 49.6秒 | **56.7%** | 95.7% |
| normal | 1680 | 16.3秒 | **36.7秒** | 63.9秒 | 35.3% | 87.3% |
| hard | 1429 | 28.6秒 | 46.2秒 | 80.2秒 | 11.3% | 73.1% |
| nightmare | 286 | 49.0秒 | 100.6秒 | 153.7秒 | 2.4% | 18.9% |

ボススプリント（規定 90〜180秒）の p50 は easy 34.0秒 / normal 43.5秒 / hard 50.3秒 /
nightmare 80.9秒で、**全難易度が規定下限を下回る**。

第3.1 の規定は「通常60〜120秒・最短でも30秒」だが、**初見が最初に触れる easy が最も短く、
半数以上が絶対下限の30秒すら割る**。p10 は10.9秒で、盤面を見る前に終わるスプリントが常態化している。

既存のペーシング回帰テスト（`tests/unit/sprintTempo.test.ts`）は、無介入経路
（`collectSprintTicks` はカードも介入も発動せず `step` だけで進める）で通常スプリントの
絶対下限・p50・p90 を既に検査している。検知できていないのは**難易度の範囲**で、対象は normal のみ。
ボスも上限（180秒）しか検査しておらず、下限（90秒）は未検査。

### RI-75 勝利種別が実質2種で、最も受動的なプレイが最上位勝利を取る（優先度: 高 / F-10）

840ラン中285勝の内訳:

| 勝利種別 | 件数 |
| --- | --- |
| `healthy`（健全勝利） | 153（54%） |
| `noDamage`（ノーダメージ勝利） | 131（46%） |
| `happiness`（現場幸福勝利） | 1 |
| `aiSuccess` / `management` / `chaos` / `normal` | **0** |

原因は `evaluateWinType`（`src/sim/outcome.ts:103-118`）が**固定の優先順ラダー**である点。
最初の判定が `!usedHeavyActions && totals.spread === 0`（残業・アンドン未使用かつ延焼ゼロ）のため、
**何もしないほど「やり込み枠」のノーダメージ勝利になる**。勝利種別は実質「重アクションを
使ったか」だけで分岐している。

組織診断も偏る（`seniorSacrifice` 450 / `reviewHell` 342 / `documentationKingdom` 21 /
`healthyAcceleration` 16 / `aiOverproduction` 11 / `reworkSpiral` 0）。

### RI-76 AI 導入が既定 ON で、既定のまま進むのが有利（優先度: 高 / F-1・F-2・F-10）

初期ロスターは `aiAssigned: arch.preferred === 'coding'`（`src/sim/member/roster.ts:135`）で、
**コーダー志向のメンバーには最初から AI が配られている**。既定プレイの AI 利用率は 82.5%、
最終 AI 依存度は 100。

| 指標（easy、スプリント平均） | 既定（`skilled`） | `noAi` |
| --- | --- | --- |
| 出荷 | 333.8 | **166.4** |
| AI 利用率 | 82.5% | 0% |
| 最終 AI 依存度 | 100 | **41.4** |
| 勝率（40ラン合計） | 12 | **8** |

**選択の入口は存在する**。新規ランは第1スプリント前に必ず `SetupScreen`（見出し「編成 —
スプリント開始前に配置とAIを決める」）を通り、各コーダーの「🤖 AI配布中」ボタン
（`FormationScreen.tsx:172`）から `setMemberAi` で解除できる。課題は次の3点である。

1. **既定が ON で、既定のまま進むのが有利**。`aiFullBet`（全員に配布）は既定とほぼ同じ結果で、
   選び直す動機が無い。
2. **解除側の代償が一方的に大きい**。`noAi` は全難易度合計 8/40 で最下位から2番目、出荷は半減する。
3. **段階が無い**。導入は編成画面の on/off だけで、「どこまで広げるか」を四半期を通じて
   問い直す機会が無い。

因果（AI を入れると速いがレビューが詰まりシニアが削れる）自体は正しく効いている。
開始時 AI を全面撤廃するのではなく、既定値の誘導・解除側の代償・導入の段階化を対象にすべきである。

### RI-77 カードが集中力だけを消費し、勝率を動かさない（優先度: 高 / F-2・F-4）

**「介入すべき状況が盤面に現れない」は誤りだった**（第1回の記述を撤回）。

カードだけを外す統制条件（`skilledNoHire` → `skilledNoCards`。採用条件は両方 `skip` で揃えてある）:

| 条件 | 成立 p50 | ok | no-focus（集中力不足） | cooldown | focus 残 | 勝率 |
| --- | --- | --- | --- | --- | --- | --- |
| easy `skilledNoHire`（カードあり） | 3 | 213 | **258** | 1068 | 3.6/15.9 | 8/10 |
| easy `skilledNoCards` | 3 | **350** | **0** | 1711 | 9.4/16.1 | 9/10 |
| normal `skilledNoHire` | 3 | 269 | **718** | 1735 | 2.5/16.0 | 5/10 |
| normal `skilledNoCards` | 4 | **582** | **0** | 3563 | 8.5/16.5 | 5/10 |
| hard `skilledNoHire` | 3 | 446 | **2006** | 3725 | 1.9/16.5 | 3/10 |
| hard `skilledNoCards` | 5 | **476** | **0** | 3456 | 7.6/16.2 | 2/10 |

カードを止めると集中力不足による介入失敗が**完全に消え**、介入成立数が 1.1〜2.2 倍、
残集中力が 1.9〜9.4 へ増える。にもかかわらず**勝率は変わらない**。
つまりカードは、**集中力という共通資源を大量に消費しながら、結果に寄与していない**。

**対象不足はアクションによって大きく異なる**（単一介入方針の実測。順序バイアスなし）:

| アクション | easy | normal | hard | nightmare |
| --- | --- | --- | --- | --- |
| `firefight`（緊急対応） | 90.7% | 87.3% | 81.7% | 50.5% |
| `assignTask`（タスク差配） | 81.1% | 89.8% | **95.2%** | **98.2%** |
| `interruptReview`（割り込みレビュー） | 59.6% | 58.7% | 64.9% | 41.7% |
| `splitPr`（PR分割） | 58.4% | 58.8% | 53.4% | 45.8% |
| `overtime` / `andon` / `pairReview` / `aiThrottle` | 0% | 0% | 0% | 0% |

（数値は試行のうち `no-target` で失敗した割合）

対象を要する4種のうち、**`assignTask` は難易度が上がるほど対象が見つからなくなり、hard/nightmare では
ほぼ空振りする**。`firefight` も easy で9割が空振りで、炎上の発生頻度に対して撃ちたい回数が多い。
一方 `overtime` / `andon` / `pairReview` / `aiThrottle` は対象を要さず、
クールダウンと集中力だけが制約になる。

参考として、全8種を 1 tick ごとに撃ち続ける `probe` の成立数は easy p50=7 / normal p50=8 / hard p50=8 で、
第3.1 の「1スプリントあたり 3〜8回」の帯に入る。ただしこれは順序バイアスを含むため、
「上限」としては読まないこと。

### RI-78 予算枯渇・信頼枯渇が予兆なく終わり、敗因ラベルが3種類の原因を1つに潰す（優先度: 中 / F-6・F-8）

敗因ごとの進行と、直前スプリントの盤面状態（840ラン・555敗）:

| 敗因 | n | 敗北までのスプリント数 p50 | 1つ前 シニアHP | 1つ前 士気 | 1つ前 負債 | 1つ前 予算 |
| --- | --- | --- | --- | --- | --- | --- |
| `aiDependency` | 165 | **1** | — | — | — | — |
| `seniorBurnout` | 167 | 2 | 29.1 | 84.8 | 8.5 | 39.2 |
| `moraleCollapse` | 16 | 2 | 32.1 | **64.3** | 8.5 | 32.1 |
| `techDebt` | 15 | 3 | 19.8 | 70.8 | **46.3** | 34.3 |
| `reviewFreeze` | 132 | 7 | 38.1 | 88.9 | 9.9 | 40.4 |
| `budgetExhausted` | 18 | 11 | **59.4** | **98.8** | 14.7 | 20.8 |
| `reorgRequired` | 14 | 18 | 54.0 | **100.0** | 12.4 | 29.3 |
| `trustExhausted` | 28 | 30 | **63.4** | **99.8** | 13.8 | 20.5 |

**進行速度は敗因ごとに明確に違う**（p50 が 1〜30 スプリント）。これは F-9 として良い性質である。
一方、`budgetExhausted` / `reorgRequired` / `trustExhausted` は
**シニアHP 54〜63・士気 98〜100 のまま終わる**。盤面上は健全に見えるのに終了するため、
F-6（次の一手が分かる）と F-8（詰みの確定を作らない）を満たさない。

加えて **`trustExhausted` ラベルは3種類の原因を1つに潰している**。
`loseReasonForOutcome`（`src/sim/run/quarterReview.ts:464`）は `missed_crisis` と `shutdown` の
**両方**を `trustExhausted` に変換する。発火条件を分解すると:

| outcome | 発火条件 | 件数 |
| --- | --- | --- |
| `missed_crisis` | `trust<=15` のみ | 18 |
| `missed_crisis` | `budget<=5` のみ | **7** |
| `missed_crisis` | `trust<=15` かつ `budget<=5` | 1 |
| `missed_crisis` | `missedCount>=4` のみ | 0 |
| `shutdown` | `seniorHp<=5` かつ `missedCount>=2` | **2** |

信頼由来は19件で、残り9件は**予算切れ（7件）とシニア枯渇（2件）**である。
予算やシニアが原因のランに信頼回復を促しても解決しない。敗因の分解と表示の一致が必要である。

### RI-79 スプリント評価が S に偏る（優先度: 中 / F-6）

840ラン・6,310スプリントの評価分布は
**S 3837（60.8%）/ A 1327（21.0%）/ B 500（7.9%）/ D 388（6.1%）/ C 258（4.1%）**。
S と A で 82% を占める。無介入の `passive` だけを見ても S 152 / A 84 / B 39 / C 25 / D 15 で、
**何もしなくても S が約半数（48%）**。評価がプレイの巧拙を反映していない。

### RI-80 ドラフトのマリガンが無い（優先度: 中 / F-12）

`DraftScreen` の選択肢は「3枚から1枚選ぶ」か「スキップ」のみで、コストを払って引き直す手段が無い
（`src/ui/DraftScreen.tsx`、`src/sim/run/engine.ts` の `drawDraft`）。
カードプールは9枚（`src/data/cards.ts`）で、うち4枚はメタ解放が必要なため初見の初期プールは5枚。
F-12 が求める「事故で負けた」→「コストを払ってでも引き直すか」への変換が成立していない。

RI-77 のとおりカードは現状ほぼ結果に寄与していないため、マリガン導入は
カード自体の効果量見直し（RI-77）とセットで扱う必要がある。

### RI-81 敗北画面に「次の一手」が無い（優先度: 中 / F-6）

`RunResultScreen` が敗北時に示すのは、敗因ラベル＋一文の説明（`LOSE_LABEL`、`src/ui/RunResultScreen.tsx:26-52`）、
組織タイプ診断、メタ進行ポイント。**次のランで何を変えるかは提示されない**。

教訓文（`FAILURE_LESSONS`、`src/sim/diagnosis.ts:64-81`）は存在し、**初回敗北でも表示される**。
ラン終了を検知した `recordIfFinished`（`src/game.ts:401`）が `applyRunReward` 経由で
今回の失敗診断を `collectedDiagnoses` に加えてから画面を更新するため、
`failureInCollection`（`src/ui/RunResultScreen.tsx:81`）は初回敗北時点で true になる。

残る問題は対応範囲と粒度である。

- 教訓は敗因（`LoseReason` 10種）ではなく**組織タイプ診断に紐づいており**、
  かつ `FAILURE_LESSONS` が対応するのは失敗診断4種だけである。実測では診断が
  `healthyAcceleration`（16件）や `documentationKingdom`（21件）のまま終わるランがあり、
  その場合は教訓が一切出ない。
- 敗因と教訓が独立しているため、`aiDependency` / `trustExhausted` / `budgetExhausted` /
  `reorgRequired`（実測合計225件）で負けても、表示されるのは診断由来の一般論であり、
  **その敗因に対する次の一手にはならない**。とくに RI-73 のリテラシー経路
  （ペアレビューで AI リテラシーを上げれば AI 依存敗北を回避できる）は、どこにも提示されない。

### RI-82 目標修正の選択が結果を変えない（優先度: 中 / F-2 第4層）

目標修正だけを固定する統制方針（各40ラン、うち実際に該当の修正を選べたのは各15ラン）:

| 選択 | 勝率 | 到達四半期 平均 | 総スプリント 平均 |
| --- | --- | --- | --- |
| `cut_scope`（スコープ削減） | 40% | 1.6 | 7.2 |
| `extend_deadline`（期限延長） | 40% | 1.7 | 7.6 |
| `quality_pivot`（品質ピボット） | 45% | 1.5 | 6.5 |

**どれを選んでも結果がほとんど変わらない**。目標修正フェーズ自体は840ラン中310ラン（36.9%）で
発生しており、選択機会は十分にあるが、選択が後続を分岐させていない。
SPEC 第19.1 F-2 が求める「四半期レビューの目標修正でジレンマが成立する」を満たさない。

（第1回の集計では `quality_pivot` の勝率を 83.3% としていたが、これは統制していない方針の
ランが提示順の先頭である `cut_scope` 群へ大量に混入した結果のアーティファクトだった。
Codex レビューの指摘を受けて、`adj*` 方針だけの比較へ訂正した。）

### RI-83 介入が結果の分散を下げない（優先度: 中 / F-5）

SPEC 第19.1 F-5 は「無介入は損ではなく不安定。介入の価値は期待値の底上げよりも**結果の偏差を抑えること**」と
定義している。ラン長の差が混入しない指標で測ると、この関係が成立していない。

easy の出荷の変動係数（CV）:

| 方針 | 1スプリントあたり出荷 CV | 第1スプリント出荷 CV |
| --- | --- | --- |
| `idle`（完全放置） | 26.6% | 10.6% |
| `passive`（無介入） | 23.4% | 10.6% |
| `naive`（初見相当） | 20.6% | 11.3% |
| `skilled`（上級者相当） | 23.7% | **12.7%** |
| `onlyFirefight` | 20.4% | 9.7% |

全ランが到達する第1スプリントで見ると、**介入する方針ほど分散がわずかに大きい**（`skilled` 12.7% が最大）。
1スプリントあたり出荷でも 26.6% → 20.6〜23.7% と、差はごく小さい。
normal / hard でも同様で、介入による明確な分散抑制は観測できない。

一方で勝率は改善している（RI-72 の表）。つまり現状の介入は**期待値を押し上げる手段**であって、
F-5 が想定する**不確実性を抑える手段**にはなっていない。

なお第1回の集計では累計出荷の CV が 57.9% → 38.6% と下がることを根拠に F-5 成立としていたが、
これは早期敗北したランほど累計出荷が機械的に小さくなるためで、
組織出力の安定性ではなくラン長の差を見ていた（Codex レビューの指摘により訂正）。

対応の方向は2つあり、どちらを取るかは設計判断である。

1. F-5 の定義を実装に合わせる（介入は期待値を上げる手段と位置づけ直す）。
2. 実装を F-5 に合わせる（介入に下振れを潰す性質を持たせ、無介入の分散を大きくする）。

受入条件:

- F-5 の定義と実装のどちらを動かすかを決め、SPEC 第19.1 に反映する。
- 実装側を動かす場合、無介入と介入で 1スプリントあたり出荷・第1スプリント出荷の
  変動係数に有意な差が出る。
- 判定を `npm run playtest` の CV 出力で検証し、回帰を検知する。

## 良かった点（回帰させないため記録）

- **介入は勝率を改善し、撃ちすぎは損になる**。全40ランでの勝利数は 完全放置 7 → 無介入 9 →
  初見相当 12 → 上級者相当（採用なし）16、全介入を撃ち続ける `probe` は 10。
  介入の量と質の関係は健全に効いている（ただし F-5 の分散抑制は別問題。RI-83）。
- **敗因ごとの進行速度が明確に違う**（F-9 の中心要件）。敗北までのスプリント数 p50 は
  1〜30 スプリントの範囲に分かれており、`seniorBurnout` の急死と `trustExhausted` の
  じり貧はプレイ感として別物になっている。
- **AI 依存敗北には、依存度を下げる経路とリテラシーを上げる経路の2種類がある**（RI-73 の表）。
  とくにペアレビューでリテラシーを上げる経路は、SPEC の主題に合致した良い設計。
  問題は発見性であって設計ではない。
- **AI の因果は正しく実装されている**。AI 配布を切ると出荷が半減し（333.8→166.4）、
  最終 AI 依存度が 100→41.4 になる。
- seed 固定の決定論が完全に効いており、`npm run playtest` は同じ入力に対して常に同じ結果を返す。

## 計測方法の訂正（第1回集計からの差分）

第1回の集計には次の誤りがあり、本節の数値はいずれも修正後のものである。
いずれも Codex レビューの指摘を受けて発見・修正した。

1. **解放コンテンツが実プレイと違った**。`RunEngine` を `allowedCards` / `allowedRelics` なしで
   生成していたため、メタ未解放のカード・レリックまでドラフト候補に入っていた。
   `game.startRun` と同じ `setUnlockedContent` を適用し、初見相当（既定解放のみ）へ揃えた。
2. **進化ポイントを使い切っていなかった**。ハーネスが進化フェーズで解放を1ノードで打ち切っていた。
   これにより「介入・カード・採用は投入するほど勝率が下がる」という誤った結論が出ていた。
3. **介入の「発動できなかった理由」を測っていなかった**。方針の閾値による見送りと、
   盤面に対象が無いことを区別できていなかったため、「介入すべき状況が盤面に現れない」と誤って結論していた。
4. **目標修正の比較に非統制ランが混入していた**。提示順の先頭である `cut_scope` 群に
   他方針のランが大量に入り、`quality_pivot` が突出して見えていた。
5. **分散の比較にラン長の差が混入していた**。累計出荷は早期敗北ランほど機械的に小さくなるため、
   1スプリントあたり出荷と第1スプリント出荷で見るよう変更した。
