# モックアップ乖離・SPEC未充足の課題一覧

このファイルは、`mockups/` のデザイン基準（第22.2）や `SPEC.md` の企画に対して、現状の
実装が**まだ届いていない／暫定のままの項目**、および**遊んでみて見えてきた設計再検討（SPEC からの
変更提案）**を課題として書き出したもの。各領域の実装直後の取りこぼしは
[follow-ups.md](./follow-ups.md) に、ここでは横断的に残る「やること」を集約する。

> 経緯: スプリント盤面（メイン画面）は本ファイル §1-A のとおり mockup 準拠の俯瞰オフィスへ
> 刷新済み（PR #38）。同じ手法で他の画面・残務へ広げるのが次の主題。

優先度の目安: **優先度:高=体験の核に直結 / 優先度:中=見栄え・整合 / 優先度:低=将来拡張**。

---

## 1. ゲーム画面のモックアップ乖離（最優先テーマ）

「数字は動くがモックアップのゲーム画面から離れている」問題。メイン画面は解消済み。残るのは
ズーム階層（第4.7〜4.11）の3画面と、メイン画面の演出残務。

### 1-A. スプリント盤面の演出残務（実装済みの上積み）

メイン画面はアイソメ俯瞰オフィスへ刷新済み。以下は「より mockup / 第18章に近づける」上積み。

- **優先度:中 タスク粒の工程間移動アニメ**: 現状は各工程に「山」で静的配置。mockup の「ベルトコンベアで
  流れる粒」「AI暴走時に高速で Review に突っ込む」までは未実装。粒の流れ（補間移動）を入れる。
- **優先度:中 延焼の連鎖演出**: 炎上タスクが隣へ燃え広がる／緊急対応成功で爽快に消火、の連鎖アニメ（第18.2）。
  現状は炎上ドットの flame 表示まで。
- **優先度:低 キャラ/粒のスプライト化**: 現状 SVG/DOM。PixiJS 移植時にスプライト＋プール（`render/iso.ts`）へ
  寄せ、疲れ顔/ガッツポーズ等の表情スプライト（follow-ups「キャラクター育成」）と統合。
- **優先度:低 アクションバーのマネージャー像**: mockup footer のマネージャーマスコットは未移植。`ActionBar` へ追加。
- **優先度:低 スイープ/スローモーション**: 割り込みレビューの一括処理スイープ、ボス最後の1件のスローモー（第18.2）。

### 1-B. 全社マップ（org-screen）の等角化 — 優先度:高

- **現状**: 島の配置は `render/iso.ts` のアイソメ投影で決まるが、チームは**フラットな矩形カード**。
  `?renderer=pixi` で全社マップのみ Pixi 切替可だが、既定 DOM も Pixi も mockup の見た目には未到達。
- **目標（`mockups/org-screen.png` / 第4.8）**: 浮遊する**等角プレート**＋部門ゾーン（縦ストライプの色分け）
  ＋**チーム島**（ミニ机＋アバター＋AIボット）＋中央の**共通基盤ハブ**（サーバーラック＋ボット）＋
  **チーム間フローの等角レーン**（詰まると赤熱）＋規模効果バッジ。健全/渋滞/Review Hell の状態色。
- **規模感**: 中〜大。スプリント盤面と同じ手法（純シーン計画＋SVG/Pixi 描画）で島・ハブ・レーンを等角化。
  `OfficeRoom`/`OfficeActors` 相当の「島アクター」を作る。

### 1-C. 部署ビュー（dept-screen）の等角化 — 優先度:高

- **現状**: アイソメ未使用のフラット表現。
- **目標（`mockups/dept-screen.png` / 第4.9）**: 各チームを **Coding▸Review▸Done の小パイプライン**として
  中解像度の等角で表示。**チーム間依存の矢印**と、上流の巨大PRが下流へ押し寄せる**連鎖炎上**の可視化。
  部門HUD・部門レバー。
- **規模感**: 中。スプリント盤面の「ミニ版パイプライン」をチーム数ぶん並べる構造。

### 1-D. 業界ランキング（industry-screen）の等角化 — 優先度:中

- **現状**: ランキング表＋スカイライン（高さ=スコア）はあるが、ビルは等角ではない。
- **目標（`mockups/industry-screen.png` / 第4.10）**: 各社本社ビルを**等角**で並べた HQスカイライン
  （1位に王冠・自社は発光）。ランキング種別タブ・リーグ/シーズン表示。
- **規模感**: 小〜中。スカイラインを等角ビルに置換＋強調演出。

### 1-E. ドリルダウン/ズームアウトのカメラ遷移演出 — 優先度:中

- **現状**: 階層移動はオーバーレイ＋クロスフェード（実装済み）。
- **目標（`mockups/drilldown.html` / 第4.11）**: 島タップ→フォーカスリング→**カメラが島へ寄る**→
  クロスフェードで現場へ着地、の「カメラズーム」演出。遷移先の炎上/渋滞が遷移演出にも反映。
- **規模感**: 中。Pixi 移植（pixi-viewport）と合わせると素直。

---

## 2. SPEC.md 未充足（機能）

ゲームループ・章構成は概ねカバー済み。残るのは以下。

- **優先度:中 称号（WinType）の永続コレクション化**（第4.6 / 周回・育成・診断・メタ進行）: 現状は実績ID集合まで。勝利種別ごとの
  達成記録・一覧化は未実装。
- **優先度:中 診断別の画面演出（6組織タイプ）**（第13・18.3 / 周回・育成・診断）: 現状は昼/曇り/地獄の3トーン。
  Healthy Acceleration / Review Hell / AI Overproduction / Rework Spiral / Senior Sacrifice /
  Documentation Kingdom それぞれの色・背景・警告文へ拡張。
- **優先度:中 業界ランキングへのデイリー記録接続**（第4.10・17 / 組織スケール・メタ進行）: `meta.dailyRuns` の自己ベストは
  Title/RunResult には出るが、`IndustryScreen` の擬似リーダーボードへの差し込みは未着手。
- **優先度:低 開始時の組織プリセット**（第17）: `UnlockKind` に preset の足場はあるが到達不能。正式実装するか
  足場を削除するか未決定（メタ進行の繰り越し）。
- **優先度:低 メンバー/トレイトのメタ解放**（第4・17 / 周回・育成・診断・キャラクター育成・メタ進行）: 現状の永続解放はカード/レリックのみ。
- **優先度:低 23章「拡張案」全般**: GitHub API 実データモード、チーム対抗ランキング、社内LT/経営プレゼンモード、
  ツール別シナリオ、「AI導入失敗図鑑」「レビュー地獄リプレイ」「なぜ燃えたか解説ログ」等（将来拡張）。

---

## 3. バランス調整（暫定値のモンテカルロ化）

`follow-ups.md` の各領域で「暫定値」と明記した係数群を、代表 seed のモンテカルロで許容レンジ化する
統計テスト基盤に統一する（スプリント/キャラクター育成/組織スケール/メタ進行/四半期レビューで共通の繰り越し）。

- **優先度:中** 全社/部門レバー係数（`src/data/levers.ts`）
- **優先度:中** 四半期レビューの代償・outcome 閾値（`src/sim/run/quarterReview.ts` / `src/data/goalAdjustments.ts`）、
  目標生成（`buildQuarterGoal`）
- **優先度:中** メタ解放コスト・points 配分（`src/data/unlocks.ts` / `applyRunReward`）
- **優先度:低** 編成差のスプリント結果への影響レンジ（キャラクター育成）

---

## 3.5 ゲームデザインの再検討（SPEC からの変更提案）

遊んでみて見えてきた、ループ設計そのものの課題。SPEC の記述を変える提案を含むため、実装前に
方針合意が要る。

### 3.5-A ノード選択（ランマップ）の廃止とイベント判定化 — 優先度:高

> **設計詰め済み**: 具体仕様は [run-loop-redesign.md](./run-loop-redesign.md)（四半期トラック＋
> 混合ビート＋組織状態重み付け）。合意済みの方針: 判定＋選択の混合 / 組織状態で重み付け /
> 高負荷・ショップ・休息は選択イベントに統合。次は同ドキュメント §7 の段階移行で実装する。

- **課題**: メイン画面（ラン中）はノード選択（ランマップ）が大半を占める。戦略的要素はあるが、
  **状況をコントロールできすぎている**。Slay the Spire は「リターンを得るためにリスクを取る」
  トレードオフ設計だが、現状はノード選択に**明確なリターンが無い**ため、結局**リスクの無い選択肢を
  取るだけ**になり、意思決定が形骸化している。
- **提案（主案）**: **ノード選択を廃止**し、従来ノードを選んでいたタイミングで**何らかのイベント判定**
  （確率/状況依存のイベント）が起きるようにする。プレイヤーが盤面を完全制御するのではなく、
  「次に何が来るか」に対処する緊張感を戻す。
- **影響範囲（SPEC）**: 第3章 基本ループ図、第4.4 ランマップ画面（分岐ルート）、第9.4 分岐選択イベント、
  第10章 ボススプリントへの到達構造。`src/sim/run/map.ts`・`RunMapScreen`・`runMachine` の `map`/`enterNode`、
  `window.game.enterNode` の公開契約に波及する大きめの変更。
- **論点（実装前に決める）**:
  - イベント判定の中身: 完全ランダムか、組織状態（渋滞/負債/士気/AI依存度）依存の重み付きか。世界観の
    制約（第2.1）に収める。
  - リスク↔リターンの担保: もしノード選択を一部残すなら、各選択に**明確なリターンとそれに見合うリスク**を
    付ける（代替案B）。主案（完全廃止＋イベント判定）と代替案B（ノードにリスク/リターンを持たせる）の
    どちらを採るか。
  - ボス/四半期末への到達: マップ廃止後、ボススプリント（第10）や四半期レビュー（第8）へどう接続するか
    （固定スプリント数＋途中イベント、等）。
  - 決定論: イベント判定は seed 付き PRNG に載せ、デイリーラン/リプレイ/テストを保てるようにする（第22.3）。

---

## 4. 技術構成（第22章）

- **優先度:中 Pixi 適用範囲の拡張**: 現状は全社マップのみ opt-in。§1 の等角化を進める際、部署ビュー/現場盤面へ
  PixiJS を広げる（粒数・ズーム階層が DOM/SVG の限界に近づいたら）。スプライト＋プールは `render/iso.ts` を供給先に。
- **優先度:低 バンドル分割**: `npm run build` の index チャンクが 500kB 超の警告（Pixi/WebGL 同梱）。動的 import 等で
  コード分割するか（機能要件ではないが計測値として残す）。

---

## 進め方の指針

§1（画面のモックアップ乖離）が「数字は動くがゲーム画面に見えない」という当初課題の本丸。スプリント盤面で
確立した手法 ——「純シーン計画（`*Scene.ts`、Vitest 検証）＋ SVG/Pixi 描画アダプタ＋設計座標空間の % 配置」——
を全社マップ→部署ビュー→業界ランキングへ横展開するのが最短。Pixi 移植（§4）はこの等角化と歩調を合わせる。
