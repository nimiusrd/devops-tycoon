# 企画書

## DevOps Tycoon: AI時代の開発組織シミュレーター

### AIを導入して開発組織をどこまで成長させられるかを遊びながら理解するWebゲーム

## 1. 企画概要

本企画では、AI導入が開発組織の生産性に与える影響を、確率モデルをベースにしたWebゲームとして表現する。

ユーザーは開発組織のマネージャーとなり、開発者、シニアエンジニア、AIツール、自動テスト、レビュー体制、ドキュメント整備などに投資しながら、スプリントを回してプロダクトを成長させる。

AIを導入すれば実装速度は上がるが、レビュー詰まり、手戻り、品質低下、シニア過労、PR爆発、仕様不明バグなども発生する。
ユーザーはそれらを観察しながら、どの施策に投資すれば組織全体の出荷量が最大化されるかを試行錯誤する。

本アプリケーションは、単なる分析ダッシュボードではなく、
**「開発組織を育てるゲーム」**
として設計する。

ゲームとしての面白さは、次の3つの体験で担保する。

* **能動操作**: スプリント中、流れてくるタスクとレビュー渋滞をリアルタイムに捌く（タワーディフェンス的介入）
* **周回構成**: 1ラン＝1四半期。固定トラックを進み、合間のイベント判定に対処しながらカードを集め、組織文化レリックを拾い、四半期末のボスに挑むローグライク
* **組織育成**: 出荷で得たポイントで組織進化ツリーを伸ばし、メンバーを育て、自分だけの「強い会社」を作る

## 2. コンセプト

コンセプトは以下である。

**AIを入れたら最強になると思ったら、レビューが燃えた。**

ユーザーは開発組織を運営する。
毎スプリント、タスクが流れてくる。
開発者はCodingする。
AIは実装を加速する。
しかしPRが大量発生し、シニアがレビューで詰まる。
雑にAIを使うとReworkが増える。
テストが弱いと障害が発生する。
ドキュメントがないとAIの成功率が下がる。
レビューが遅れると開発者の士気が下がる。

つまり、これはAI導入ゲームであると同時に、
**開発組織の制約管理ゲーム**
である。

そして、その制約は「眺めて受け入れるもの」ではなく、
**プレイヤーがリアルタイムに手を入れて捌くもの**
として表現する。

### 2.1 世界観の制約（現実の開発組織から大きく逸脱しない）

本ゲームのすべての要素（イベント、ボス、敗北条件、演出、称号）は、
**現実の開発組織で実際に起こりうる範囲**に留める。
誇張は「あるある」の範囲までとし、SF的・終末的な飛躍は入れない。

この制約を、設計・実装・拡張すべての判断基準とする。

#### 扱う（OKな範囲）

* レビュー渋滞、PR爆発、手戻り、本番障害、技術的負債の蓄積
* シニアの過労・離脱、士気低下、属人化、オンボーディング遅延
* 予算超過、ツール解約、リリース遅延、プロジェクト頓挫
* AIの誤生成（存在しないAPI、もっともらしい嘘、巨大PR）
* 「見かけの生産性」と実態の乖離、KPIの歪み

これらは、現実のチームが日常的に直面する事象であり、
プレイヤーが「自分の現場でも起きる」と感じられることを重視する。

#### 扱わない（NGな範囲）

* 大災害・戦争・天変地異で会社や世界が消滅する
* AIが意思を持って人間を駆逐する、暴走して支配するなどのSF的展開
* 現実離れした超技術や、魔法・超常現象
* 特定の実在企業・個人を貶める表現

#### 演出上の比喩について

「炎上」「Review Hell」「ヘドロが溜まる」などの表現は、
あくまで**組織状態を直感的に伝えるための比喩・雰囲気演出**であり、
物理的な破壊や破滅を意味しない。
比喩はわかりやすさのために使うが、世界観の前提（現実の開発組織）は壊さない。

なお、敗北は「会社の消滅」ではなく
**「その組織運営が立ち行かなくなった（プロジェクト失敗・チーム崩壊・リリース停止）」**
という、現実的な行き詰まりとして表現する。

## 3. ゲームの基本ループ

> **実装状況**: 第3・4.4・9・10 章のループ記述は**実装済み**。固定トラック（`SPRINTS_PER_QUARTER`、
> 最終がボス）＋スプリント間ビート（判定/選択の混合・組織状態で重み付け）で動作する
> （`phase='setup'`→`beginSetupSprint()` で第1スプリント開始、`phase='beat'`→`resolveBeat()` で
> イベント解決）。分岐マップ（`enterNode`/`RunState.map`）は撤去済み。実装は
> `src/sim/run/engine.ts`・`events.ts`、公開契約は `src/game.ts` を正とする。

ゲームは **ラン（1四半期）→ スプリント → ドラフト → 進化 → スプリント間イベント → …（四半期末）→ 四半期レビュー** の入れ子構造で進行する。
従来のローグライト的な「目標未達 = 即ゲームオーバー」ではなく、実際の開発組織に近い **目標修正・信頼調整・次四半期への継続判断** を中核に置く。

四半期は**固定トラック**（スプリント列。最終がボス）として進む。プレイヤーは「どのルートを通るか」を選ばない。
そのかわり、スプリントの合間に**イベント判定**が起き、組織の状況に対処させる（詳細は第9章）。

```text
[ラン開始] 組織と難易度を選ぶ
   │
   ├─ セットアップ（第1スプリント前の編成: メンバー配置・AI配布）
   │     ↓
   │  ┌─ スプリント実行（能動操作フェーズ）─────────┐
   │  │ 1. バックログからタスクが流入            │
   │  │ 2. 開発者が実装（AIで加速）             │
   │  │ 3. PRがReviewに流れ、渋滞が発生          │
   │  │ 4. プレイヤーが介入アクションで捌く        │
   │  │ 5. Incidentは炎上タイマー内に鎮火する      │
   │  │ 6. Doneが出荷され、コンボとポイントが出る    │
   │  └──────────────────────────────────────────┘
   │     ↓
   │  スプリントリザルト（評価・称号・診断）
   │     ↓
   │  カードドラフト（3枚から1枚をデッキに加える）
   │     ↓
   │  組織進化ツリーにポイントを割り振る
   │     ↓
   ├─ スプリント間イベント判定（第9章）
   │     ・判定イベント: 組織状態に応じた事象が自動で起きる（制御できない緊張感）
   │     ・選択イベント: リスク/リターンの2〜3択を迫る（断る選択にも代償）
   │     ・高負荷案件 / 予算で補強 / 一息つく も、この選択イベントとして提示
   │     ↓
   └─ （次のスプリントへ）……四半期末の最終スプリントで
[ボススプリント] 大型リリース / 大規模障害 / 監査 / 経営レビュー
   ↓
[四半期レビュー] 目標達成度・信頼・負債・士気を評価
   ├─ 達成 / 超過達成: 評価とメタ進行を獲得
   ├─ 未達だが継続可能: 目標を修正し、代償を払って次四半期へ
   └─ 信頼・予算・士気が枯渇: プロジェクト終了 / 組織再編
   ↓
[次四半期] 修正後の目標と前期の傷跡を引き継いで再挑戦
```

従来の「スプリント後にカードを選ぶ」だけのループに対し、本企画では
**スプリント中の能動操作**と**スプリント間のイベント判定**を加え、毎回違う展開で遊べるようにする。
ルートを選んで安全策を取れてしまう分岐マップは廃し、かわりに「四半期目標への進捗＝リターン」と
「渋滞・炎上・信頼・負債＝リスク」を天秤にかけ続けさせる。安全側の選択にも必ず代償（目標から遅れる等）を
持たせることで、リスクを取らない最適解を作らない。
さらに、未達時にも「何を諦め、誰の信頼を使い、何を次期へ先送りするか」を選ばせることで、単なる勝敗ではなく組織運営の継続的な意思決定を遊びにする。

## 4. ゲーム画面

### 4.1 メイン画面: 開発ライン（能動操作フェーズ）

> モックアップ: `mockups/main-screen.html` / `main-screen.png`（俯瞰オフィスのアイソメトリック表現。机ごとに担当が働き、タスクが机から机へ流れる）

画面中央には、開発タスクが粒として流れるラインを表示する。

```text
Backlog → Coding → Review → Rework → Done
```

タスクは種類ごとに見た目を変える。

| 表現      | 意味       |
| ------- | -------- |
| 小さい丸    | 定型タスク    |
| 中くらいの丸  | 通常タスク    |
| 大きい丸    | 複雑タスク    |
| 光る丸     | AI利用タスク  |
| 赤い丸     | 手戻りタスク   |
| 炎上エフェクト | 障害化したタスク |
| 金色の丸    | 高価値タスク   |
| 黒い丸     | 技術的負債タスク |

AI導入後は、Codingを高速で抜けるタスクが増える。
しかしReviewに大量の粒が溜まり、画面上で「渋滞」が起きる。

この渋滞そのものが、ゲームの見どころになる。

そして本企画では、渋滞は**眺めるだけでなく操作対象**になる。
プレイヤーは画面下部の**介入アクションバー**から、レビュー割り込み・PR分割・緊急対応などを発動し、
流れをリアルタイムに整える（詳細は第6章）。

### 4.2 ステータス表示

画面上部には、組織の状態をゲームのステータスとして表示する。

```text
出荷ポイント: 128
開発速度: S
レビュー耐性: C
品質: B
シニア体力: 34%
AI依存度: 72%
技術的負債: 41
士気: 66
炎上リスク: HIGH
```

AI導入によって開発速度がSになっても、レビュー耐性がCなら詰まる。
品質が落ちればReworkが増える。
シニア体力が0になると「レビュー停止」イベントが発生する。

ステータスは数値だけでなく、**画面全体の雰囲気**にも反映する。
健全なら明るい昼、Review Hellに近づくと画面が赤く曇り、夜になる（詳細は第18章）。

### 4.3 介入アクションバー

画面下部に、リアルタイムで発動できる介入アクションをカードUIで並べる。

```text
[割り込みレビュー] [PR分割] [緊急対応🔥] [AIスロットル] [残業号令] [アンドン⏸]
   ⚡3            ⚡2       ⚡1         ⚡2          ⚡4         ⚡5
   ▮▮▮▮▮▮▮▮       Ready    Ready      ▮▮▮□□        Ready       ▮□□□□
```

各アクションは**マネジメント集中力（⚡）**を消費し、クールダウン（CD）を持つ。
集中力はスプリントごとに回復する有限資源であり、「いつ・何に使うか」が判断の核になる。

### 4.4 スプリント間イベント画面（判定 / 選択）

> 実装済み（`BeatScreen` / 第1スプリント前は `SetupScreen`）。第3章冒頭の実装状況を参照。

四半期は**固定トラック**（スプリント列。最終がボス）として進む。ルートを選ぶマップは置かない。
スプリントとスプリントの間に、**イベント判定**を 1 回提示する（中身は第9章、組織状態で重み付け）。

```text
   Sprint i 完了 → Result → Draft → Evolution
        ↓
   ┌─ スプリント間イベント ───────────────────────┐
   │ 〔判定イベント〕状況に応じた事象が自動で起きる        │
   │   例: 技術的負債↑→本番障害 / シニアHP↓→レビュー停止 │
   │ 〔選択イベント〕リスク/リターンの2〜3択（断るにも代償） │
   │   例: 大型案件を受ける？ 予算で補強？ 一息つく？     │
   └──────────────────────────────────────────────┘
        ↓
   Sprint i+1（四半期末の最終スプリントがボス）
```

提示されるイベントの種類は以下。

| 種類          | 内容                                                         |
| ----------- | ---------------------------------------------------------- |
| 判定イベント      | 組織状態に応じた事象が自動で起きる（選択なし）。良い/悪い/ネタ。制御できない緊張感（第9.1〜9.3） |
| 選択イベント      | リスク/リターンの2〜3択を迫る分岐（第9.4）。**断る/安全側にも必ず代償**             |
| ─ 高負荷案件（選択） | 受ける＝次スプリントを高負荷化（大出荷／渋滞・炎上リスク）／ 見送る＝通常（出荷控えめ＝目標遅れ）  |
| ─ 予算で補強（選択） | 受ける＝ショップを開く（購入/強化・採用）／ 見送る＝予算温存                       |
| ─ 一息つく（選択）  | 受ける＝シニアHP回復 / カード強化 / Tech Debt返済 ／ 攻め続ける＝回復しない         |
| ボス          | 四半期末の最終スプリント（固定。第10章）                                |

旧来は「ルート選択そのものが読み合い」だったが、安全ルートを選ぶだけになりがちだった。本設計では
**各イベントの決断**（安全側にも代償）と、四半期目標への進捗（＝リターン）／渋滞・炎上・信頼・負債（＝リスク）の
天秤が読み合いになる。実装は `src/sim/run/engine.ts`・`events.ts`・`src/data/events.ts`。

### 4.5 組織進化ツリー画面

出荷で得た**進化ポイント**を割り振る、スキルツリー画面を用意する（詳細は第11章）。
ノードを解放すると恒久的に組織能力が伸び、「どの会社になるか」というビルドの個性が生まれる。

### 4.6 スプリントリザルト画面

スプリント終了時には、結果をゲーム風に表示する。

```text
SPRINT RESULT

Done: 24 tasks
Delivered: 82 pt
Max Combo: x6
AI Assisted: 71%
Review Queue Max: 18 PR
Rework: 7 tasks
Incidents: 1 (鎮火 1 / 延焼 0)
Senior HP: -12
介入: 割り込み×3 / 緊急対応×1

評価: B-

診断:
AIによって実装量は増えましたが、
レビュー工程が限界を超えています。

称号:
「PRを増やす者」
```

結果に応じて称号を付与する。

称号例:

* 健全な加速者
* PRを増やす者
* レビューを焼き尽くす者
* Rework職人
* シニア過労メーカー
* テスト救世主
* AI丸投げ将軍
* ドキュメントの民
* 爆速だが不安定
* 静かな崩壊
* 見かけ上の生産性王
* 火消しの達人（緊急対応を多用してクリア）
* コンボ職人（高コンボで出荷）
* ノー残業の勇者（残業号令ゼロでクリア）

### 4.6.1 四半期レビュー / 目標修正画面

四半期末、またはボススプリント未達時に表示するレビュー画面。
単なる勝敗画面ではなく、以下を一覧し、次期に何を持ち越すかを選ばせる。

* 目標達成度: Delivery / Quality / Tech Debt / Morale / Incident / AI Adoption など
* ステークホルダー信頼: 経営・顧客・チーム
* 未達理由: スコープ過多、レビュー詰まり、品質問題、AI過信、属人化など
* 選択肢: スコープ削減、期限延長、品質改善ピボット、追加予算申請、AI導入一時停止、組織再編
* 代償: 信頼低下、予算消費、士気低下、技術負債増加、次期目標の制約

未達は即ゲームオーバーではなく、組織が学習しながら次の四半期へ進む契機にする。
ただし、信頼や予算が尽きている状態で未達を重ねると、プロジェクト終了・大規模再編としてラン終了になる。

### 4.7 組織スケールとズーム階層（巨大組織対応）

本ゲームは1チームの現場から始まるが、組織が成長すると **複数チーム・複数部署を抱える巨大組織** へとスケールする。
そのため画面は、同じアイソメトリックの作法のまま **ズーム階層** として地続きに接続する。

```text
🌏 業界ランキング ── 🗺 全社マップ ── 🏢 部署ビュー ── 💻 チーム現場（能動操作）
     俯瞰・比較                          ── ズームイン →                手を動かす
```

| 階層        | 主に見えるもの            | 主に管理するもの          | 主なリソース/レバー    | モックアップ          |
| --------- | ------------------ | ----------------- | ------------- | --------------- |
| 💻 チーム現場  | タスク粒・レビュー渋滞・炎上     | スプリント中の即時介入       | マネジメント集中力 ⚡   | main-screen     |
| 🏢 部署ビュー  | 部門内チームの小パイプライン・依存 | チーム編成・部門施策        | 部門予算 💰       | dept-screen     |
| 🗺 全社マップ  | 全部門・全チーム・共通基盤      | 採用・投資・標準化・組織再編    | 四半期予算 💰      | org-screen      |
| 🌏 業界ランキング | 他組織とのスコア比較         | シーズン目標・ベンチマーク     | メタ進行ポイント      | industry-screen |

* 各画面の上部に **パンくず** を置き、階層を自由に行き来できる。
* 下位ほど「手を動かす（能動操作）」、上位ほど「俯瞰して采配する（戦略）」になる。
* 同じ等角表現・同じキャラ・同じ状態演出を保つことで、ズームしても世界が地続きに感じられる。

設計指針として、**下位の事象は上位に集約され、上位のレバーは下位の制約をまとめて緩める**。

```text
チーム現場の「レビュー渋滞」  → 部署ビューの「炎上チーム」     → 全社マップの「赤い部門」      → 業界ランキングのスコア低下
                              ↑ 採用・基盤投資・標準化・組織再編などの上位レバーで、下位の制約をまとめて改善できる
```

この入れ子は既存システムとも対応する。チーム施策は **カード/デッキ（第7章）**、部門・全社の恒久能力は **組織進化ツリー（第11章）** と **組織文化レリック（第8章）**、組織タイプの良し悪しは **組織タイプ診断（第13章）**、業界での積み上げは **メタ進行（第17章）** に接続する。

### 4.8 全社マップ（全社俯瞰モード）

> モックアップ: `mockups/org-screen.html` / `org-screen.png`

会社全体を **浮遊する等角プレート** に見立て、組織を一望する戦略画面。

* **部門ゾーン**: プレートを縦ストライプで色分けし、事業部/部門を区切る（例: プロダクト事業部 / 基盤・プラットフォーム部 / 新規事業部）。
* **チームの島**: 各チームを小さな等角の島（ミニ机＋アバター＋AIボット）として配置。島の上のバッジに出荷・AI依存度・状態を表示。
* **状態の色**: 健全（緑）／渋滞ぎみ（黄＋PRが少し溜まる）／Review Hell（赤・煙・炎上、AI生成PRが溢れる）を一目で判別できる。
* **共通基盤ハブ**: 中央に CI・Docs・AIガイドラインを全チームへ供給する基盤（サーバーラック＋ボット）。ここへの投資が全チームへ波及する＝規模効果。
* **チーム間フロー**: PR・依存・リリース連携を等角レーンで接続。詰まっているレーンは赤熱する。
* **全社HUD**: 全社出荷・部門/チーム数・エンジニア数・全社AI依存度・全社技術的負債・全社士気・炎上中チーム数・組織タイプ診断。
* **全社レバー**: 採用ドラフト / 全社AIガイドライン / 基盤投資 / 標準化(CI・Docs) / 火消し部隊派遣 / 組織再編 を四半期予算で発動する。

全社マップでは「どのチームを今すぐ救うか」「どこに採用・基盤投資を振るか」という、現場とは時間軸の異なる采配を行う。

### 4.9 部署ビュー

> モックアップ: `mockups/dept-screen.html` / `dept-screen.png`

全社マップで気になった部門にズームインした中間ビュー。部門内の各チームを **Coding ▸ Review ▸ Done の小パイプライン** として中解像度で表示する。

* チームごとに、開発（AIで加速）・レビュー（PRの山・過労シニア・炎上）・出荷の様子が見える。
* **チーム間依存** を矢印で表現し、上流チームの巨大PRが下流チームに押し寄せて **連鎖炎上** する様子を可視化する。
* **部門HUD**: 部門出荷・部門レビュー耐性・部門AI依存度・部門技術的負債・部門士気・炎上チーム数。
* **部門レバー**: レビュー応援を送る / PRサイズ制限の一括適用 / AIスロットル（部門）/ シニア採用集中 / 依存整理 / 部門フリーズ。

部署ビューは「現場の細かな捌き」と「全社の采配」の橋渡しで、ボトルネックがどのチーム・どの工程にあるかを診断する層になる。

### 4.10 業界ランキングビュー

> モックアップ: `mockups/industry-screen.html` / `industry-screen.png`

自組織を、他組織と比較する **シーズン制リーダーボード**。現実のベンチマーク（他社事例との比較）の比喩であり、世界観の前提（現実の開発組織）を壊さない。

* **ランキング表**: 順位・組織名・組織タイプ・出荷スコア・健全度ランク・趨勢（▲▼）。自組織をハイライト表示。
* **HQスカイライン**: 各社の本社ビルを等角で並べ、ビルの高さ＝スコアで表現（1位に王冠、自社は発光）。
* **ランキング種別**: 総合出荷 / 健全経営 / AI活用 / 急成長 などタブで切り替え。
* **シーズン/リーグ**: リーグ昇格目標、健全度Bで「健全経営」ボーナスなど、メタ進行（第17章）と接続。デイリーラン（同一シード）で他プレイヤーと競う。

「見かけの出荷量だけ高い組織」は総合では上位でも健全経営ランキングでは沈む、といった形で、第13章の組織タイプの良し悪しが順位に表れる。

### 4.11 画面遷移（ドリルダウン / ズームアウト）

> モックアップ: `mockups/drilldown.html` / `drilldown-filmstrip.png`

階層間の移動は、ぶつ切りの画面切替ではなく **カメラのズーム＋クロスフェード** で地続きに演出する。

```text
[全社マップ] → 該当チームの島をタップ（フォーカスリング）→ カメラが島へ寄る → クロスフェードで [チーム現場] に着地
[チーム現場] → ズームアウト → [部署ビュー] → さらにズームアウト → [全社マップ] → [業界ランキング]
```

* ドリルダウン（下る）で「俯瞰 → 具体」、ズームアウト（上る）で「具体 → 俯瞰」へ、注意の粒度を滑らかに切り替える。
* 移動先の状態（炎上・渋滞）が遷移演出にも反映され、「赤い部門に降りていく」体験そのものが情報になる。

## 5. プレイヤーが操作するリソース

ゲーム内には複数のリソースを用意する。

| リソース           | 説明                              |
| -------------- | ------------------------------- |
| Budget         | AIツール、採用、改善施策に使う予算              |
| Senior HP      | シニアのレビュー余力                      |
| Team Morale    | チームの士気                          |
| Quality        | 品質水準                            |
| Delivery Score | 出荷成果                            |
| Tech Debt      | 技術的負債                           |
| AI Literacy    | AIを適切に使う能力                      |
| Test Coverage  | 自動テストによる安全性                     |
| Documentation  | AIと人間が参照できる知識量                  |
| マネジメント集中力 ⚡    | スプリント中の介入アクションに使う有限資源（毎スプリント回復） |
| 進化ポイント         | 組織進化ツリーの解放に使う成長資源               |

これにより、単に「AI利用率を上げれば勝ち」ではなくなる。

AI利用率を上げると、Coding速度は上がる。
しかしAI Literacy、Test Coverage、Documentationが低いと、ReworkやIncidentが増える。

そして集中力という有限資源を加えることで、
**「渋滞も炎上も、全部には手が回らない。どこを捨て、どこを救うか」**
という即時の判断がゲームの緊張感を生む。

## 6. スプリント中の能動操作

本企画の最大の特徴は、スプリントが「自動で進む様子を眺める」だけでなく、
**プレイヤーがリアルタイムに介入して捌く**ことにある。
イメージは「工場管理ゲーム＋タワーディフェンス」である。

### 6.1 介入アクション

介入アクションは集中力（⚡）を消費し、クールダウンを持つ。

| アクション         | 効果                                       | コスト | 副作用・リスク                |
| ------------- | ---------------------------------------- | --- | ---------------------- |
| 割り込みレビュー      | Reviewキューから複数PRを即座に捌く                    | ⚡3  | シニアHPを少量消費             |
| PR分割          | 巨大PRをドラッグして小PRに分割。レビューしやすくなる             | ⚡2  | 処理に時間がかかる              |
| 緊急対応🔥        | 炎上タスクを延焼前に鎮火する。タイミングゲー                   | ⚡1  | 失敗すると延焼が広がる            |
| タスク差配         | タスクを特定レーン/担当へドラッグ。複雑はシニア、定型はAIへ          | ⚡1  | 偏らせると過労・士気低下           |
| AIスロットル       | AI出力レートを一時的に絞り、Review渋滞を抑える              | ⚡2  | 出荷速度が一時的に低下            |
| 残業号令          | 当スプリントのスループットを大幅ブースト                     | ⚡4  | Morale・シニアHPが減少        |
| アンドン⏸（ライン停止） | タスク流入を止め、溜まったキューを捌き切る                    | ⚡5  | その間の出荷機会を失う            |
| ペア/モブレビュー     | 詰まったPRをジュニア＋シニアで処理。AI Literacyが少しずつ上がる   | ⚡2  | 一時的に2人を拘束             |

### 6.2 アクション経済とコンボ

* 集中力はスプリント開始時に上限まで回復し、スプリント中は回復しない（進化ツリーで上限・回復を強化可能）
* 各アクションはクールダウンを持ち、連打できない
* タスクが連続でDoneになると**コンボ**が成立し、出荷ポイントに倍率がかかる
* コンボはReworkと、鎮火が間に合わなかった炎上（自動鎮火・延焼）で途切れる。
  **点火しただけでは途切れず、タイマー内に緊急対応で鎮火すればコンボは守られる**（コンボ救出）
  → 「攻めて出荷を伸ばす」と「守って崩壊を防ぐ」のせめぎ合い
* 介入を適切に決めると**連携ゲージ**が溜まり、一時的に集中力回復が早まる

### 6.3 リアルタイム演出メーター

* **渋滞メーター**: Reviewレーンの混雑度。上限を超えるとPR凍結（敗北条件）に近づく
* **炎上タイマー**: Incidentが発生すると点火し、時間内に緊急対応で鎮火しないと延焼して被害拡大
* これらが**手を動かす理由**になり、ステータスがただの数字でなく「いま捌くべき対象」になる

#### 炎上タイマーの詳細ルール（実装済み）

障害（Incident）は発生の瞬間に決着せず、**点火 → 猶予（タイマー）→ 解決** の流れを取る。
対応が早いほど安く済む「対応の質の勾配」が、リアルタイム介入の核になる。

| 解決経路 | 条件 | 代償 | コンボ |
| --- | --- | --- | --- |
| 緊急対応（能動） | タイマー内に発動（⚡1） | シニアHP小 | **守られる** |
| 自動鎮火（受動） | タイマー切れ時にシニアHPに余力あり | シニアHP大 | 途切れる |
| 延焼 | タイマー切れ時にシニアHPが枯渇 | 負債↑・士気↓・**隣のReview待ちPRへ連鎖着火** | 途切れる |

さらに火が燃えている間は、
**Reviewスループットが低下**（シニアが火事対応に気を取られる）し、
**シニアHPの自然回復も鈍る**。放置すれば渋滞と過労が同時に育つため、
「今すぐ消すか、渋滞を捌くか」という集中力の使いどころが常に問われる。

この能動操作により、同じデッキ・同じ組織でも、プレイヤーの捌き方次第で結果が変わる。

## 7. AI導入施策カード（デッキ）

施策はカードとして表現する。
本企画ではカードを**デッキ**として扱い、ドラフトで集め、強化していく（ローグライク的構成）。

### 7.1 カードの扱い

* スプリント後のドラフトで、提示された3枚から1枚を選んでデッキに加える
* スプリント開始時、デッキから数枚が手札として配られ、その中から発動する
* ショップ/休息ノードでカードを**強化（アップグレード）**できる（例: コスト減、効果増）
* カードにはレアリティがある（コモン / レア / レジェンダリ）

### 7.2 カード例

#### Copilot全員配布（コモン）

```text
Cost: 10
Coding速度 +15%
定型タスク処理速度 +30%
AI依存度 +5%
```

#### Claude Code解禁（レア）

```text
Cost: 20
通常タスク速度 +20%
複雑タスク成功率 +5%
レビュー負荷 +10%
AI Literacyが低い場合、Rework率 +8%
```

#### Devin導入（レジェンダリ）

```text
Cost: 35
並列実装力 +25%
タスク分割が悪い場合、迷走率 +15%
ドキュメントが高い場合、成功率 +20%
```

#### 自動テスト強化（コモン）

```text
Cost: 18
Rework率 -15%
Incident率 -20%
短期出荷速度 -5%
長期安定性 +20%
```

#### PRサイズ制限（コモン）

```text
Cost: 8
レビュー効率 +15%
巨大PR発生率 -30%
開発者の不満 +5%
```

#### AI利用ガイドライン（レア）

```text
Cost: 12
AI Literacy +15%
Rework率 -8%
効果発現まで2スプリント
```

#### ドキュメント整備（コモン）

```text
Cost: 15
AI成功率 +10%
オンボーディング速度 +15%
短期出荷速度 -8%
```

#### シニア採用（レア）

```text
Cost: 40
レビュー容量 +30%
設計判断力 +20%
採用まで3スプリント
```

#### レビューBot導入（レア）

```text
Cost: 22
小さなPRを自動でレビュー処理
Reviewキューの伸びを抑制
誤検知でたまにノイズが出る（士気 -2%）
```

## 8. 組織文化レリック

ラン中に獲得する**恒久パッシブ**として、組織文化レリックを用意する。
カードが「毎ターン使う手」なら、レリックは「組織にしみついた性質」である。
ボス報酬、イベントの選択結果、ショップ購入などで入手する。

### レリック例

| レリック         | 効果                                    |
| ----------- | ------------------------------------- |
| 心理的安全性      | 悪いイベントによるMoraleダメージ -30%、Morale下限を保証 |
| ポストモーテム文化   | Incidentが起きるたびにTest Coverageが少量上昇     |
| ドキュメント駆動    | AI成功率の下限を底上げ                          |
| 小さく出す文化     | PRサイズを常時抑制、コンボが途切れにくくなる              |
| 強いCI        | Reworkの一部を自動で弾く防御バリアを常設              |
| フロー重視       | 介入アクションのクールダウン -20%                   |
| 金曜デプロイ禁止    | スプリント終盤の事故率を大幅減（ネタ枠）                  |
| 一次情報主義      | ドキュメント/テスト由来の効果が逓増                    |

レリックの組み合わせで、同じ難易度でも組織の「効きどころ」が変わり、ビルドの幅が生まれる。

## 9. ランダムイベント（周回進行の中核エンジン）

イベントは「合間の演出」ではなく、**周回進行の中核**である。四半期トラックのスプリントの合間（第4.4）で
毎回イベント判定を行い、組織に対処させる。

**2種類**:

* **判定イベント**（第9.1〜9.3）: 組織状態に応じた事象が**自動で起きる**（選択なし）。プレイヤーが状況を
  制御しすぎないための緊張感。一部は敗北条件（レビュー停止など）に触れうる（第15章）。
* **選択イベント**（第9.4）: リスク/リターンの2〜3択を迫る。高負荷案件・予算補強・一息つくも、ここに統合した
  選択として提示する。**断る/安全側にも必ず代償**を持たせ、リスクを取らない最適解を作らない。

**組織状態による重み付け**: どのイベントが起きやすいかは、現在の組織状態で決まる。
技術的負債が高い→本番障害、AI依存度が高くリテラシーが低い→誤生成（存在しないAPI・巨大PR）、
シニアHPが低い→レビュー停止・燃え尽き、テスト/ドキュメントが厚い→好転イベント、といった具合に、
**組織の制約が次の事故（や好転）を呼ぶ**。これにより第19・20章のメッセージ（個人生産性と組織生産性の差、
AI導入には土台が要る）を、機構として体験できる。抽選は seed 付き決定論で、デイリーラン/リプレイを保つ。

### 9.1 良いイベント

* AIがテストケースを大量生成した
* 仕様の曖昧さをAIが検出した
* ジュニアがAI活用に覚醒した
* CI改善により手戻りが激減した
* ドキュメントがAIに刺さった
* 小粒タスクが大量に高速処理された

### 9.2 悪いイベント

* 巨大AI生成PRが投下された
* シニアがレビューで燃え尽きた
* AIが存在しないAPIを使った
* テストが通っているのに仕様が違った
* レビュー待ちが20件を超えた
* Devinが前提を誤解して迷走した
* 誰も読めない抽象化が生成された
* "動いているように見える"障害が本番で発覚した
* プロンプト職人が属人化した
* AI利用率だけがKPIになって組織が歪んだ

### 9.3 ネタ寄りイベント

* 「とりあえずAIでやりました」PRが発生
* `any` が増殖した
* READMEが古すぎてAIが騙された
* PR説明がやたら流暢だが中身が怪しい
* AIレビューBot同士が議論を始めた
* テストがすべてモックになった
* リファクタと称した全面改修が始まった
* "軽微な修正"が1,200行になった

### 9.4 選択イベント（リスク/リターンの決断）

スプリント間ビートで選択イベントを引くと、選択肢のあるイベントを提示する。
選択にはトレードオフがあり、レリックや一時バフ、リスクが絡む。**断る/安全側にも代償**を持たせる。

```text
[緊急のお願い] 経営から「来週デモがある」と連絡が来た。

A) 残業して間に合わせる
   → 出荷 +30pt / Morale -15 / Senior HP -10
B) スコープを削って出す
   → 出荷 +10pt / Tech Debt +5
C) 正直に延期を交渉する
   → 出荷 0 / 経営信頼 -低 / レリック「期待値マネジメント」を獲得
```

どの選択肢にも**機械的な代償**がある点に注意（安全に見える C も、出荷ゼロ＝四半期目標から遅れ、
かつ経営信頼が下がる）。ノーリスクで得をする選択を残さないことが、この再設計の肝である（第3章）。
選択の積み重ねが、その組織の「文化」を形作っていく。

## 10. ランとボススプリント

1ランは**1四半期（固定トラックのスプリント列）**で構成し、その**最終スプリントがボススプリント**となる。
分岐マップは無く、何スプリントで山場（ボス）が来るかは確定している（どのルートで着くかは選ばない）。
ボスは通常スプリントの延長ではなく、特別なルールを持つ「山場」として設計する。

### ボス例

#### 大型リリース

```text
期限内に目標出荷ポイントを達成せよ。
タスク量が普段の2倍。渋滞を捌き切れるかが鍵。
```

#### 本番大規模障害

```text
複数の炎上が連続発生する。
緊急対応で鎮火しつつ、出荷を止めずに耐え抜け。
タワーディフェンス的な防衛戦。
```

#### セキュリティ監査

```text
Tech Debtが高いほど失点。
品質・テスト・ドキュメントの蓄積が問われる査定戦。
```

#### 経営レビュー

```text
AI利用率と健全性（Morale・Quality）を両立して見せよ。
"見かけの生産性"だけでは突破できない。
```

ボススプリントは「敵を倒す」よりも、四半期末に組織が外部評価を受ける山場として扱う。
突破時は高評価としてメタ進行ポイントを獲得する（第17章）。未達時も即ゲームオーバーにはせず、四半期レビューで目標修正・スコープ削減・期限延長・品質改善へのピボットなどを選ぶ。
ただし、信頼・予算・士気・Senior HP などの継続リソースが枯渇している場合は、プロジェクト終了や組織再編としてラン終了になる。

## 11. 組織進化ツリー

出荷で得た**進化ポイント**を割り振り、組織能力を恒久的に伸ばすスキルツリーを用意する。
ツリーはラン内で育てるビルド要素であり、「何に強い会社か」を形作る。

### ブランチ構成

| ブランチ   | ノード例                                       |
| ------ | ------------------------------------------ |
| 開発力    | Coding速度上限 / 並列実装枠 / ジュニア成長速度              |
| レビュー体制 | レビュー容量 / 自動レビュー解放 / 割り込みレビュー強化            |
| 品質保証   | Test Coverage効率 / 防御バリア常設 / Incident耐性     |
| AI活用   | AI成功率 / AI Literacy逓増 / AIスロットル強化 / Devin枠 |
| 文化     | Morale上限 / 集中力上限 / レリック所持枠 / コンボ倍率         |

ツリーの伸ばし方によって、
「レビュー無双で渋滞を許さない会社」
「テストとドキュメントで盤石な会社」
「AIに全振りして爆速だが綱渡りな会社」
など、まったく異なるプレイスタイルが生まれる。

## 12. キャラクター育成

開発者・シニアを**個体**として扱い、育成と編成の対象にする。

### 12.1 メンバーステータス

各メンバーは以下を持つ。

* 実装力 / レビュー力 / AI習熟 / スタミナ
* 性格特性（トレイト）

トレイト例:

* AI職人（AIタスク成功率が高い）
* 巨大PR製造機（出力は多いがレビュー負荷を上げる）
* レビュー鬼（レビュー速度が速いがスタミナ消費が大きい）
* ドキュメント魔（Documentationを少しずつ積む）
* ジュニアの星（育成・伝播が速い）
* 燃え尽き気味（スタミナ上限が低い）

### 12.2 成長と編成

* タスク経験でレベルアップし、ジュニア → ミドル → シニアへ昇格する
* どのレーンに誰を置くか、AIを誰に配るかという**編成**が戦術になる
* スタミナが尽きると離脱（休職）リスクが発生し、リソース管理に人間ドラマが生まれる
* メンバーの状態は**キャラの表情**としてビジュアルに反映する（疲れ顔、ガッツポーズなど）

育成があることで、「採用カード」が単なる数値ではなく「未来の主力をどう育てるか」という選択になる。

## 13. 組織タイプ診断

ゲームの途中や終了時に、組織タイプを診断する。

### Healthy Acceleration型

AI導入によって出荷量が増え、レビュー負荷やReworkも制御できている状態。

特徴:

* Coding速度が上がっている
* Reviewが詰まっていない
* Rework率が低い
* テストとドキュメントが整っている

### Review Hell型

AIによってPRが増え、レビュー工程が崩壊している状態。

特徴:

* Review待ちが増加
* シニアHPが減少
* Doneはあまり増えない
* 開発者は速くなった気がしている

### AI Overproduction型

実装量は増えるが、検証能力を超えている状態。

特徴:

* PR数が急増
* 差し戻しが多い
* 品質が不安定
* 見かけの生産性だけ高い

### Rework Spiral型

AI出力の検証不足により、手戻りが連鎖している状態。

特徴:

* Rework率が高い
* 本番障害が増える
* 出荷速度が安定しない
* チームの士気が下がる

### Senior Sacrifice型

シニアがすべてを支えている状態。

特徴:

* シニアレビュー依存
* シニアHPが常に低い
* ジュニアのAI生成物が流れ込む
* 短期的には回るが長期的に破綻する

### Documentation Kingdom型

ドキュメントとテストが整備され、AIが安定して機能している状態。

特徴:

* AI成功率が高い
* Rework率が低い
* オンボーディングが速い
* 長期的に安定する

## 14. 勝利条件

勝利条件は、1ランのクリア基準ではなく、四半期レビューでの評価結果として複数用意する。
ボススプリントや四半期目標を突破すれば高評価となるが、未達でも信頼・予算・士気が残っていれば目標を修正して次四半期へ継続できる。
最終的な勝利は「出荷したか」だけでなく、「どの代償を抑えて組織を継続できたか」で評価する。

### 通常勝利

四半期末のボスを突破し、目標Delivery Scoreを達成する。

### 健全勝利

Delivery Score、Quality、Moraleをすべて一定以上に保ってクリアする。

### AI導入成功勝利

AI利用率を一定以上にしつつ、Rework率とReview詰まりを抑えてクリアする。

### 経営勝利

予算制約内で最大の出荷成果を出してクリアする。

### 現場幸福勝利

MoraleとSenior HPを高く保ちながら、安定した出荷を続けてクリアする。

### カオス勝利

Incidentを連発しながらも、なぜか出荷量だけは最大化してクリアする。

### ノーダメージ勝利（やり込み）

残業号令・アンドンを使わず、炎上の延焼を一度も許さずにボスを突破する。

## 15. 敗北条件 / 継続不能条件

敗北条件は、単一目標の未達ではなく、組織が継続不能になる状態として設定する。
目標未達だけなら四半期レビューで修正・繰り越しできるが、以下が重なるとプロジェクト終了・大規模再編としてラン終了になる。

* Senior HPが0になりレビュー停止
* Tech Debtが上限を超えて開発速度が急落
* Incidentが連続発生してリリース停止
* Moraleが0になりチーム崩壊
* Budgetが尽きてAIツール解約
* Review Queueが上限を超えてPR凍結
* AI依存度が高すぎて人間が仕様を説明できなくなる
* ボススプリントで目標を達成できず、かつ信頼・予算・士気などの代償を払えない

未達時の代表的な継続選択肢は以下。

| 選択 | 効果 | 代償 |
| --- | --- | --- |
| スコープ削減 | Delivery 目標を下げて次期へ継続 | 顧客信頼低下 |
| 期限延長 | 品質と士気を守って再挑戦 | 経営信頼低下・予算消費 |
| 品質改善ピボット | Tech Debt / Incident を下げる | 今期の出荷評価低下 |
| 追加予算申請 | 採用・AIツール・外部支援を得る | 経営信頼低下、次期予算制約 |
| AI導入一時停止 | Review / Rework を安定化 | AI Adoption 評価低下、短期速度低下 |
| 組織再編 | 属人化やレビュー停止をリセット | 士気低下、メンバー離脱リスク |

## 16. 難易度設定と試練

組織の状態を難易度として選べるようにする。

### Easy: モダンなプロダクトチーム

* テストがある
* ドキュメントもそこそこある
* PRサイズが小さい
* AIが効きやすい

### Normal: 普通の開発組織

* 一部にテストがある
* レビューは属人的
* ドキュメントは古い
* AI効果にばらつきがある

### Hard: レガシー業務システム

* 仕様がコードにしかない
* 巨大PRが発生しがち
* シニア依存が強い
* AIが自信満々に間違える

### Nightmare: すべてが暗黙知

* ドキュメントがない
* テストがない
* 仕様を知る人が忙しい
* AIがもっともらしい嘘をつく
* しかし経営はAIで倍速化を期待している

### 試練（ランモディファイア）

クリア後に解放される、ランごとの追加ルール。スコア倍率と引き換えに難度を上げる。

* 集中力 -1（介入の余裕が減る）
* 予算半減（採用も施策も渋い）
* 炎上が燃え広がりやすい
* レビュー容量に上限キャップ
* AI依存度が自然増加していく

## 17. メタ進行とアンロック

ランをまたいで蓄積する**メタ進行**を用意し、繰り返し遊ぶ動機を作る。

* ボス撃破や実績達成でメタ進行ポイントを獲得
* 新しいカード、レリック、開始時の組織（プリセット）を解放
* 解放した要素は次のランのドラフトやショップに登場するようになる
* 実績（例: 「ノー残業クリア」「コンボ x20達成」「全ボス撃破」）でコレクション要素を提供

進化ツリーがラン内ビルドなら、メタ進行は**プレイヤー自身の上達と選択肢の広がり**を担う。

## 18. 視覚表現

画面はゲームUI寄りにする。
「数字が動く」だけでなく、**手応えのある演出（ジューシーさ）**を重視する。

### 18.1 基本演出

* タスク粒がベルトコンベアのように流れる
* Reviewが詰まると赤く点滅する
* Senior HPが減るとシニアアイコンが疲れる（汗・隈）
* Incidentが起きると画面が揺れる
* Doneが増えるとコインやポイントが出る
* Tech Debtが増えると画面下に黒いヘドロが溜まる
* Test Coverageが上がると防御バリアが表示される
* Documentationが増えるとAIタスクに補助線が出る
* AI暴走時はタスク粒が高速でReviewに突っ込む

### 18.2 ジューシーな手応え演出

* **数字ポップ**: 出荷ごとに `+pt` がポップし、高価値タスクは大きく金色で弾ける
* **コンボ表示**: 連続Doneで `COMBO x6` を画面端に表示し、倍率に応じて演出が派手になる
* **渋滞ヒートマップ**: Reviewレーンが混むほど赤熱し、危険度が一目でわかる
* **延焼と鎮火**: 炎上タスクは隣へ燃え広がり、緊急対応の成功で爽快に消火される
* **スイープ演出**: 割り込みレビューで一気にPRがはけると、まとめて流れる気持ちよい動き
* **スローモーション**: ボス障害の最後の1件を捌く瞬間に時間が一瞬止まる

### 18.3 画面ステート（組織の空気感）

組織の状態に応じて、画面全体のトーンを変える。

| 状態          | 演出               |
| ----------- | ---------------- |
| 健全          | 明るい昼、軽快なBGM      |
| 渋滞ぎみ        | 空が曇りはじめる         |
| Review Hell | 画面が赤く染まり、夜になる    |
| 復活          | 朝が来て、色が戻る        |

### 18.4 ご褒美演出

* レリック獲得時は、組織に文化が宿る荘厳な演出
* 進化ツリー解放時は、パキッと枝が伸びる手応え
* リザルトでは称号がドンと表示され、評価ランクSはきらめく特別演出

見た目としては、以下のようなイメージにする。

```text
工場管理ゲーム + タワーディフェンス + ローグライクデッキ構築 + 開発生産性ダッシュボード
```

## 19. 面白さの核

このゲームの面白さは、AI導入を単純な強化アイテムにしないことにある。

AIは強い。
しかし雑に使うと壊れる。

プレイヤーは以下のジレンマに向き合う。

```text
AIを入れるとCodingは速くなる
でもReviewが詰まる

レビューを強化すると品質は上がる
でも短期出荷は落ちる

ドキュメントを書くとAIは賢くなる
でも今スプリントの出荷は減る

テストを増やすと事故は減る
でも初速は落ちる

Devinを入れると並列度は上がる
でもタスク分割が悪いと迷走する
```

加えて、本企画では次の**プレイ中の即時判断**が緊張感を生む。

```text
渋滞も炎上も同時に来る
でも集中力は有限で、全部は救えない

攻めればコンボが伸びて出荷が跳ねる
でも一手間違えると崩壊が連鎖する

残業号令で今を乗り切れる
でも士気とシニアHPは戻ってこない
```

これらの構造的ジレンマと即時判断、そして毎回違うマップ・デッキ・レリックの組み合わせにより、
ゲームとして何度も遊べる。

加えて、四半期レビューでは「未達をどう扱うか」が長期的な面白さになる。

```text
出荷目標を守るために品質を落とすのか
品質を守るために納期を延ばすのか
経営信頼を削ってでもチームを守るのか
顧客信頼を削ってスコープを切るのか
AI導入を進めるのか、一度止めてプロセスを整えるのか
```

プレイヤーは「ボスを倒せるか」ではなく、
**どの約束を守り、どの約束を諦め、どの痛みを次期に持ち越すか**を選び続ける。
これにより、単なる劣化ローグライトではなく、開発組織の継続的な目標調整シミュレーターとして独自性を出す。

## 20. 教育的価値

ゲームとして遊べる一方で、開発組織の生産性に関する重要な学びが得られる。

学べることは以下である。

* 個人生産性と組織生産性は違う
* AI導入はCoding工程だけを速くするとは限らない
* ボトルネックがReviewに移動することがある
* AI導入にはテスト、ドキュメント、レビュー体制が必要
* タスク分割の品質がAI活用の成否を左右する
* シニア依存の組織ではAI導入が逆に負荷を増やす
* 短期速度と長期安定性はトレードオフになる
* マネジメントの注意（集中力）も有限の資源である

## 22. 技術構成

初期版はフロントエンドのみで構築する。
ただし本ゲームは「リアルタイムに多数のタスク粒が流れ、アイソメトリックの盤面を
現場↔部署↔全社とズームする」性質を持つため、
**UIシェル（React）と、ライブな盤面描画（初期はDOM/SVG、規模拡大時はCanvas/WebGL）を分離するハイブリッド構成**を採る。

### 22.1 全体アーキテクチャ

```text
React (UIシェル) ────────────┐  HUD / サブヘッダ / 介入アクションバー / カードドラフト /
  状態: Zustand + XState      │  進化ツリー / スプリント間イベント / リザルト / 業界ランキング表 / パンくず
                             │
DOM/SVG → PixiJS 盤面 ─────┤  タスク粒・アイソメのオフィス/全社プレート・チーム島・
  + pixi-viewport (ズーム/カメラ)│  フロー・渋滞ヒートマップ・延焼・スイープ演出
                             │
Simulation (純TS) ───────────┘  固定タイムステップ＋seed付きPRNGで状態を更新（描画と分離）
  重い試算/バッチは Web Worker (+Comlink)
```

| 領域       | 技術                                          | 役割                                                          |
| -------- | ------------------------------------------- | ----------------------------------------------------------- |
| 言語       | TypeScript                                  | 全レイヤ共通                                                      |
| ビルド/Dev  | Vite                                        | 開発サーバ・バンドル                                                 |
| UIシェル    | React                                       | HUD・アクションバー・カード・進化ツリー・スプリント間イベント（旧マップ）・リザルト・業界ランキング表・パンくず |
| UI演出     | Framer Motion                               | 画面遷移・カード・HUDのマイクロインタラクション                              |
| 盤面描画     | **DOM/SVG → PixiJS + pixi-viewport**（規模拡大時に移行） | タスク粒・アイソメ盤面・チーム島・フロー・ヒートマップ・延焼・**ズーム/カメラ**          |
| 状態管理     | Zustand（ラン/メタ状態）＋ XState（フェーズ遷移）            | 予測可能な状態と、setup→スプリント→ドラフト→進化→ビート のフロー管理（固定トラック＋ビート。`setup`/`beat` フェーズ） |
| シミュレーション | 純TS・固定タイムステップ・**seed付きPRNG**               | 確率モデル本体。描画から分離し、**決定論**で再現可能にする                       |
| 重い試算     | Web Worker（+Comlink）                        | what-if計算・バッチ集計など、フレームを止めたくない処理                        |
| 静的グラフ    | Recharts または visx                           | リザルト/診断の図（ゲーム中のライブメーターはPixi/自前描画）                     |
| 保存       | localStorage（現状）→ IndexedDB（idb / Dexie）   | セーブ・メタ進行・リプレイ。データが育ったらIndexedDBへ移行                    |
| テスト      | Vitest                                      | シミュレーションの不変条件（例: AI依存度↑でRework傾向↑）を担保                  |
| バックエンド   | 当面なし（ローカル擬似）／将来: 最小API                      | 業界ランキングの共有・デイリーランの同一シード配信を“本物の競争”にする場合に追加          |

### 22.2 描画方針（React と Pixi の分担）

* **React は状態を持ち、盤面描画は状態を読んで描くだけ**の一方向に徹する（双方向バインドしない）。初期は DOM/SVG で実装し、Pixi へ差し替えられるよう描画アダプタを分離する。
* タスク粒・盤面・チーム島など **数百〜数千の動的オブジェクト** に達したら Pixi のスプライトへ移行し、DOM/SVG では持たない（DOMは要素数が増えると60fpsを維持できないため）。
* アイソメtrの重なり順（depth sort）・画面外カリング・ズーム/カメラは、Pixi 移行後に `pixi-viewport` とスプライトのソートで扱い、現場↔部署↔全社の遷移（第4.11章）を地続きに実装する。
* HUD・カード・メニュー等の **離散的なUI** は React + Framer Motion のまま（DOMの強みを活かす）。
* 現状の HTML/SVG モックアップ（`mockups/`）は **デザイン・レイアウト検証の正**として維持する。DOM/SVG 実装はこの基準に寄せ、Pixi 移行後も見た目の基準として使う。

### 22.3 シミュレーションと決定論

* シミュレーションは描画から完全に分離し、**固定タイムステップ**で更新する（描画フレームレートと独立）。
* 乱数は **seed付きの決定論PRNG**（例: `mulberry32` 程度の軽量実装）に統一する。これにより、**デイリーラン（同一シード）**・リプレイ・不具合再現・テストが可能になる。
* 大量の試算（バランス調整時のモンテカルロ等）や重いバッチは Web Worker に逃がし、メイン操作のレスポンスを保つ。

### 22.4 段階的移行（過剰投資を避ける）

* **初期段階**: 粒数がまだ制御可能なうちは、盤面は DOM/SVG（モックアップ準拠）で素早く実装し、周回・診断・勝敗の通しプレイを優先して PixiJS への早期投資は避ける。ただし **最初から sim 層を描画から分離し、seed付きで** 書く（描画エンジンを後から差し替え可能にしておく）。
* **巨大組織・4階層ズームの段階**: 数百〜数千オブジェクト、チーム島を扱うため、盤面描画を **PixiJS + pixi-viewport へ移植**してから着手する。粒数・ズーム階層が DOM/SVG の限界に近づく場合は前倒しする。sim を分離済みなら移植コストは小さい。
* React / TypeScript / Framer Motion / Recharts は捨てずに、役割を限定して継続する。

### 22.5 テスト方針

描画とロジックを分離した利点を活かし、**「ロジック・座標・状態→見た目」は GPU不要の Vitest で大量に、「実ピクセル・操作」は実ブラウザの Playwright で少数に**、という二段構えを採る。
既に `mockups/` を Playwright + Chromium で撮っている構成を、そのまま視覚回帰の基盤として流用する。

| レイヤ          | テスト対象                                   | ツール / 環境                    | GPU |
| ------------ | --------------------------------------- | -------------------------- | --- |
| シミュレーション     | 確率モデルの不変条件（AI依存度↑→Rework傾向↑ 等）・seed再現性 | Vitest（純TS）                | 不要  |
| ビューモデル/シーン構築 | アイソメ投影・depth sort・カリング・「状態→見た目」マッピング   | Vitest（純関数）                | 不要  |
| シーングラフ構造     | Sprite/Container の x/y/zIndex/visible/tint | Vitest（`Texture.WHITE`でモック） | 不要  |
| 視覚回帰         | seed＋一時停止で固定したフレームのスクショ比較             | Playwright（実ブラウザ）          | 要   |
| 相互作用 E2E     | 島クリック→ドリルダウン・介入アクション発動               | Playwright                 | 要   |
| パフォーマンス      | カリング数・スプライトプール再利用・粒数上限               | Vitest（カウンタ検証）             | 不要  |

PixiJS の DisplayObject（Container/Sprite）は WebGL なしで生成・座標計算・tint 検証ができるため、
実 GPU が要るのは「実際に描いたピクセルが正しいか」だけ。それを Playwright に集約する。

**決定論フック（テスト容易性の前提）**

* 乱数は seed付きPRNG に一本化し、`?seed=` 等で再現可能にする（第22.3）。
* レンダラは「状態を読んで描くだけ」（第22.2）＝同一状態なら同一フレーム＝スクショが安定する。
* `window.game`（`GameHandle`）にラン駆動 API を露出する。seed は `?seed=` または `startRun` / `newRun` の引数で指定し、E2E から `pause()` と各フェーズ操作（`beginSetupSprint` / `resolveBeat` / `step(ms)` / `dispatch` 等）で状態を固定・進める。公開契約は `src/game.ts` の型定義を正とする。デバッグ専用の `engine` 参照は E2E から使わない。アニメ停止は `document.getAnimations()` を用いる。
* スプライト生成は依存注入にして、テストでモックへ差し替えられるようにする。

**避けること（CIが脆くなる）**

* Node 上で実 WebGL（headless-gl 等）を回す → 実ピクセル検証は Playwright に寄せる。
* FPS を CI で直接 assert する → 代わりにカリング数・プール再利用・生成スプライト数の上限を数値で検証する。

能動操作のリアルタイム描画では、粒の数が増えても破綻しないよう、
**状態更新（シミュレーション）と描画（Pixi）を分離**し、描画は Canvas/WebGL に寄せることを設計の前提とする。

## 23. 拡張案

将来的には以下を追加する。

* GitHub API連携による実データモード
* 実際のPR数、レビュー時間、リードタイムの取り込み
* チーム横断の組織診断ダッシュボード（基本となるズーム階層は第4.7〜4.11章で本体機能化済み。さらに時系列トレンドや部門間比較を深掘りする）
* カードデッキのカスタマイズ
* 組織シナリオ共有
* 社内LT用デモモード
* 経営層向けプレゼンモード
* Devin / Copilot / Claude Code などツール別シナリオ
* チーム対抗スコアランキング
* デイリーラン（全員が同じシード・マップで競う）
* 四半期レビューと目標修正の高度化（OKRテンプレート、ステークホルダー別交渉、複数四半期ロードマップ）
* 「AI導入失敗図鑑」
* 「レビュー地獄リプレイ」
* 「なぜ燃えたか」解説ログ

## 24. 企画の価値

本企画の価値は、AI導入を真面目な分析対象にしつつ、ゲームとして面白く見せられる点にある。

通常、開発生産性の話は抽象的で退屈になりやすい。
しかし、タスクが流れ、レビューが詰まり、シニアHPが削られ、AI生成PRが炎上する形で見せれば、非エンジニアにも直感的に伝わる。

さらに、その渋滞や炎上を**自分の手で捌く**体験にすることで、
「なぜそれが大変なのか」を頭でなく手で理解できる。

特に以下のメッセージを強く伝えられる。

```text
AIは開発組織を速くする可能性がある。
ただし、組織の制約を無視して導入すると、
速くなるのはCodingだけで、全体はむしろ詰まる。
```

このゲームは、AI導入の是非を問うものではない。
AIを活かすために、どの組織能力が必要かを体験的に理解するためのツールである。

## 25. 結論

本企画では、AI導入による開発組織の変化を、確率モデルに基づくWebゲームとして表現する。

プレイヤーはAIツール、レビュー体制、テスト、ドキュメント、採用、タスク分割などに投資しながら、開発組織を成長させる。
そして、スプリント中は流れてくるタスクと渋滞・炎上をリアルタイムに捌き、
ランをまたいでカード・レリック・進化ツリーを積み上げ、自分だけの組織を育てていく。

AIは強力な加速装置である。
しかし、レビュー容量、品質保証、ドキュメント、チームのAIリテラシーが不足していると、組織は簡単に詰まる。

その構造を、数字だけでなく、動き・介入・イベント・称号・失敗パターンとして見せることで、AI時代の開発組織運営を楽しく理解できるようにする。

本アプリケーションは、
**「AI導入の説明資料」ではなく、AI時代の開発組織を遊びながら理解するシミュレーションゲーム**
である。
</content>
</invoke>
