# 残課題バックログ

現在の未充足・保留事項を管理する。完了項目は追跡可能な要約だけを残し、具体的なPR、係数、検討過程はGit履歴と[playtest-findings.md](./playtest-findings.md)を参照する。

## 1. 現在の課題

| ID | 課題 | 優先度 | 状態 | SPEC |
| --- | --- | --- | --- | --- |
| RI-101 | 敗北直前の実質的な選択肢を反実仮想評価できない | 高 | 未着手 | 第19.1（F-8・F-9）/ 第22.5 |
| RI-102 | スプリント間の戦略層がプレイヤーを急かさないことを検証できない | 低 | 未着手 | 第19.1（F-3）/ 第22.5 |
| RI-104 | バランスパラメータSSoTの導入 | 高 | 未着手 | 第21 / 第22.3 / 第22.5 |
| RI-105 | SSoT対象境界と互換方針の確定 | 高 | 未着手 | 第21 / 第22.3 / 第22.5 |
| RI-106 | 型付きバランスレジストリと検証基盤 | 高 | 未着手 | 第21 / 第22.5 |
| RI-107 | バランスパラメータ表の生成とCI差分検査 | 高 | 未着手 | 第21 / 第22.5 |
| RI-108 | 詳細工程モデルのパラメータ移行 | 高 | 未着手 | 第2 / 第22.3 / 第22.5 |
| RI-109 | メンバー・採用パラメータの移行 | 中 | 未着手 | 第12 / 第22.3 / 第22.5 |
| RI-110 | 介入パラメータの移行 | 高 | 未着手 | 第6 / 第22.3 / 第22.5 |
| RI-111 | ラン進行・経済パラメータの移行 | 中 | 未着手 | 第3 / 第9〜11 / 第22.3 |
| RI-112 | 四半期KPI・勝敗閾値の移行 | 高 | 未着手 | 第14〜16 / 第22.5 |
| RI-113 | 粗粒度チームモデルのパラメータ移行 | 中 | 未着手 | 第4.7〜4.11 / 第22.3 / 第22.5 |
| RI-114 | スプリント負荷・ペーシングパラメータの移行 | 中 | 未着手 | 第3.1 / 第22.3 / 第22.5 |
| RI-115 | 既存コンテンツ正本の生成カタログへの集約 | 中 | 未着手 | 第7〜11 / 第16 / 第21 |
| RI-116 | バランスルールセットの版と指紋 | 高 | 未着手 | 第17 / 第22.3 / 第22.5 |
| RI-117 | セーブへのルールセット記録と互換判定 | 高 | 未着手 | 第17 / 第22.3 / 第22.5 |
| RI-118 | リプレイへのルールセット記録と表示 | 中 | 未着手 | 第17 / 第22.3 / 第22.5 |
| RI-119 | バランス差分・多数seedレポート | 中 | 未着手 | 第19.1 / 第22.5 |
| RI-120 | タスク分布・スプリント評価パラメータの移行 | 中 | 未着手 | 第4.6 / 第22.3 / 第22.5 |
| RI-121 | デイリー・診断情報へのルールセット伝播 | 中 | 未着手 | 第17 / 第22.3 / 第22.5 |
| RI-122 | カード実行ルールのパラメータ移行 | 中 | 未着手 | 第7 / 第22.3 / 第22.5 |
| RI-123 | 代表確率曲線のSSoT生成 | 中 | 未着手 | 第2 / 第22.5 |
| RI-34 | ローカル完結の将来拡張 | 低 | 保留（要判断） | 第23 |

### RI-101 敗北直前の実質的な選択肢を反実仮想評価できない

RI-89で危険域における介入の機械的な発動可否は記録できるが、その介入が敗北を遅延・回避するかは評価できない。効果の無い介入も「打てる手」に含まれるため、F-8とF-9の有効手を検証できていない。

受入条件:

- 代表的な危険状態を決定論的に複製し、無介入と適用可能な介入・選択肢を同じ乱数状態から分岐比較する。
- 敗北までのスプリント数、危険域からの離脱、最終敗因を記録し、有効な選択肢が残る最後の時点を敗因別に集計する。
- F-8の回復余地とF-9の敗因別の有効手を別々に判定する。
- 状態複製、乱数再現、分岐数の上限、集計規則をユニットテストで固定する。

RI-89の観測結果を入力に使う。難易度は**高**。

### RI-102 スプリント間の戦略層がプレイヤーを急かさないことを検証できない

SPEC F-3のプレイテスト観測項目は追加済みだが、戦略フェーズが入力を待ち続ける自動テスト契約が無い。

受入条件:

- `setup` / `result` / `draft` / `evolution` / `beat` / `shop` / `rest` / `recruit` / `quarterReview` で、入力なしにフェーズ・選択・資源が変化しない。
- カウントダウン、自動決定、リアルタイム層と同じ進行圧が無いことをユニットまたはE2Eで固定する。

既存仕様の検証追加が中心であり、難易度は**低**。

### RI-104 バランスパラメータSSoTの導入

調整対象の値が工程モデル、タスク生成、メンバー、介入、ラン進行、KPI、粗粒度モデル、ペーシングへ分散し、ゲーム・確率モデル文書・グラフ・プレイテスト補助スクリプトが同じ値を共有できていない。[balance-ssot-plan.md](./balance-ssot-plan.md)を設計正本とし、型付きの分割レジストリと既存`src/data/`定義から、ゲーム実行値、生成文書、ルールセット識別子を導出する。

完了条件:

- 調整対象の基本値に安定ID、値、単位、許容範囲、説明、タグがあり、派生値を別の基本値として重複保持しない。
- ゲームと生成パラメータ表・代表曲線が同じ定義を参照し、`balance:check`が定義違反と生成差分を検出する。
- 値の移動だけを行うPRは乱数消費順と同一seed・同一入力の結果を変えず、値を調整するPRと分離する。
- ルールセットの版と指紋をセーブ、リプレイ、デイリー、診断情報へ記録し、旧データと不一致時の扱いをテストで固定する。
- 外部設定サービス、実行時上書き、管理画面、数式DSLは導入しない。

実装順は RI-105 → RI-106 → RI-107 → RI-108〜114・RI-120・RI-122 → RI-123 → RI-116 → RI-117 → RI-118 → RI-121 を基本とする。RI-119は基盤完了後の調整支援であり、親エピックの完了を妨げない独立課題とする。RI-73／RI-76完了後の現行値と再計測結果を移行前ベースラインとし、SSoTへの移動PRでは値を変更しない。AI依存モデルの意味変更は値の移動ではないため、本エピックには含めず別課題として扱う。

### RI-105 SSoT対象境界と互換方針の確定

[balance-ssot-plan.md](./balance-ssot-plan.md)の着手前判断を最新`main`に合わせて確定する文書PR。対象値と除外理由を棚卸しし、保存中ランのルールセット不一致UX、版の更新規則、ペーシングの分類、生成物のGit管理、多数seedレポートの実行方式を決める。

受入条件:

- `src/sim/`、`src/data/`、`src/ui/sprintTempo.ts`、プレイテスト／文書生成スクリプトの調整値と直接参照を一覧化する。
- プロトコル不変値、表示専用値、テスト専用値をSSoTへ含めない理由とともに分類する。
- 未決事項の選択結果がRI-106以降の受入条件へ反映され、実装PRで設計判断をやり直さずに済む。

### RI-106 型付きバランスレジストリと検証基盤

`src/data/balance/`へ型、定義ヘルパー、集約入口を追加し、代表値を少数だけ移す。ID重複、非有限値、単位別範囲、確率分布、最小・最大の逆転をユニットテストで検出し、既存exportは互換別名として維持する。

受入条件:

- エントリーが`id`、`value`、`unit`、`allowedRange`、`label`、`description`、`tags`、派生値区分を型安全に持つ。
- 不正な定義を網羅する検証テストと、代表値移行前後の同一seed回帰が通る。
- 実行時の外部I/Oと設定上書きを追加せず、静的バンドルを維持する。

### RI-107 バランスパラメータ表の生成とCI差分検査

レジストリから`plan/generated/balance-parameters.md`を生成する`balance:docs`と、定義検証・生成差分を検査する`balance:check`を追加する。生成物には直接編集禁止を明記し、CIで更新漏れを検出する。

受入条件:

- 同じ定義から安定順序・再現可能なMarkdownを生成し、生成時刻などの非決定的情報を含めない。
- `npm run balance:check`が不変条件違反と未生成差分の両方で失敗し、通常CIへ組み込まれる。
- このPRでは代表曲線の置換や大規模な値移動を行わない。

### RI-108 詳細工程モデルのパラメータ移行

`src/sim/model/process.ts`のCoding、Review、Rework、Incident、Security、炎上、コンボの基本値、式内係数、`clamp`境界を`balance/process.ts`へ移す。既存exportと評価順を維持し、値の移動だけでモデルの意味を変えない。

受入条件:

- Coding、Review、Rework、Incident、Security、炎上、コンボを安定IDで調整できる。
- 移行前後で固定seed結果、単調性、既存統計レンジが一致する。
- AI依存モデル再設計、値調整、確率曲線生成、タスク分布の移行を同じPRへ含めない。

### RI-109 メンバー・採用パラメータの移行

`src/sim/member/roster.ts`の能力倍率、スタミナ、成長、休職、復帰、負荷分散、採用費・人数上限を`balance/member.ts`へ移す。スターター編成や名前などのコンテンツ定義は移動しない。

受入条件:

- メンバー領域の調整値を安定IDから参照し、既存exportが必要な箇所は互換別名を残す。
- 編成、成長、休職、復帰、採用の既存ユニットテストと代表seed結果が移行前後で一致する。

### RI-110 介入パラメータの移行

`src/data/actions.ts`と`src/sim/actions.ts`の集中力コスト、クールダウン、ゲージ、効果量、副作用、状況閾値、持続tickを`balance/actions.ts`へ移す。アクションID、ラベル、説明は既存コンテンツ定義に残す。

受入条件:

- 介入の実行判定、効果適用、ActionBar表示が同じレジストリ値を参照する。
- 発動可否、対象選択、コスト、クールダウン、効果タグ、固定seed結果が移行前後で一致する。
- RI-73で確定した介入値を変更せず、値調整を同じPRに含めない。

### RI-111 ラン進行・経済パラメータの移行

`src/sim/run/constants.ts`、`effects.ts`、`engine.ts`にある四半期構成、イベント率、休息、ショップ、進化報酬、インフラ費用などを`balance/run.ts`へ移す。フェーズ一覧、保存可能フェーズ、イベントIDはプロトコル／コンテンツ定義として移動しない。

受入条件:

- ラン進行と経済の基本値を安定IDで参照する。
- ランループ、イベント選択、休息、ショップ、進化、インフラ費用の既存テストと代表seed結果が一致する。
- `scripts/playtest-report.mjs`の定数直接読取を壊さず、レジストリを正規に参照する方式へ更新する。

### RI-112 四半期KPI・勝敗閾値の移行

`src/sim/run/quarterReview.ts`と`src/sim/outcome.ts`の目標倍率、下限、評価閾値、継続不能条件、勝利種別閾値を`balance/run.ts`または専用の`balance/outcome.ts`へ移す。結果ラベルや敗因IDは移動しない。

受入条件:

- KPIと勝敗の閾値を安定IDから参照し、派生目標は基本値から計算する。
- 四半期レビュー、目標修正、敗北、勝利種別の境界テストと代表seed結果が一致する。
- RI-73／RI-76で確定した値と判定境界を変更せず、値調整を同じPRに含めない。

### RI-113 粗粒度チームモデルのパラメータ移行

`src/sim/orgscale/teamState.ts`の初期分布、出荷、行列、Incident、状態ドリフト、入り込みコストを`balance/coarse-team.ts`へ移す。重複しているReview容量・Incident bias計算を純関数へ寄せ、値を変えずに投影と進行で共有する。

受入条件:

- 粗粒度モデルの基本値と境界を安定IDから参照し、同じ式・係数の重複保持を除く。
- 固定seed結果を維持し、詳細モデルと粗粒度モデルでAI、Review、品質、Incidentの因果方向が一致するテストを追加する。
- 式の共通化によって評価順や丸め位置を変えない。

### RI-114 スプリント負荷・ペーシングパラメータの移行

`src/sim/run/sprintBaselineBuild.ts`の難易度別タスク床、elite倍率、通常／ボスの最小・最大tickと、RI-105で調整対象と決めた`src/ui/sprintTempo.ts`の壁時計換算・体験目標帯を`balance/pacing.ts`へ移す。フレーム処理やタブ復帰上限など表示・実行安全専用の値は分離したままにする。

受入条件:

- 難易度別タスク床、通常／ボスのtick境界、`MS_PER_TICK_1X`、通常／ボス／四半期／ランの目標時間帯、介入回数帯を単位付きで参照する。
- tick駆動のsim決定論を変更せず、既存ペーシング統計とUI速度テストが一致する。

### RI-115 既存コンテンツ正本の生成カタログへの集約

カード、イベント、難易度、試練、ボス、レリック、特性、進化など既存`src/data/`定義を正本のまま、生成パラメータ表と後続の指紋入力へ集約する。値を新しいレジストリへ複製しない。

受入条件:

- ゲーム結果へ影響するコンテンツのID、値、重みを安定順序で列挙できる。
- 既存定義と生成カタログの間に二重管理を作らず、追加・削除・重複をテストで検出する。
- 表示専用メタデータとゲーム結果へ影響する値を区別する。

### RI-116 バランスルールセットの版と指紋

手動更新する`BALANCE_RULESET_VERSION`と、バランスレジストリおよびRI-115のコンテンツカタログを安定順序で正規化した決定論的指紋を追加する。指紋計算はブラウザとNodeの双方で同じ結果になる方式にする。

受入条件:

- 列挙順の違いでは指紋が変わらず、ゲーム結果へ影響する値・IDの変更では指紋が変わる。
- 版の更新規則と指紋対象を生成文書へ表示し、ユニットテストで固定する。
- seedや入力列を指紋へ混ぜず、ルールセット識別と実行識別を分離する。

### RI-117 セーブへのルールセット記録と互換判定

ラン途中セーブへルールセット版・指紋を記録し、RI-105で決めた不一致時の継続可否とUXを実装する。保存スキーマ更新、旧セーブ補完、読込時検証を同じPRで扱う。

受入条件:

- 新規セーブがルールセット情報を往復し、旧スキーマと不一致データを方針どおりに扱う。
- 不一致時に無言で現行ルールとして再開せず、継続、警告付き継続、無効化の決定結果をUIとテストへ反映する。
- メタ進行データを不要に無効化しない。

### RI-118 リプレイへのルールセット記録と表示

リプレイの保存スキーマと表示へルールセット版・指紋を追加し、記録時ルールの表示と現行ルールでの再計算を区別する。旧リプレイの読込方針も同じPRで固定する。

受入条件:

- リプレイがseedとルールセットを組で提示し、旧リプレイも方針どおりに読める。
- ルールセット変更前後のリプレイを同一結果として誤表示しない。

### RI-119 バランス差分・多数seedレポート

同一seedペアでパラメータ変更前後を比較し、勝率、分位点、Delivery、Incident、Reworkと主要パラメータの感度を出力する。CI毎回実行か手動・定期実行かはRI-105の判断に従い、開発用閲覧画面は必要性が確認できるまで対象外とする。

受入条件:

- ルールセット版・指紋とコホート条件を記録した再現可能なレポートを生成する。
- 設定値の差分と結果分布の差分を分け、既存`npm run playtest`の判定を重複実装しない。
- サンプル数、seed集合、比較対象、欠損ランの扱いをテストまたは固定fixtureで検証する。

### RI-120 タスク分布・スプリント評価パラメータの移行

`src/sim/sprint.ts`のタスク種別重み、高価値率、完了時の状態変化、評価ペナルティ、評価境界、称号・診断閾値を`balance/sprint.ts`へ移す。配列順、乱数消費順、フォールバックを維持する。

受入条件:

- タスク分布、評価、称号の調整値を安定IDから参照し、合計1の分布を検証する。
- タスク列、評価・称号境界、代表seed結果が移行前後で一致する。
- 詳細工程式の移行や値調整を同じPRへ含めない。

### RI-121 デイリー・診断情報へのルールセット伝播

デイリーランのseed・記録と、不具合再現に使う公開診断情報へルールセット版・指紋を伝播する。旧日付記録との互換方針はRI-105の決定に従う。

受入条件:

- デイリー識別子が日付とルールセットを含み、同日・同ルールセットの決定論を維持する。
- 診断情報がseed、版、指紋を組で提示し、再現条件をコピーできる。
- 旧デイリー記録とメタ進行を不要に失わない。

### RI-122 カード実行ルールのパラメータ移行

`src/sim/cards.ts`の手札枚数、強化倍率、集中力下限、優先ドラフト重み、効果`clamp`境界など、カード定義そのものではない共通実行ルールを`balance/cards.ts`へ移す。

受入条件:

- カードID、価格、効果値は既存`src/data/cards.ts`を正本のまま維持し、実行ルールだけを安定ID化する。
- ドロー、強化、発動コスト、効果合成、優先ドラフトの境界テストと代表seed結果が一致する。

### RI-123 代表確率曲線のSSoT生成

RI-108で移行した値と純粋な計算関数から代表確率曲線SVGを決定論的に生成し、`probability-model.md`の現行値スナップショットを生成物へ置き換える。AI依存モデルの候補曲線は現行値と明確に分離する。

受入条件:

- `balance:docs`がMarkdown表と代表曲線を同じレジストリから生成する。
- SVG生成が冪等で、定義変更時だけ`balance:check`に差分が出る。
- 曲線生成のためにゲーム式を複製せず、モデル変更を同じPRへ含めない。

### RI-34 ローカル完結の将来拡張

外部API・共有バックエンドは対象外。着手時に個別IDへ切り出す。

- ツール別シナリオ
- 組織シナリオ共有（ローカルファイル／文字列による入出力）
- 組織診断ダッシュボードの時系列・部門比較
- 四半期レビューの高度化

カードデッキの研修方針、デイリーラン、AI導入失敗図鑑、レビュー地獄リプレイ、「なぜ燃えたか」は実装済み。社内LT／経営プレゼンモードと外部サービス連携は対象外。

## 2. 確定した判断

- Zustandは要件ではない。`RunEngine`を状態の正本とし、決定論、保存、リプレイ、UI同期を満たせばよい。
- 巨大組織は表示用の合成チームではなく、独立状態を持つ複数チームとして扱う。
- カード、レリック、進化ノードのSPEC内の具体例は方向性であり、実装データとの差を許容する。
- カードはラン外のコレクション画面で一覧・解放状況を確認できるようにする。

## 3. 完了項目の要約

| グループ | ID | 完了内容 |
| --- | --- | --- |
| 描画・演出 | RI-01〜RI-10, RI-59, RI-63, RI-92 | 等角表示、Pixi盤面、カメラ、演出、音響、ゲームSVGアセット統合 |
| 技術基盤 | RI-11〜RI-13, RI-39, RI-57, RI-58, RI-61 | Pixi既定化、分割ロード、Worker/Recharts、遷移契約、IndexedDB、セーブ、リプレイ |
| バランス基盤 | RI-14〜RI-19, RI-41, RI-56, RI-62, RI-66 | モンテカルロ許容レンジ、代表seed、介入効果、ペーシング統計 |
| メタ・コンテンツ | RI-20〜RI-32, RI-42, RI-65 | コレクション、診断、デイリー、採用・育成、試練、レリック、カード図鑑 |
| ラン進行 | RI-33, RI-35〜RI-40 | 固定四半期トラック、イベント判定、契約・通しテスト |
| UXフィードバック | RI-43〜RI-55, RI-60 | 効果タグ、what-if、HUD、介入演出・ログ・分析、チュートリアル |
| 組織スケール | RI-64 | 独立チーム状態、粗粒度sim、横断施策、保存／リプレイ |
| プレイテスト改善 | RI-67〜RI-71, RI-73〜RI-90 | 導線、KPI、ペーシング、難易度カーブと固定強手、勝利種別のビルド分岐、AI・投資・敗因・評価・マリガン・F-1〜F-12の計測と改善 |
| テスト品質 | RI-72, RI-91 | Mutation run 30261978402 / 30698016740向けユニット強化（RI-91はIssue [#187](https://github.com/nimiusrd/devops-tycoon/issues/187)） |
| レイアウト基盤 | RI-93〜RI-100 | 5 viewport契約、名前付きスロット、共通AspectStage、CSS境界、レスポンシブ・ビジュアルトークン正本、1024×768・6レリックの盤面最低寸法 |
| 第23章の実装済み拡張 | RI-34′, RI-34″, RI-34‴, RI-34⁗ | 炎上原因、失敗図鑑、レビュー地獄リプレイ、研修方針 |

RI-24とRI-25は「追加実装しない」と決定して完了した項目を含む。

## 4. バックログ運用

- IDは`RI-NN`の連番とし、欠番を再利用しない。
- ミューテーション改善はエピック`RI-{N}`（再ベースラインごと）と実装単位`RI-{N}-{GROUP}{SEQ}`（1PR = 1 GitHub Issue）に分ける。採番・進捗の正本はGitHub Issueとし、本ファイルへ番号予約や状態台帳を置かない。方針は[mutation-remediation.md](./mutation-remediation.md)を参照する。
- 状態は`未着手`、`進行中`、`保留（要判断）`、`完了`を使う。
- 1項目は原則1PRで完了できる大きさに分割する。エピックは受入条件と分割順を記載する。
- 完了時は個別節を削除し、上の完了要約と[spec-mapping.md](./spec-mapping.md)を更新する。
