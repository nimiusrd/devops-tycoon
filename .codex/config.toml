# Select the user-defined Docker profile for this project only.
# The profile definition is kept in ~/.codex/config.toml because project-local
# [permissions] entries are not active in the supported Codex CLI.
# Trust this repository in Codex when prompted before starting a task.
default_permissions = "workspace-docker"
