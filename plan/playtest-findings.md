# プレイテスト所見（2026-07-20）

実機プレイで洗い出した改善課題の記録。バランス・オンボーディング・UI・ポリッシュを対象とし、
実行可能な項目は[remaining-issues.md](./remaining-issues.md)へ `RI-67`〜`RI-71` として登録した。

## 検証条件

- ビルド: `npm run dev`（ポート5174）／DOMレンダラ（`?renderer=dom`）
- seed: `review-hell`、難易度: Easy、試練なし
- `window.game`（第22.5）ファサードでフェーズを駆動し、タイトル→スプリント→リザルト→ドラフト→進化→ボス→四半期レビューまで通しプレイ
- プレイ方針を3通りで比較（無介入 / 介入のみ / 編成でレビュー分散＋介入）

## 結論の要約

コアループは良プレイで第2四半期に勝利でき、リザルト・組織診断・盤面演出・決定論の完成度は高い。
一方で、初見・受動プレイヤーの体験と、四半期レビューの看板KPIに明確な課題がある。

## 課題一覧

### RI-67 オンボーディングとシニア燃え尽きの断絶（優先度: 高）— 完了

チュートリアル／遊び方／HUD で緊急対応へ導き、Easy・`review-hell` の生存をユニットテストで固定した（sim 定数は未変更）。以下は当時の所見。

Easy でも「素直に遊ぶ」と第1四半期スプリント2でシニア燃え尽き（`seniorBurnout`）敗北する。

同一 seed（`review-hell`・Easy）で、介入と編成を切り分けた統制実験の結果:

| プレイ方針 | 結果 |
| --- | --- |
| D 無介入（自動進行のみ） | Q1 sprint2 で `seniorBurnout` 敗北 |
| A 編成のみ（middle を review へ、介入なし） | それでも Q1 sprint2 で敗北 |
| B 介入のみ（緊急対応＋andon、編成そのまま） | 12スプリント以上を燃え尽きずに継続 |
| C 編成＋介入（B に編成変更を追加） | Q1 sprint2 で敗北 |

敗北条件は `org.seniorHp <= 1`（`src/sim/outcome.ts:57`）で、これはメンバー個別の stamina ではなく
**抽象的なグローバル値**である。`org.seniorHp` の主な減少要因は次の2つ:

- レビュー1件ごとの一律消費 `REVIEW_HP_COST`=1.6（`reviewOne`、`src/sim/sprint.ts:252`）。担当のランク・
  人数に関わらず同額を削る。
- 猶予切れの炎上を受動処理する**自動鎮火** `INCIDENT_HP_COST`=12（`advanceBurning`、`src/sim/sprint.ts:340`。
  リザルトの `シニアHP -12` の主因）。緊急対応（firefight）で先に鎮火すれば `FIREFIGHT_HP_COST`=2 で済む。

スプリントは全タスクが Done になるまで進む（`isDrained`、`src/sim/sprint.ts:480`）。実測では第1スプリントは
seed 固定 25 タスクを endTick 46〜71 で完了し、`maxTicks`=1500（`src/sim/scenarios.ts:46`）の打ち切りには
達しない。ただしレビュー**回数**はタスク数固定ではない: 手戻りタスクは Rework 経由で Review へ戻り
（`advanceRework`、`src/sim/sprint.ts:378`）、緊急対応で鎮火したタスクも Review へ戻る（`fire.lane='review'`、
`src/sim/actions.ts:151`）ため、レビュー試行数＝タスク数＋（手戻り・炎上の再レビュー）となり、手戻り率・炎上率に
応じて増減する。したがって**手戻り率**を下げる施策（品質・PR分割・AIスロットル。`reworkProbability` の quality・
`task.split`・aiAssisted・aiDependency 項）は再レビュー回数を減らし、`REVIEW_HP_COST` と炎上判定回数を**間接的に**
減らしうる。ただしこれは「レビュー1件あたりの点火率（`incidentProbability`）」を下げるのとは別で、点火率が参照するのは
testCoverage・`task.aiAssisted`・カード効果のみ（品質値・`task.split` は参照しない）。一方 `org.seniorHp` は
毎 tick `REVIEW_HP_REGEN`=0.7
（炎上中は `BURNING_REGEN_MUL`=0.5 で半減、`src/sim/sprint.ts:474`）で回復し、炎上確率 `incidentProbability` は
レビュー1件ごとに評価され**待ち行列長を入力に取らない**（`src/sim/model/process.ts`）。よって「待ち行列を短くすれば
点火が減る」は成り立たない（当初この所見が編成の待ち行列短縮を生存要因としていた点を、Codex レビューの指摘と
下記実測で訂正）。

第1スプリントの実測（同一 seed、条件別の自動鎮火回数と終了時 `org.seniorHp`）:

| 条件 | endTick | 自動鎮火(−12) | HP終値 |
| --- | --- | --- | --- |
| D 無介入 | 65 | 3 | 42 |
| A 編成のみ | 54 | 3 | 36 |
| 緊急対応のみ | 62 | 1 | 62 |
| B 緊急対応＋andon | 71 | 0 | 74 |
| C 編成＋介入 | 46 | 1 | 55 |

読み取れる実際の要因（直接測定できた範囲）:

- **HP を最も動かすのは自動鎮火（−12）の回数**で、これを直接減らすのが **緊急対応**（−12 になる前に −2 で鎮火。
  ただし鎮火後は Review へ戻り再レビュー1件ぶんの `REVIEW_HP_COST` は掛かる）。緊急対応のみで自動鎮火 3→1、
  HP 42→62 に改善し、これは条件を1つだけ変えた比較として最も確度が高い。andon はレビュー件数を減らすのではなく、
  流入を一時停止（`intake` の早期 return、`src/sim/sprint.ts:183`、通常 30 tick）して**既存キューを捌く猶予**を作り、
  同時炎上を抑えて緊急対応を間に合わせる補助（B で自動鎮火 0）。
- **編成（middle を Review へ）は単一要因ではない**。review 増員（`reviewCapacityMul`）だけでなく Coding 担当が減り、
  `foldFormationEffects`（`src/sim/member/roster.ts:251`）経由で `codingSpeedMul` / `codingSlotBonus` / `aiAdoptionShare`
  と AI 由来の確率補正が次スプリント生成に同時適用される。C（編成＋介入）が B（介入のみ）より早く燃え尽きた事実は
  確かだが、複数因子が連動するため B/C 差を「レビュー速度×回復」等の単一機序に帰属できない。確実に言えるのは、
  **編成変更はシニアHP保護の主手段ではなく（A は無介入と同じく即死）、燃え尽き回避を編成に頼らせるべきでない**こと。
  厳密な寄与分解には Coding 側を統制した比較が要る（RI-66 の統計検証と合わせて後追い）。
- AIスロットルは流入を止めず、新規タスクの `aiAssisted` を false にして AI 由来の手戻り・炎上確率
  （`reworkProbability` / `incidentProbability` の aiAssisted・aiDependency 項）を下げる。上記のとおり手戻り減は
  再レビュー減につながるため、HP 消費・炎上判定の**間接的な**緩和にはなる。

素直プレイが即死する構造は、シニア1人＝唯一のレビュアー（`src/data/members.ts` `STARTER_ARCHETYPES`）で
炎上が起きやすいうえ、チュートリアル（`src/ui/tutorial.ts`）が介入バー・レビュー渋滞・コンボの3点のみで、
**シニア体力（燃え尽き）と、燃え尽きを防ぐ主手段＝炎上を自動鎮火前に緊急対応で鎮火することを教えていない**ため。
Easy は導入難易度（「モダンなプロダクトチーム」）であり、この即死は体験を損なう。

改善方向（いずれか／組み合わせ）:

- チュートリアルに「シニア体力（燃え尽き）」と、それを防ぐ最大手段＝炎上を自動鎮火 −12 になる前に緊急対応で
  −2 鎮火することを追加する。補助として、andon（既存キューを捌く猶予）、AIスロットル（レビュー1件あたりの
  点火率と手戻り率を下げる）、品質・PR分割（手戻り率を下げて再レビューを減らす。点火率自体は下げない）も
  位置づける。編成での review 増員はスループット策であり燃え尽き回避の主手段ではない点を明確にする。
- `org.seniorHp` の収支（`REVIEW_HP_COST` / `INCIDENT_HP_COST` / `REVIEW_HP_REGEN`、`src/sim/model/process.ts` ほか）や、
  Easy 序盤の障害率を見直し、標準的な操作理解のプレイが2スプリントで即死しないようにする。
- 燃え尽きが近いときに盤面・HUD で明確に警告し、対処アクション（緊急対応・andon・スロットル）へ誘導する。

### RI-68 四半期レビュー Delivery KPI のスケール不整合（優先度: 高）

第2四半期レビューで **Delivery 目標20 に対し実績1693（約80倍超過）**、Quality/Morale/TechDebt も
すべて「超過」。目標生成（`src/sim/run/quarterReview.ts` の `deliveryTarget`、概ね1スプリント相当の
`minSprintDelivered` ベース）に対し、判定は四半期累計 `quarterTotals.delivered` を比較しているため、
看板KPIの「出荷ポイント＝勝利条件の進捗」が常に自明達成となり意味を失う。プレイヤーには
「目標20 → 実績1693 超過」が壊れて見える。

改善方向:

- `deliveryTarget` を四半期の実出力スケールに合わせて設定する、
  または判定・表示の単位（1スプリント平均 vs 四半期累計）を一致させる。
- 実際に効いている制約（Morale・Quality・Incident・燃え尽き）が勝敗条件として伝わる表示にする。

### RI-69 スプリント上部操作バーと盤面の重なり（優先度: 中）

デスクトップ幅（1440px、DOM レンダラ `?renderer=dom` で実測）で、上部の操作バー（`通常スプリント /
❚❚ / 1x-2x / 渋滞メーター / 炎上タイマー / COMBO`）が盤面領域の上端と重なり見切れる。1行への詰め込み過多。
DOM 経路の盤面は Pixi キャンバスではなく DOM 要素（`FlowArrows` / `TaskDot`）で描画されるため、これは
レンダラ非依存のヘッダ／盤面コンテナのレイアウト問題として扱う（Pixi レンダラでも別途再現確認する）。
対象: スプリント画面のヘッダ／盤面レイアウト（`src/ui/SprintScreen.tsx` 周辺）。

### RI-70 モバイルのスプリント操作性（優先度: 中）

390px 幅で横スクロール・崩れは無い（`documentElement.scrollWidth === innerWidth`）が、縦積みの
KPIカードの下に盤面とアクションバーが来るため、時限スプリント中に盤面のタスク流動と介入ボタンを
見るのにスクロールが必要。リアルタイム介入ゲームとして操作性が落ちる。
改善方向: 狭幅ではKPIを折り畳み／要約し、盤面＋アクションバーを一画面に収める。

### RI-71 favicon 未設定による 404（優先度: 低）

`index.html` に favicon 宣言が無く `public/` にも実体が無いため、`/favicon.ico → 404` が毎ロード時に
コンソールへ出る（`curl` で 404 を確認）。ブラウザタブのアイコンも空。`<link rel="icon">` の追加と
アイコン配置で解消できる。

## 参考: 既存の未充足（プレイでも該当を体感）

- RI-64 独立状態を持つ複数チーム運営（全社・部署ビューはプレイヤーチームからの合成）
- RI-66 ペーシング統計検証の補強

### RI-84 介入が結果の分散を下げない（優先度: 中 / F-5）

> **この節の結論は撤回済み。現行の判定は第2回の [RI-84](#ri-84-条件を揃えると介入の寄与がほぼ消える優先度-高--f-5--完了) を参照する。**
>
> ここでは「分散は抑えていないが**期待値は押し上げている**」としているが、これは
> 介入と同時に進化・カード・採用が変わっている条件で比較していたためだった。
> 介入だけを外した統制条件 `noInterventionCtl` を立てると 11勝 vs `skilledNoHire` 15勝で、
> **期待値への寄与もほとんど無い**。分散の測り方（同一スプリント番号ごとの CV）も改めている。
>
> 末尾の「対応の方向は2つあり、どちらを取るかは設計判断」も解決済みで、
> **実装を定義へ寄せる**（介入に下振れを潰す性質を持たせる）と決定した（2026-07-26）。
> SPEC 第19.1 F-5 に明記してある。以下は経緯として残す。

SPEC 第19.1 F-5 は「無介入は損ではなく不安定。介入の価値は期待値の底上げよりも**結果の偏差を抑えること**」と
定義している。ラン長の差が混入しない指標で測ると、この関係が成立していない。

easy の出荷の変動係数（CV）:

| 方針 | 1スプリントあたり出荷 CV | 第1スプリント出荷 CV |
| --- | --- | --- |
| `idle`（完全放置） | 26.6% | 10.6% |
| `passive`（無介入） | 23.4% | 10.6% |
| `naive`（初見相当） | 20.0% | 11.3% |
| `skilledNoHire`（上級者相当） | 23.7% | **12.7%** |
| `onlyFirefight` | 22.0% | 9.7% |

easy では、全ランが到達する第1スプリントで見ると**介入する方針ほど分散がわずかに大きい**
（`skilledNoHire` 12.7% が最大）。1スプリントあたり出荷でも 26.6% → 20.0〜23.7% と差はごく小さい。

normal / hard では向きが逆になる。第1スプリント出荷 CV は
normal で `idle` 11.1% → `skilledNoHire` 8.9%、hard で `idle` 7.9% → `skilledNoHire` 6.3% /
`onlyFirefight` 4.9% と下がる。一方、1スプリントあたり出荷 CV は
normal `idle` 26.8% → `skilledNoHire` 28.5%、hard `idle` 20.9% → `skilledNoHire` 26.7% と**上がる**。

つまり指標と難易度によって向きが変わり、**一貫した分散抑制は観測できない**。

一方で勝率は改善している（RI-73 の表）。つまり現状の介入は**期待値を押し上げる手段**であって、
F-5 が想定する**不確実性を抑える手段**にはなっていない。

なお第1回の集計では累計出荷の CV が 57.9% → 38.6% と下がることを根拠に F-5 成立としていたが、
これは早期敗北したランほど累計出荷が機械的に小さくなるためで、
組織出力の安定性ではなくラン長の差を見ていた（Codex レビューの指摘により訂正）。

対応の方向は2つあり、どちらを取るかは設計判断である。

1. ~~F-5 の定義を実装に合わせる（介入は期待値を上げる手段と位置づけ直す）。~~ **不採用**
2. **実装を F-5 に合わせる（介入に下振れを潰す性質を持たせ、無介入の分散を大きくする）。← 採用**

受入条件（現行のものは第2回 RI-84 を参照）:

- ~~F-5 の定義と実装のどちらを動かすかを決め、SPEC 第19.1 に反映する。~~
  **決定済み（2026-07-26）。実装側を動かす。SPEC 第19.1 F-5 に記載。**
- 無介入と介入で 1スプリントあたり出荷・第1スプリント出荷の変動係数に有意な差が出る。
- 判定を `npm run playtest` の CV 出力で検証し、回帰を検知する。

## 良かった点（回帰させないため記録）

- リザルト画面（ランク・タイムライン・「なぜ燃えたか」分析）
- Game Over の組織タイプ診断（例: Senior Sacrifice 型）
- isometric 盤面の演出とイベントティッカー
- seed 付き決定論、DOM/Pixi レンダラ切替

---


# プレイテスト所見 第2回（2026-07-26）— 面白さ定義に基づく判定

[SPEC 第19.1「面白さの定義と判定基準」](../SPEC.md)を先に定義し、その F-1〜F-12 に照らして
全難易度を回した記録。実行可能な項目は[remaining-issues.md](./remaining-issues.md)へ
`RI-73`〜`RI-86` として登録した。

## 検証条件

- 対象: `main`（`8629f5f`）
- 方法: `npm run playtest`（`tests/playtest/harness.ts`）→ `npm run playtest:report`
- 母数: **4難易度 × 34方針 × seed `pt-1`〜`pt-10` = 1,360ラン**
- 本書とバックログに書いた**方針別の勝利数は `npm run playtest:check` で実測と突き合わせている**
  （`scripts/check-findings.mjs`）。再計測のたびに手で追うと必ず取りこぼすため
  （実際、方針を1つ足しただけの回で概要と詳細表がずれ、レビューで指摘された）
- 出力（`playtest-out/runs.json`）には**どのコホートを回したか**を一緒に書き出しており、
  絞り込み実行（`PT_DIFFS=easy` など）の結果は `playtest:check` が「未計測」として降りる。
  40ランを前提にした数値と10ランの出力を突き合わせると偽陽性が出るためである。
  また `npm run playtest` は**走り始める前に旧出力を消す**。途中で落ちた実行のあとに
  前回の成功結果を最新値として集計してしまうのを防ぐ
- **メタ進行は初見相当**（`PT_META=fresh`）。`game.startRun` と同じく既定解放分のみをドラフト候補にする
- 壁時計換算は実装の `MS_PER_TICK_1X`（`src/ui/sprintTempo.ts`）から読む
- 現行スナップショット（2026-08-20、generation `51b228a8dbc675a3`）は
  **4難易度 × 39方針 × seed `pt-1`〜`pt-10` = 1,560ラン**。72勝 / 1,488敗。
  F-8 / F-9 は `PT_COUNTERFACTUAL=1` かつ `PT_CF_POLICIES=naive,skilledNoHire,onlyFirefight,noInterventionCtl`
  で対象方針だけ反実仮想した同一コホートから判定する

### 統制の考え方

方針は**介入・カード・進化順・ドラフト選好・採用・目標修正**を独立に指定できる。
ある要素の寄与を見るときは、他を揃えた条件同士を比較する。

| 方針 | 内容 |
| --- | --- |
| `idle` | 介入もカードも進化も使わない完全放置 |
| `passive` | 介入・進化なし、カードのみ |
| `noInterventionCtl` | **F-5 用の統制**。`skilledNoHire` から介入だけを外し、カード・進化・採用は揃える |
| `naive` | 初見想定。異常が目立ってから反応、**約4.1秒ごとに判断**（6 tick）。進化はツリー表示順 |
| `skilled` | 上級者想定。閾値低め＋andon＋差配、**約2.0秒ごとに判断**（3 tick）、採用あり（専用フェーズ・ショップ・休息・即時採用イベントすべて）。進化はレビュー容量優先 |
| `skilledNoHire` / `skilledNoCards` / `skilledSelectiveCards` | `skilled` から採用を外した条件／さらにカードも外した条件／カードを状況選択にした条件 |
| `skilledSelectiveHire` | **採用の第3条件**。欠員（休職者あり／実働2名以下）があり、採用後も予算が残るときだけ採る |
| `skilledShopCtl` / `skilledShopBuy` | **ショップ投資の統制**。両方とも出荷加速カードだけを発動し、`skilledShopBuy` だけがショップで最良1点を購入する |
| `skilledRestRepay` / `skilledRestUpgrade` | **休息の選択肢の統制**。休息到達時に常に返済／常にカード強化。統制先は常に回復する `skilledNoHire` |
| `onlyXxx`（8種） | 単一介入だけを打ち続ける。進化順とビート選択は `skilledNoHire` と揃える |
| `aiFullBet` / `noAi` / `reviewHeavy` | AI 全配布＋AI 進化＋AI カード優先 / AI 解除＋品質進化＋品質カード優先 / レビュー寄せ編成 |
| `adj*`（6種） | 目標修正だけを固定して選ぶ（全6選択肢） |
| `probe` | 全8種の介入を 1 tick ごとに試行。**上限測定ではない**（順に発動するため先行手が後続の機会を消す） |

### 計測モデルの前提と限界

方針は人間の操作を模したものだが、**閾値や重みは実測から導いたものではなく、こちらで決めた値**である。
以下は結果に効くため、数値を読むときの前提として明記する。

| 箇所 | 決めた内容 | 影響 |
| --- | --- | --- |
| 介入の発動閾値 | `naive` は炎上2件以上／レビュー12件以上、`skilled` は炎上1件以上／レビュー6件以上／andon はレビュー10件以上 | 介入回数と勝率。閾値を変えれば方針の強さは変わる |
| カードの `selective` 条件 | 集中力6割以上、レビュー6件未満、炎上0 のときだけ発動 | RI-78 の `skilledSelectiveCards`（2/40）の位置づけ |
| ビート `stateAware` の評価式 | 出荷 ×0.05、予算・シニアHP・士気・信頼は残量が少ないほど重く（`1+(100-現在値)/50` 倍。**信頼と組織値はクランプ後の実効差分で採点**）、負債 ×-0.4、品質 ×0.3、テスト網羅 ×0.25、AIリテラシー ×0.25×希少性、レリック +4（**実際に獲得できるときだけ**。重複・枠満杯では `grantRelic` が no-op）、カード +2。**適用後に敗北条件へ入る選択肢は除外する**（予算0・シニアHP1以下・士気1以下・負債90以上）。**信頼は除外条件に含めない**（`evaluateQuarterOutcome` はボス突破かつ全KPI達成なら `met`/`exceeded` を先に返すので、信頼10以下でも勝ち得る）。代わりに踏み込むぶんを減点し、危機域（15以下）へ入るなら -6、`shutdown` 域（10以下）へ入るならさらに -25 | RI-85 の decision 由来の敗北件数、`trustExhausted` の件数 |
| 組織指標のクランプ | 加算は**クランプ後に実際に動く量**で採点する（`applyEventOutcome` は 0..100 に丸める）。士気100 で「士気+4・負債+2」を加点しないため | 上限に張り付いた指標への加点で選択が歪むのを防ぐ |
| `nextSprint` の一時効果 | タスク量倍率は高負荷と同じ式で符号を反転（消耗しているほど減少に価値）。レビュー負荷 ×-0.3、手戻り率 ×-10、集中力上限 ×0.3 | `rest-offer` の選択。休息の発生数と F-4 の所要時間 |
| 休息フェーズへの遷移 | 方針が実際に選ぶ内容（`restChoiceFor`）で採点する。`heal` はクランプ後に実際に回復するシニアHP（`REST_HEAL` + レリックの `restHealBonus`）×0.5・士気 ×0.3 に加え、個体スタミナ回復 ×0.02 と復職 +5/人、`repay` は実際に減る負債 ×0.4 に加え次スプリント手戻り率 -8% ×10、`upgrade` は一定値 +2 に次スプリント集中力上限 +2 ×0.3 | `rest-offer` の選択。休息投資の比較（RI-78）。`skilledRestRepay` は閾値を持たず休息到達時に必ず返済 |
| ベンチ配置 | レビュー適性 ≧ 実装適性なら review、それ以外は coding | 採用・復職の効果 |
| `reviewHeavy` の下限 | コーダーが0人になったら、実装適性の高いレビュー担当を1人 coding へ戻す。休職で唯一のコーダーが抜けると `foldFormationEffects()` が coding slot 0・速度0.15 まで落とすため | F-10 の `reviewHeavy` 比較 |
| 方針固有の編成の再適用 | ベンチ配置と同じタイミング（`sprint` 以外の全フェーズ）で `spec.ai` / `spec.formation` を掛け直す | 復職者の AI 配布・編成。RI-77 の AI 統制と F-10 のビルド比較 |
| 高負荷スプリントを受ける条件 | 負荷増分 `load = eliteTaskMul(difficulty) - 1`（難易度別。固定値ではない）を、`(min(シニアHP, 士気) - 45) × 0.15 - (技術的負債 / 100) × 4` に掛ける。低い方が45を上回るぶんが出荷機会、下回るぶんがリスクになる | `elite-offer` の選択。高負荷スプリントの発生数と F-4 の帯判定 |
| 選択的採用の「欠員」 | 休職者がいる、または実働が2名以下。かつ**採用費を引いた残額**が `RECRUIT_COST`（=25）以上 | `skilledSelectiveHire` の結果 |
| ショップ購入 | 出荷加速（`codingSpeedMul>1`）のカード／レリックだけを候補にし、次スプリント出荷寄与スコアが最良の1点だけ買う。購入カードは次スプリント手札へ優先配布し、導入支援として集中力上限 +2 とタスク量 ×1.1 を付与。カード発動は統制 `skilledShopCtl` と同じ `preferDelivery`。残す予算は**採用方針で変える**（採用する方針は次の採用機会のため `RECRUIT_COST`＝25、採用しない方針は四半期レビューの危機条件 `budget<=5` に対する余裕として10）。どちらも `recruit: 'skip'` | `skilledShopBuy` の結果 |
| 休息の返済方針 | `skilledRestRepay` は休息到達時に必ず `repay`（`stateAware` は他方針との互換用に残す） | `skilledRestRepay` の結果 |
| F-11 の希少性標本 | 代表4方針（`naive` / `skilledNoHire` / `aiFullBet` / `noAi`、ポイント使い切り）に固定 | RI-86 の全解放率（上限20%） |
| F-11 の方向確定標本 | `skilledStateEvolve`（`evolve: stateAware` / `maxEvolutionUnlocksPerPhase: 3`）に固定 | RI-86 の方向確定率・ブランチ多様 |
| F-4 の標本 | 代表3方針（`naive` / `skilledNoHire` / `noInterventionCtl`）に固定 | RI-75 の帯外率 |
| 進化の解放順 | 初見はツリー表示順、熟練はレビュー容量優先、AI ビルドは AI 優先、品質ビルドは品質優先。方向確定は盤面依存（`stateAware`） | ビルド差分と F-11 |
| ドラフト選好 | AI ビルドは `copilot` / `claude-code` / `devin` / `ai-guideline`、品質ビルドは `auto-test` / `pr-size-limit` / `docs` / `review-bot` / `hire-senior` を優先 | ビルド差分 |
| F-11 の主要指標 | **希少性**: 代表4方針の Q1 全解放率 ≤20%。**方向確定**: `skilledStateEvolve` で厳密過半＋ multiPhase の確定が ≥20/40、確定ブランチ ≥2 種。固定順方針のブランチ偏りは成立判定に使わない | RI-86 の成立可否 |
| F-7 の判定方針 | 初見相当は `naive`（介入は炎上2件以上／レビュー12件以上、約4.1秒ごとに判断、進化はツリー表示順） | RI-73 の成立可否 |
| `noAi` の位置づけ | **ビルド差分の方針であって AI の統制条件ではない。** `skilledNoHire` に対し AI 配布のほか andon の有無・進化ブランチ（`qualityFirst`）・ドラフト選好（`quality`）・採用（`hire`）まで違う | AI の因果は `noAiCtl`（AI だけを外す）で見る |
| 反応間隔の換算 | `stepMs` は壁時計ではなくシミュレーション時間。`stepMs/100` tick、1 tick が 1x で 680ms（`MS_PER_TICK_1X`）。`skilled`=3 tick=約2.0秒、`naive`=6 tick=約4.1秒 | F-4 の「介入が間に合うか」の読み方 |

**閾値の変更で順位が入れ替わりうる結論**（`naive` と `skilled` の優劣、カードの `selective` の効果、
`skilledSelectiveHire` の「欠員」の定義、ショップの購入順）と、
**閾値に依らない結論**（Nightmare の第1スプリント全敗、ボスの規定下限割れ、
修正前に `reviewFreeze` が judgment ビートだけで確定していたこと）を区別して読む必要がある。
後者は方針をどう定義しても変わらない（RI-85 で judgment 即死経路は解消済み）。
旧コホートで「勝利種別が実質2種」としていた整理は、現行の勝利5件（`management` 3 /
`chaos` 2）では再現できないため、閾値非依存の結論からは外す（RI-76）。

とくに `skilledSelectiveHire` の閾値は一度直している。当初は「採用後も `RECRUIT_COST` の
2倍が残る」条件にしていたが、これは採用時点で予算75を要求する一方**実測の予算は最大68**で、
一度も成立せず `skilledNoHire` と40ラン全部が同一のランになっていた（何も測っていなかった）。
到達可能な水準（採用費を引いた残額が `RECRUIT_COST` 以上）へ下げた結果、**旧コホート**では
40ラン中4ランで実際に採用が発生して経過が分かれ、そのうえで勝敗は 16/40 で
`skilledNoHire` と同じだった（無差別採用は当時11勝）。
**現行コホート**では選択的採用 `skilledSelectiveHire` が 2/40、見送り `skilledNoHire` が 2/40、
無差別採用の `skilled` が 1/40 で、
勝利数は近い範囲にあるため採用の優劣は未検証とする（「採用が有利／不利」とは言えない）。

## 結論の要約

**成立している基準と検証上の限界**:

- **F-9 のうち進行速度と決着位置は成立している**（**F-9 全体の成立ではない**）。
  現行1,560ランの全体参考値では、敗北までのスプリント数 p50 は `aiDependency` 3 /
  `moraleCollapse` 3 / `seniorBurnout` 4 / `reviewFreeze` 6 / `techDebt` 6 /
  `kpiMissed` 12 / `reorgRequired` 24。決着フェーズも、`reviewFreeze` は全59件が `sprint`、
  `kpiMissed` は全44件が `quarterReview`、`seniorBurnout` は複数フェーズに分かれる。
- **観測手段（RI-89 / RI-101）**として、その時点で機械的に**発動可能な介入**は `canApplyAction` /
  `hasActionTarget`（盤面非破壊）とハーネスの `availableActionsInDanger` で記録できる。
  有効手は同一乱数状態からの反実仮想（無介入 vs 適用可能介入）で、敗北遅延・回避・危険域離脱・
  敗因変化だけを数える。`PT_COUNTERFACTUAL=1` で last-non-empty フレームを評価する。
  F-8 の回復余地ギャップ p50 は対象方針 152 敗北で 0（PASS）。F-9 の有効手集合は
  完全評価が 0 件のため未計測（RI-132）。

**未充足・未検証の基準**:

| 基準 | 現況 | 対応 |
| --- | --- | --- |
| F-1 | 緊急対応の不利盤面・採用の壊れにくさは維持。単一介入（`onlyAndon` / `onlyFirefight` / `onlySplit` を含む）は複合を上回らない | RI-73（完了） |
| F-3 | 9戦略フェーズで `step` してもフェーズ・選択・資源が変わらず、judgment も明示操作まで解決しない | RI-102（完了） |
| F-7 | `idle` は全難易度 0/10。`naive` easy 2/10（20%）が≈20%帯 | RI-73（完了） |
| F-8 | 対象方針の回復余地ギャップ p50=0（n=152）で PASS。合否定数は p50≤1 | RI-132（完了） |
| F-9 | 進行速度と決着位置は敗因ごとに違う。有効手集合は完全評価 n≥10 の資格敗因が 0 のため未計測 | RI-89／RI-101／RI-132（ゲート実装済み） |
| F-10 | ビルド方針の modal は `chaos` / `healthy` / `normal` の3種で PASS（TVD・セキュリティ対・共通 seed 裏付けを含む） | RI-76（完了） |

充足済みの F-2 / F-3 / F-4 / F-5 / F-6 / F-11 / F-12 は、実装・受入の詳細を各課題節とGit履歴に残す。

## 課題一覧

### RI-73 難易度カーブと、常に正解／常に不正解な手がある構造（優先度: 高 / F-1・F-7）— 完了

**機構を維持したまま、easy の消耗・薄キューアンドン罰・PR分割コストを再調整してコホート受入を満たした。**
初見相当の方針（`naive`）の勝率で F-7 を判定する。全方針の平均は使わない。
判定はメタ解放も初見相当（`PT_META=fresh`）に限る。

| 難易度 | **`naive`（初見相当・判定に使う）** | 目標との差 |
| --- | --- | --- |
| easy | **2/10（20%）** | ≈20% 帯 |
| normal | **0/10（0%）** | 導入帯は easy 側で確保。厳しめ |
| hard | **0/10（0%）** | 導入難易度外 |
| nightmare | **0/10（0%）** | 同上 |

`idle` は既定フルコホート（4難易度×10seed）で **全難易度 0/10**。F-7 の「放置で勝ち越し無し」と
「初見相当が概ね20%前後」はともに充足。

**F-1（2026-08-13の既定フルコホート 1,560ラン / `PT_META=fresh` の該当方針）:**

| 方針 | easy | normal | hard | nightmare |
| --- | --- | --- | --- | --- |
| `idle` | 0/10 | 0/10 | 0/10 | 0/10 |
| `naive` | 2/10 | 0/10 | 0/10 | 0/10 |
| `onlyFirefight` | 0/10 | 1/10 | 0/10 | 0/10 |
| `onlyAndon` | 0/10 | 1/10 | 0/10 | 0/10 |
| `onlySplit` | 1/10 | 0/10 | 0/10 | 0/10 |
| `skilledNoHire` | **1/10** | **1/10** | 0/10 | 0/10 |
| `skilled` | 1/10 | 0/10 | 0/10 | 0/10 |
| `probe` | 0/10 | 0/10 | 0/10 | 0/10 |

- **単一介入は複合を上回らない**（`probe` は全難易度 0。`onlyAndon` / `onlyFirefight` / `onlySplit` は
  各難易度で `skilledNoHire` を上回らない）。
- **緊急対応の不利盤面**: 単発先消しは HP11・士気-5・コンボ切断。緊急（複数炎上／猶予≤15）だけ安く安定付与。
- **採用の壊れにくさ（easy）**: `skilled` は `seniorBurnout` 6/10・最終シニアHP平均13.9、`skilledNoHire` は 9/10・4.2。勝率は予算代償で下がりうる。
- 調整: easy `seniorHpCostMul` 0.68→0.76、薄キュー andon 士気+12 / HP14、`ANDON_TICKS` 16→12、
  `splitPr` 士気4 / シニアHP4・運用安定なし。

### RI-74 Nightmare は AI 依存を意識しない方針で第1スプリント敗北が確定する（優先度: 高 / F-8）

nightmare は **33方針330ランすべて敗北**し、うち多数が `aiDependency`。
敗北条件は `org.aiDependency >= 95 && org.aiLiteracy <= 30`（`src/sim/outcome.ts:63-65`）で、
nightmare の初期値 `aiDependencyBase`=55 / `aiLiteracy`=25 はリテラシー側を最初から満たしている。

**回避経路は2種類ある**。依存度を下げる経路と、**リテラシーを上げて敗北条件そのものを外す経路**である。
とくに `onlyPair`（ペアレビュー連打）は**依存度100のまま**リテラシーを 25→62 へ上げて回避する。
`pairReview` の説明は「詰まったPRをペアで処理。AI Literacy が上がる」で、
SPEC 第19章の「AI は強い。しかし雑に使うと壊れる」に最も合致する対処である。

問題は回避不能なことではなく、**回避に必要な方針が事前知識を要し、外すと第1スプリントで終わる**こと。
初手で終わるため「負けて学ぶ」が成立しない。リテラシー経路はチュートリアルにもリザルトにも現れない。

### RI-75 スプリントが規定帯をほぼ全面的に下回る（優先度: 高 / F-4・第3.1） — 完了

難易度別タスク量・タスク床・elite 倍率・`MS_PER_TICK_1X=780`・ボス `maxTicks`・
早期ドレイン時の `minCompleteTick` で再調整し、F-4 代表3方針で帯内を確認した。

| 難易度 | 種別 | n | p50 | p50 帯判定 | 下回り | 上回り | 絶対下限30秒未満 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| easy | 通常 | 209 | 61.6秒 | **帯内** | 45.9% | 1% | **0%** |
| easy | 高負荷 | 13 | 80.3秒 | **帯内** | 15.4% | 7.7% | 0% |
| easy | ボス | 37 | 90.5秒 | **帯内** | 0% | 0% | — |
| normal | 通常 | 178 | 67.9秒 | **帯内** | 37.6% | 1.7% | **0%** |
| normal | 高負荷 | 13 | 71.8秒 | **帯内** | 23.1% | 23.1% | 0% |
| normal | ボス | 26 | 90.5秒 | **帯内** | 0% | 0% | — |
| hard | 通常 | 109 | 78.0秒 | **帯内** | 29.4% | 11.9% | **0%** |
| hard | 高負荷 | 8 | 92.8秒 | **帯内** | 12.5% | 25% | 0% |
| hard | ボス | 7 | 105.3秒 | **帯内** | 0% | 0% | — |
| nightmare | 通常 | 93 | 78.8秒 | **帯内** | 23.7% | 12.9% | **0%** |
| nightmare | 高負荷 | 6 | 97.5秒 | **帯内** | 0% | 33.3% | 0% |
| nightmare | ボス | 4 | 118.6秒 | **帯内** | 0% | 0% | — |

回帰は `tests/unit/ui/sprintTempo.test.ts` の F-4 ハーネス検証（代表3方針 × `pt-1..10`）で固定。

### RI-76 勝利種別が実質2種で、「重アクションを使ったか」でしか分岐しない（優先度: 高 / F-10）— 完了

現行コホートは **1,560ラン中72勝（敗北1,488）**で、勝利種別の内訳は
`happiness` 8 / `chaos` 29 / `healthy` 4 / `management` 18 / `normal` 13。`aiSuccess` /
`noDamage` は 0件だった。
F-10 受入はビルド方針（`aiFullBet` / `harnessBloated` / `harnessOptimized` / `noAi` /
`reviewHeavy` / `skilledNoHire` / `securityNeglect` / `securityFocus`）だけで判定し、一致≥2 / 混在首位≥3・share≥2/3・同率除外・
異 modal 方針間 TVD≥0.5・試練プロファイル込みの共通 seed 分岐（双方が自身の modal を
再現した組だけ）・既定コホートを要求する。
F-10 modal は `chaos` / `healthy` / `normal` の3種で PASS（採用方針
`aiFullBet=normal` / `securityNeglect=chaos` / `securityFocus=healthy`、
異 modal TVD≥0.5、セキュリティ対 TVD=1.00、共通 seed 裏付けあり）。
`harness*` は標本不足のまま frontier 試練を通常条件と混ぜない。`noDamage` の量産もない。

勝利があった方針だけを列挙する。

| 方針 | 勝利種別 |
| --- | --- |
| `adjCutScope` | `chaos` 1 / `management` 1 |
| `adjExtendDeadline` | `chaos` 1 |
| `adjPauseAiRollout` | `happiness` 2 / `chaos` 1 / `management` 1 / `normal` 2 |
| `adjQualityPivot` | `chaos` 2 / `management` 3 / `normal` 2 |
| `adjReorgTeams` | `chaos` 2 / `normal` 1 |
| `adjRequestBudget` | `normal` 1 |
| `adjStakeholderCare` | `normal` 1 |
| `aiFullBet` | `chaos` 1 / `normal` 2 |
| `harnessBloated` | `happiness` 1 |
| `harnessOptimized` | `happiness` 1 |
| `naive` | `management` 2 |
| `naiveNoInterventionCtl` | `normal` 1 |
| `noAi` | `happiness` 1 |
| `noAiCtl` | `management` 1 |
| `noInterventionCtl` | `happiness` 1 / `chaos` 2 |
| `onlyAndon` | `chaos` 1 |
| `onlyAssign` | `normal` 1 |
| `onlyFirefight` | `chaos` 1 |
| `onlyInterrupt` | `chaos` 1 |
| `onlyPair` | `chaos` 1 / `management` 1 |
| `onlySplit` | `management` 1 |
| `onlyThrottle` | `chaos` 1 / `management` 1 |
| `passive` | `chaos` 1 |
| `reviewHeavy` | `healthy` 1 |
| `securityFocus` | `healthy` 3 |
| `securityNeglect` | `chaos` 2 |
| `skilled` | `normal` 1 |
| `skilledNoCards` | `chaos` 2 / `management` 1 |
| `skilledNoHire` | `chaos` 1 / `management` 1 |
| `skilledRestRepay` | `happiness` 1 / `chaos` 1 |
| `skilledRestUpgrade` | `happiness` 1 / `chaos` 1 |
| `skilledSelectiveCards` | `chaos` 1 / `management` 1 |
| `skilledSelectiveHire` | `chaos` 1 / `normal` 1 |
| `skilledShopBuy` | `chaos` 1 / `management` 1 |
| `skilledShopCtl` | `chaos` 2 / `management` 1 |
| `skilledStateEvolve` | `chaos` 1 / `management` 2 |

`evaluateWinType`（`src/sim/outcome.ts`）の優先順位は次のとおり。

1. **`noDamage`**: 重介入なし・延焼0に加え、品質≥70・士気≥70・シニアHP≥60・手戻り率0.15未満・
   健全系診断（`healthyAcceleration` / `documentationKingdom`）をすべて満たすとき
2. **`healthy`（セキュリティ重視）**: セキュリティ≥85・品質≥65・士気≥65・AI利用率≥0.55・手戻り率0.22未満
3. **`chaos`（セキュリティ軽視）**: インシデント≥16・累計出荷≥180・セキュリティ<50
4. **`aiSuccess`**: AI 利用率≥0.55・Literacy≥40・手戻り率0.22未満・セキュリティ≥50・
   レビューピーク<16。`reviewHell` / `aiOverproduction` / `reworkSpiral` も除外する
5. **`healthy`（documentationKingdom）**: 診断が `documentationKingdom` かつ品質・士気・手戻り
6. **`happiness`**: 士気≥70 かつシニアHP≥45
7. **`chaos`**: インシデント≥20 かつ累計出荷≥250（高セキュリティで上の健全を外した残差）
8. **`healthy`**: 品質・士気・手戻り、かつレビューピーク<16
9. **`management`**: 予算≥50（他シグネチャの残差）
10. それ以外は **`normal`**

したがって `noDamage` と `healthy` は、残業・アンドンの有無「だけ」で入れ替わる構造ではない。
この総数・種別内訳・方針別表は、旧コホートの数値を混ぜず、同じ
`playtest-out/runs.json` から生成した現行集計に揃えている。

組織診断のビルド差の例:

| 方針 | 組織診断 |
| --- | --- |
| `aiFullBet` | `reviewHell` 3 / `seniorSacrifice` 37 |
| `noAi` | `reviewHell` 4 / `seniorSacrifice` 32 / `documentationKingdom` 1 / `healthyAcceleration` 3 |

### RI-77 AI 導入が既定 ON で、既定のまま進むのが有利（優先度: 高 / F-1・F-2・F-10）— 完了

機構を入れた（開始時は `starter-ai-junior` のみ AI、ビート後は必ず `setup`、
`aiDeliveryValueMul` とインフラ単価調整で既定部分配布の S1 出荷を正方向へ）。
代表 seed のコア因果（Review/Rework 増）は `aiAdoptionSeeds` で維持。
既定フルコホート再計測で勝率帯と `skilledNoHire` vs `noAiCtl` のセル比較を確定した。

#### 再計測後の統制比較（現行正本）

比較は **`noAiCtl`**（`skilledNoHire` から AI 配布だけを外す）。出荷は同一 seed・同一スプリント番号の共通到達分のみ。

| 難易度 | S1 | S2 | S3 |
| --- | --- | --- | --- |
| easy | **793.5** → 677.7 | **819.9** → 683.6 | **905.8** → 732.8 |
| normal | **643.8** → 585.3 | **629.1** → 567.0 | **737.0** → 615.6 |
| hard | **498.5** → 450.1 | **507.8** → 477.3 | **539.1** → 483.9 |
| nightmare | **344.6** → 315.2 | **369.3** → 335.9 | **393.4** → 356.9 |

（`skilledNoHire` → `noAiCtl`。各セル共通 n=8〜10）

比較できた12セルすべてで既定部分配布側の出荷が多く、実装前の「AI を切ると出荷が増える」逆転は解消した。
勝率は **2/40 → 1/40**、AI 利用率平均 41.8% → 0%、最終 AI 依存度平均 94.4 → 36.7。
敗因は両方針ともほぼ `seniorBurnout`（`skilledNoHire` 38 / `noAiCtl` 39）で、`kpiMissed` は乗らない。
平均プレイスプリントは 7.5 → 13.8 と、AI なし側が長く粘って負ける形は残る。

以下は**実装前コホートの記録**（再計測前の比較根拠。数値はこの時点のスナップショット）。

#### 実装前コホートでの観測

初期ロスターは `aiAssigned: arch.preferred === 'coding'`（`src/sim/member/roster.ts:135`）で、
コーダー志向のメンバーには最初から AI が配られている。既定プレイの AI 利用率は 72〜86%、
最終 AI 依存度は 100 に張り付く。

選択の入口は存在する（`SetupScreen`「編成 — スプリント開始前に配置とAIを決める」、
`FormationScreen.tsx:172` の「🤖 AI配布中」ボタン）。課題は次の3点であった。

1. **既定が ON で、選び直す動機が薄い**。実装前コホートでは全面ベットと熟練見送りの勝率差が小さく、
   既定のまま進むことと全面ベットの差は観測しにくかった（数値は訂正履歴と当時スナップショットを参照）。
2. **解除の勝率差は当時のコホートでは単純な逆転になっていない**。AI 配布だけを外した統制条件でも
   比較対象の熟練見送りを下回り、品質寄りの noAi ビルドとも大きな差は出なかった。
   「解除側の代償が一方的に大きい」という以前の結論は、当時のコホートでは成立しない。

   > **代償の中身についての当初の説明は取り下げる。** 以前ここには「noAi ビルドは
   > `budgetExhausted` 敗北の過半を占める。AI で稼げないぶん予算を維持できず、
   > AI 導入が実質必須」と書いていたが、**`budgetExhausted` は当時1,360ラン中0件**である。
   > 当時の14件はすべて回避可能な自滅で、内訳は採用で予算を使い切った6件（訂正履歴86）と、
   > ビルド比較3方針だけがビート選択を統制されておらず確定敗北を自分から選んだ8件（同88）だった。
   > **予算枯渇は AI を外した代償ではなく、ハーネスの統制不足が作っていた**。
3. **段階が無い**。導入は編成画面の on/off だけで、四半期を通じて問い直す機会が無い。

#### AI の因果は効いているが、効き方は「出荷が増える」ではない（実装前）

比較には **`noAiCtl`**（`skilledNoHire` から `setMemberAi` だけを false にした統制条件）を使う。
ビルド差分の `noAi` は AI に加えて andon の有無・進化ブランチ・ドラフト選好・採用まで
同時に違うため、差を AI へ帰属できない。

出荷は**同一 seed の同一スプリント番号**で対応を取る（方針で到達スプリント数が違うので、
到達スプリントを平均するだけでは生存者の構成差が出荷差に化ける）:

| 難易度 | S1 | S2 | S3 |
| --- | --- | --- | --- |
| easy | 278.3 → **307.1** | 307.3 → **357.1** | 349.9 → 347.2 |
| normal | 277.5 → **353.2** | 291.5 → **378.8** | 395.7 → **428.8** |
| hard | 265.9 → **334.3** | 311.4 → **394.0** | 421.4 → **451.3** |
| nightmare | 269.7 → **304.6** | 共通到達なし | 共通到達なし |

（`skilledNoHire` → `noAiCtl`。各セル共通 n=8〜10）

**AI を切ると1スプリントの出荷はむしろ増える**。比較できた10セル中9セルで上回り、
残る1セル（easy S3）もほぼ同等で、明確に下回るセルは無い。にもかかわらず勝率は 16→10 に下がる。
つまり実装上、AI の寄与は出荷ではない（これが RI-77 の是正対象だった）。

敗因の構成は入れ替わる。`skilledNoHire` の `aiDependency` 10件が消える代わりに、
`seniorBurnout` 5→11・`reviewFreeze` 9→11 が増え、`trustExhausted` 3件・`reorgRequired` 4件・
`techDebt` 1件が新たに出る。敗北ランの長さも 5.0→14.1スプリントへ伸び、
**長く粘って結局負ける**形になる。
Nightmare では AI を切ると第1スプリント即死（RI-74）を回避でき、平均 1.0→6.6スプリントまで
延びるが、勝利には至らない。

一方で四半期目標の未達回数は AI なしの方が多い（1四半期あたり 0.60→1.10、
達成〈met+exceeded〉は 37%→14%）。**1スプリントの出荷が多いのに四半期目標の達成率は下がる**という
食い違いがあり、原因は特定できていない（RI-68 の Delivery KPI のスケール不整合が絡む可能性がある）。

> **この統制条件にも残差がある。** `spec.ai` が変えるのはロスターの `aiAssigned` だけで、
> そこから決まるのは intake の AI 採用率（`aiAdoptionShare`）である。`org.aiEnabled` は
> true のままなので、`assignTask` 介入が `defaultAssignee` 経由で個別タスクを AI へ回す
> 経路が残る（実測で492スプリント中5件、最大4%）。**AI 配布ゼロ＝AI 利用ゼロではない**。
> 残差は小さく上の結論を動かさないが、「AI を完全に断つ」条件を測るには
> `org.aiEnabled` 側も落とす別の統制が要る。

> **以前の記述は誤りだった。** 「AI 配布を切ると出荷 334.5→291.4」を因果の根拠にしていたが、
> これは統制条件ではない `noAi` の値で、しかも向きが逆である。
> 「AI で出荷が増える」は実務感覚とも一致するぶん、**実装がそうなっていないことが問題**であった。

開始時 AI の全面撤廃ではなく、既定値の誘導・解除側の代償・導入の段階化に加えて、
**AI が何を良くして何を悪くするのかを出荷の増減として体感できるようにすること**を対象にした。

### RI-78 スプリント間投資のうち、ショップと休息の選択が結果を変えない（優先度: 高 / F-2）— 完了

カードと介入の集中力共有は維持し、`CardDef.focusCost`（2〜4）をショップ価格 `cost` から分離した。休息は返済／強化へ次スプリント効果を付与済み。ショップ純出荷の未達分は、購入方針・定価・次スプ導入支援（手札優先・`focusMaxAdd +2`・`taskCountMul 1.1`）と、カード発動を揃えた統制 `skilledShopCtl` で閉じた。

| 比較 | 共通機会 / 共通ラン | 次スプリントの結果 |
| --- | ---: | --- |
| `skilledShopBuy` vs `skilledShopCtl`（実購入） | 14（完走ペア14） | 純出荷改善 **13/14（92.9%）**、純出荷平均 821.1 vs 746.4、粗出荷 +75.8 |
| `skilledShopBuy` vs `skilledShopCtl`（補助KPI） | 14 | Rework 0.8 vs 1.4、Reviewピーク 18.8 vs 15.9、HP 18.7 vs 27.1 |

休息・カード層の既存結論（返済／強化のKPI差、selective vs none の勝利分岐）は維持。ショップ比較の対照は `skilledNoHire` ではなく `skilledShopCtl`（`preferDelivery`）を使う。

### RI-79 敗因ラベルが3種類の原因を1つに潰す（優先度: 中 / F-6・F-8）

`trustExhausted` は四半期 outcome の `missed_crisis` と `shutdown` の**両方**から作られる
（`loseReasonForOutcome`、`src/sim/run/quarterReview.ts:464`）。発火条件を分解すると:

| outcome | 発火条件 | 件数 |
| --- | --- | --- |
| `missed_crisis` | `trust<=15` | 21 |
| `missed_crisis` | `budget<=5` | **1** |
| `shutdown` | `seniorHp<=5` かつ `missedCount>=2` | **4** |
| `shutdown` | `trust<=10` | 1 |

`trustExhausted` 28件のうち信頼由来は23件（`trust<=15` 21・`trust<=15`と`budget<=5`の同時成立1・`trust<=10` 1）で、残り5件は**予算切れ（1件）とシニア枯渇（4件）**。
予算由来が6件から1件へ減ったのは、ビート評価に予算を入れて即敗北する選択肢を外したため
（回避可能な自滅が減った）。
予算やシニアが原因のランに信頼回復を促しても解決しない。

この内訳は判定時と同じ入力から復元している。`companyOrgFromTeams` は `morale` と `seniorHp`
だけ平均を取らず選択中チームの値をそのまま使い、`trust` と `budget` はラン単位でチーム別の値を
持たないため、shutdown の3条件はすべて再現できる（実測でも 5/5 件を分類できており未分類は無い）。

また `trustExhausted` / `reorgRequired` は**敗北を確定させた処理の直前**に
シニアHP 33.8〜36.4・士気 98.0〜100 のまま終わる。盤面上は比較的健全に見えるのに終了するため、
**予兆が無く詰みが確定する**点で F-8 を満たさない。敗北画面の「次の一手」提示（F-6）は
RI-82 で実装済みであり、本節の残課題はラベル潰しと予兆（F-8）側である。

**この「健全に見えたまま終わる」性質は、直前状態の取り方を直したことで対象が広がった。**
以前は最後に完了したスプリントの終了時点を直前状態として読んでいたが、そこから敗北までには
ビート・ショップ・休息・setup が挟まる。実時点の控え（`lostPrevState`）へ替えたところ、
`seniorBurnout` の直前シニアHP は 12.6 → **19.9**、`techDebt` は 11.8 → **52.4** へ上がった。
つまり**敗因ラベルが指す資源が枯れて見える局面は、旧計測が作っていた見かけ**であり、
実際にはシニア枯渇による敗北ですら直前は HP 19.9 ある。209件中168件が
`senior-burnout(judgment)` など選択不能なビートで一撃で決まっており、
「じわじわ減って詰む」のではなく「健全な値から1回の判定で落ちる」形である。
予兆の乏しさは RI-79 が挙げた3敗因に限らない。

**信頼を直前状態へ入れたところ、`trustExhausted` の予兆の無さがさらに具体的になった。**
28件の直前の最小信頼の分布は **15が20件**・14が2件・7が1件・20が1件・45以上が4件である。
`missed_crisis` の発火条件はちょうど `trust<=15` なので、**過半数が閾値そのものの値で終わっている**。
信頼はじわじわ削れて危険域へ入るのではなく、**イベントの離散的な減少で一度に閾値へ着地し、
次の四半期レビューで確定する**。観察できる「近づいていく過程」が存在しないため、
警告 UI を足すだけでは予兆にならない（閾値に触れた瞬間には既に確定している）。
なお45以上の4件は信頼由来ではない敗北（シニア枯渇・KPI未達）で、
`trustExhausted` ラベルが3種類の原因を潰している問題（上記）と整合する。

一方で、直前の最小信頼は**敗因ごとに明確に分かれる**（`reorgRequired` 19.1 / `trustExhausted` 19.8 /
`techDebt` 33.7 / `reviewFreeze` 42.0 / `aiDependency` 45.0 /
`moraleCollapse` 47.7 / `seniorBurnout` 47.8）。F-9 の「敗因ごとに状態推移が異なる」に対する
根拠が1軸増えたことになる。

### RI-80 評価が無介入と熟練を区別しない（優先度: 中 / 第4.6）— 完了

> **これは F-6 の根拠ではない。** F-6 は敗北画面・リザルト・組織診断から
> 次の一手と現場への示唆が読み取れるかを扱い、スプリント評価の中央値とは別である。
> F-6 の判定は RI-82 で行う。RI-80 は操作の巧拙が第4.6の評価へ反映されるかだけを見る。

`computeGrade` は健全比のS境界を 95.5% へ引き上げ、成立した安定化系介入へ
1回0.45ポイント・最大1.5ポイントの運用判断ボーナスを加える。
残業号令は加点せず、手戻り・障害・延焼・シニア消耗の実績ペナルティは従来どおり残す。

重点統制コホート（4難易度 × 2方針 × 10seed）の463スプリントでは、
**S 186（40.2%）/ A 189（40.8%）/ B 44（9.5%）/ C 26（5.6%）/
D 18（3.9%）**。特定ランクへの50%以上の偏りはない。

比較は `skilledNoHire` と条件を揃えた `noInterventionCtl` を使い、両方が到達した
共通seedだけで行う。12条件のうち8条件で熟練の中央値が高く、4条件は同値、逆転は無かった。
共通99スプリントをまとめた中央値も無介入Bに対して熟練Aとなり、受入条件を満たした。

境界・ボーナスの回帰は `tests/unit/sim/sprint.test.ts`、再計測は
`npm run playtest` の統制方針で確認した。

### RI-81 ドラフトのマリガンが無い（優先度: 中 / F-12）

`DraftScreen` の選択肢は「3枚から1枚選ぶ」か「スキップ」のみで、コストを払って引き直す手段が無い。
カードプールは9枚（`src/data/cards.ts`）で、うち4枚はメタ解放が必要なため初見の初期プールは5枚。
カード条件は `skilledSelectiveCards` と `skilledNoCards` の共通40ランで勝利2 vs 3となり、
発動カード平均も差が計測できる。
プールとドラフトの構造上はどの3枚を引くかに意味がある。
マリガンはその「引きの事故」を緩和する手段として、単独で導入する価値がある。

### RI-82 敗北画面に「次の一手」が無い（優先度: 中 / F-6）— 完了

**対応済み。** `loseNextActionView` が全 `LoseReason` に次の一手と現場示唆を返し、
`RunResultScreen` が敗北時に常時表示する。失敗図鑑の診断由来教訓は役割分担のまま残す。

### RI-83 目標修正の選択が結果をほとんど変えない（優先度: 中 / F-2 第4層）— 完了

**対応済み。** 各選択肢に次四半期物理キャリーオーバー（`nextQuarterEffects`）を追加し、
RunBar で持ち越しを追跡できるようにした。再計測は adj* 7方針×各40ラン、共通コホート6組。

| 選択 | n | **初回修正の次四半期まで生存** | 参考: ラン全体の勝率 | 参考: 総スプリント 平均 |
| --- | --- | --- | --- | --- |
| `cut_scope`（スコープ削減） | 6 | 0% | 0% | 8.3 |
| `request_budget`（予算要求） | 6 | 0% | 0% | 8.2 |
| `stakeholder_care`（ステークホルダーケア） | 6 | 0% | 0% | 8.2 |
| `pause_ai_rollout`（AI 展開の一時停止） | 6 | 16.7% | 0% | 11.7 |
| `extend_deadline`（期限延長） | 6 | 33.3% | 0% | 11.3 |
| `reorg_teams`（組織再編） | 6 | 33.3% | 0% | 13.0 |
| `quality_pivot`（品質ピボット） | 6 | **50%** | 50% | 11.0 |

生存率は 0〜50%（最大差50pt）で高/中/低の3帯に分かれる。コホート内の敗因は
Tech Debt / シニア燃え尽きが中心で、負債削減・シニア回復を持つ選択が有利、
出荷焦点や信頼ケアだけでは不利（F-1）。

### RI-84 条件を揃えると介入の寄与がほぼ消える（優先度: 高 / F-5） — 完了

F-5 の定義（介入は期待値底上げではなく下振れ抑制）へ実装を寄せた（方針決定 2026-07-26）。
安全側の介入（残業号令を除く）に時限の「運用安定」（`STABILITY_TICKS=180`）を付与し、安定中は
Review 手戻り率を `STABILITY_REWORK_MUL=0.4` へ抑え、燃え尽き時の延焼を封じ、高価値コンボの
上振れを抑える。ActionBar に運用安定表示、効果タグで持続と倍率を出す。

回帰は `tests/unit/sim/actions.test.ts` / `combo.test.ts` / `eventOutcomeView.test.ts` と
E2E（`tests/e2e/interventions.spec.ts`）で固定。`playtest:report` の F-5 CV 比較も維持する。

実装前コホートでは `noInterventionCtl` / `naive` / `skilledNoHire` がいずれも低勝率で、
勝率でも第1スプリント出荷 CV でも介入の寄与が観測できなかった（経緯は第1回の撤回節と
訂正履歴を参照）。現行コホートでは `skilledNoHire` 2/40。コホート全体の勝率差の再計測は
RI-73（F-1 受入）後の既定フルコホートで行った。

### RI-85 レビュー凍結は選択不能な判定イベントでしか確定しない（優先度: 高 / F-4） — 完了

`review-freeze` の即死 `forceLose` をやめ soft judgment（消耗中心。閾値直前の即敗北を避けるため
`reviewLoadAdd` は付けない）へ変更し、スプリント中の HUD に凍結予兆チップ（`reviewFreezeHudCopy`）を
追加した。予兆のピーク入力は通算ではなく進行中スプリントのピーク／現在キューを使う。

再計測（1,560ラン、敗北 1,488）では `reviewFreeze`（59件）はすべて `sprint` で決着し、
即死イベント経路は消えた。ピーク経路（`REVIEW_FREEZE_PEAK`）とスプリント中の対処へ委ねる。

| 敗因 | 決着フェーズ |
| --- | --- |
| **`reviewFreeze`**（59件） | `sprint` **100%** |
| `seniorBurnout`（1245件） | beat / sprint / quarterReview が混在 |
| `kpiMissed`（43件） | `quarterReview` 100% |
| `budgetExhausted`（34件） | setup 33件 / beat 1件 |
| `techDebt`（69件） / `moraleCollapse`（13件） | `sprint` が大半 |
| `aiDependency`（24件） | sprint 19件 / setup 5件 |
| `reorgRequired`（1件） | `quarterReview` 100% |

回帰は `tests/unit/scenarios/reviewFreeze.test.ts` と E2E（凍結チップ + decision UI）で固定。

### RI-86 Q1 で進化ツリーを取り切れてしまい、ビルドの方向という概念が成立しない（優先度: 中 / F-11）— 完了

ノードコスト再配分（総コスト **26→103**、入手式据え置き）で希少性を入れ、
盤面依存の `skilledStateEvolve` と複数スプリント継続の方向確定指標を追加した。
選んだブランチは同一フェーズで2ノードまで深化し、先端は基礎スコアに依らず候補外化して
次スプリントの tip sticky で触り multiPhase を作る。
方向確定方針は `maxEvolutionUnlocksPerPhase: 3`（余り持ち越し）を**方針契約**とし、
希少性は代表4方針のポイント使い切り契約で別計測する（report で分離記載）。
全解放率の定量上限は **20%**（`playtest-report` の総合受入に含む）。

**構造的事実**（`src/data/evolution.ts` / `RunEngine.evoPointsFor`）:

| 指標 | 値 |
| --- | --- |
| 進化ツリー | **15ノード / 総コスト 103** |
| 1スプリントの入手ポイント | `1 + floor(出荷/40)`（高負荷は +1） |

**受入条件（固定）**:

- 代表4方針（`naive` / `skilledNoHire` / `aiFullBet` / `noAi`、n=160）で Q1 の全解放率が **20%以下**であること。
- `skilledStateEvolve`（n=40、`maxEvolutionUnlocksPerPhase: 3`）で、Q1 の方向確定が **20/40（50%）以上**であること。
- 方向確定したランのブランチが **2種類以上**に分かれること。

**実測**（代表4方針 `naive` / `skilledNoHire` / `aiFullBet` / `noAi`、n=160）:

| 指標 | 実測 |
| --- | --- |
| Q1 で入手する総ポイント p10 / p50 / p90（**全 n=160**） | 17 / **61** / 116 |
| Q1 解放数 p10 / p50 / p90（解放ありのラン n=160） | 5 / **11** / 15 |
| Q1 中にツリーを取り切ったラン | **32/160（20%）** |

**方向確定**（`skilledStateEvolve`、`evolve: stateAware` / `maxEvolutionUnlocksPerPhase: 3`、n=40）:
Q1 で方向確定 **28/40（70%）**、確定ブランチは `ai` 21 / `review` 6 / `quality` 1
（分散 12 / Q1解放なし 0）。確定スプリント p50=2。
希少性上限・方向確定率・ブランチ多様を満たすため、RI-86 / F-11 は完了とする。
同一進化フェーズの連続取得だけでは確定とせず、複数スプリントにまたがる継続選択を要求する。
固定順方針のブランチ偏りは成立判定に使わない。

### RI-132 F-8 / F-9 既定コホートの合否再走 — 完了

合否定数（`scripts/playtest-f8f9.mjs`）:

- 対象方針: `naive` / `skilledNoHire` / `onlyFirefight` / `noInterventionCtl`
- F-8: 回復余地ギャップの **p50 ≤ 1**（有効手なしは Inf）
- F-9: 難易度横断の完全評価で敗因 n≥10 かつ **distinctEffectiveSetCount ≥ 2**

既定コホート（generation `51b228a8dbc675a3`、1,560ラン）を `PT_COUNTERFACTUAL=1` で再走した。
反実仮想は対象4方針に限り、他方針は勝敗集計のみ。値調整はしていない。

F-8 受入（RI-132）: 対象方針 naive / skilledNoHire / onlyFirefight / noInterventionCtl / p50≤1（実測 p50=0 n=152） → PASS

F-9 は 152 件すべてが分岐上限（action 96 / combo 32 / strategic 192）で不完全評価のため、
完全評価の資格敗因が 0 種で未計測。不完全評価の集合は参考値として、`seniorBurnout`（138）が
レバー・setup 中心、`techDebt`（9）が andon / dependencyCleanup、`reviewFreeze`（4）が
interruptReview / pairReview、`aiDependency`（1）が pairReview / aiThrottle と分かれる。
ゲーム側の立て直し余地追加は対象外。

## 良かった点（回帰させないため記録）

- **敗因ごとに進行速度と決着位置が違う**（F-9 のうち速度・位置の部分）。
  現行コホート全体の敗北までのスプリント数 p50 は `aiDependency` 3 / `seniorBurnout` 4 /
  `reviewFreeze` 6 / `techDebt` 6 / `kpiMissed` 12 / `reorgRequired` 24。`reviewFreeze` は全件 sprint、
  `kpiMissed` と `reorgRequired` は全件 quarterReview、`seniorBurnout` は複数フェーズに分かれる。
  機械的な「打てた手」は RI-89 で観測でき、有効な手は RI-101 の反実仮想で判定できる。
  F-8 の回復余地ギャップは既定コホートで p50=0（PASS）。F-9 の有効手集合差は完全評価不足で未計測（RI-132）。
- **ビルドの違いは組織診断と勝利種別の両方に出ている**。`noAi` は
  `healthyAcceleration` 3 と、`aiFullBet`（`reviewHell` 3 / `seniorSacrifice` 37）から
  明確に分かれる。F-10 ビルド modal は `chaos` / `healthy` / `normal` の3種で PASS（RI-76 完了）。
- **AI の on/off は状態へ正しく伝播し、出荷も正方向**。AI 配布を切ると AI 利用率 0%、
  最終 AI 依存度が 94.4→36.7（`noAiCtl`）になり、共通到達セルの出荷は既定部分配布側が多い（RI-77 完了）。
- **カード条件は選択発動2 vs 無カード3**で、共通機会の次スプリントKPIと発動数も記録できる（RI-78完了）。
- seed 固定の決定論が完全に効いており、`npm run playtest` は同じ入力に対して常に同じ結果を返す。

## 計測方法の訂正（経緯）

自動レビューの指摘により、以下の測定上の誤りを順次修正した。

> **本節に書かれた数値は「その修正を入れた直後に観測した値」であり、現在値ではない。**
> 各修正の効果（どれだけ動いたか、向きは何か）を残すための記録なので、後続の修正で
> 母集団や方針構成が変わっても遡って書き換えない。**現在値は本節より上の各節を参照すること。**
> 例えば 29 の `skilledRestRepay` や 34 の Q1 入手 p50 は、その後の修正で値が動いている。

1. **解放コンテンツが実プレイと違った** — `setUnlockedContent` 相当を適用し初見相当へ揃えた。
2. **進化ポイントを使い切っていなかった** — 「介入は投入するほど勝率が下がる」という誤結論の原因。
3. **介入の失敗理由を測っていなかった** — 「介入すべき状況が盤面に現れない」という誤結論の原因。
4. **採用方針が専用フェーズにしか効いていなかった** — ショップ・休息・即時採用イベントにも適用。
5. **初見と熟練で進化順が同じだった** — 初見はツリー表示順、熟練はレビュー容量優先に分離。
6. **単一介入群の進化順が比較対象と違った** — 揃えて F-1 の判定を介入構成に帰属できるようにした。
7. **ビルド方針で進化ブランチとドラフトが分岐していなかった** — AI／品質へ分岐させた。
8. **無介入群の進化・カード・採用が比較対象と違った** — `noInterventionCtl` を追加。これにより
   F-5 の判定が「介入は勝率を改善している」から「寄与がほぼ無い」へ変わった（RI-84）。
9. **分散比較にラン長が混入していた** — 同一スプリント番号ごとの CV へ変更。
10. **目標修正の比較に非統制ランが混入し、対象も3種類だけだった** — 全6種の統制方針へ。
11. **敗北直前の状態を敗北フェーズで区別していなかった** — 区別した結果 RI-85 が判明した。
12. **採用したメンバーをベンチに残していた** — `recruitMember` は採用直後を必ず `bench` に置くが、
    ハーネスが再配置していなかったため、採用が予算を払うだけになっていた。能力に応じて
    review / coding へ配置した結果、採用が勝率を下げる傾向は**変わらなかった**（10→9 と 16→17）。
13. **ビートの選択肢を常に先頭で取っていた** — `urgent-demo` の先頭は `seniorHp -10 / morale -15` で、
    イベント定義の並び順による自滅が敗因分布に混入していた。状態を見て選ぶ方針へ変え、
    judgment と decision を分けて記録した結果、RI-85 の対象が `reviewFreeze` に絞られた。
14. **F-11 の母数が解放イベントだけだった** — 進化に到達しないランが消え、常に「Q1で100%」に
    見えていた。母数を全ランにした。
15. **F-11 の分子が「Q1 に1ノード解放」だった** — 複数ブランチへ均等投資したランも
    「方向が決まった」と数えていた。ブランチへのコミット（同一ブランチ2ノード以上かつ過半）を
    要求する基準へ変え、判定が成立から未充足へ反転した（RI-86）。
16. **復職者を採用方針でしか再配置していなかった** — 休職から戻ったメンバーが
    採用なしの方針でベンチに残り、採用比較へ混入していた。全方針で実働へ戻すようにした。
17. **介入の発動条件を毎ステップ1回しか評価していなかった** — 直前の介入が盤面を変えても
    後続の条件が古い値を見ていた。dispatch ごとに取り直すようにした。
18. **ペーシングを種別ごとの規定帯で判定していなかった** — ボスの下限90秒未達が
    違反として現れていなかった（実際は easy 99.5% / normal 96.7% / hard 92.0% が下回る）。
19. **単一介入群のビート選択を統制していなかった** — `single()` が `firstChoice` に落ち、
    比較対象の `skilledNoHire`（`stateAware`）と条件が違った。揃えた結果 `beat:decision`
    での敗北が 90件（12%）→ 40件（5%）へ減った。
20. **採用が常時採用／常時見送りの二択しかなかった** — 「必要なときだけ採る」を測れず、
    「採用は常に見送るのが正解」を無差別採用の結果だけから言っていた。
21. **ショップのカード・レリックを一度も買っていなかった** — F-2 のスプリント間投資のうち
    ショップ層が「投資しない」条件しか測れていなかった。
22. **目標修正の比較に選抜バイアスが残っていた** — 方針ごとに「提示され選べたラン」が
    異なるまま勝率を並べていた。共通コホート（16組）へ限定した。
23. **F-11 をハーネスの解放順で測っていた** — ブランチの偏りは方針の `evolve` 設定が
    決めており、基準の緩さ次第で 1.1%〜92.9% まで動いた。方針に依らない
    「Q1 で取り切れるノード数」（p50=15＝ツリー全体）へ主要指標を移した。
24. **F-7 を全方針の平均で判定していた** — 実験用統制群を足し引きするだけで値が動く。
    初見相当の `naive` の勝率で判定するよう変えた。
25. **スプリント中の即時敗北で直前状態を取り違えうる実装だった** — ハーネスは
    `sprintsPlayed` が増えたスプリントしかログへ残さないため、カード発動などで結果を
    残さず敗北した場合、ログ末尾は敗北スプリントではなく最後の完了スプリントになる。
    `lostSprintCompleted` を記録して区別するようにした。なお当時の1,200ランでは
    `sprint` フェーズ敗北322件すべてが完走由来で、**この修正による数値の変化は無い**。
26. **`stepMs` を壁時計ミリ秒として記載していた** — 実際は `stepMs/100` tick で、
    1 tick が 1x で 680ms。`skilled` は約2.0秒、`naive` は約4.1秒ごとの判断だった。
27. **進化ツリーの総数を観測ログから推定していた** — 絞り込み実行で総数0・分位点 NaN になった。
    `src/data/evolution.ts` から読むよう変更（15ノード / 総コスト26）。
28. **ビート評価に予算が入っていなかった** — 残予算10で `postmortem-culture`（予算 -10・
    レリック付与）が出るとレリックの加点が勝り、予算0＝`budgetExhausted` を自分から選んでいた。
    適用後に敗北条件へ入る選択肢を除外した結果、`budgetExhausted` が19件→15件、
    `beat:decision` 敗北が40件→35件へ減った。
29. **休息が採用しないとき常に `heal` だった** — 負債返済とカード強化が一度も選ばれないまま
    F-2 の「スプリント間投資が結果を変えない」の根拠に使われていた。`skilledRestRepay` /
    `skilledRestUpgrade` を追加した（13 / 11 で、`skilledNoHire` 14 を上回らない）。
30. **F-7 を全メタプロファイルで集計していた** — `PT_META=full` の結果を初見の勝率として
    出しうる。`fresh` のランだけで判定するようにした。
31. **目標修正コホートのキーにメタ解放が無かった** — `fresh` と `full` の同一 seed が衝突する。
32. **評価中央値を到達ラン独立で比較していた** — 到達率が違うと生存者バイアスで優劣が反転しうる。
    両方針が到達した seed だけの共通コホートへ変更した。
33. **F-11 の標本が方針構成に依存していた** — 全ランを母数にすると方針を足し引きするだけで
    分位点が動く。代表4方針へ固定し、構造的事実（総コスト26 vs Q1 入手 p50=44）を主指標にした。
34. **ボススプリントの進化ポイントを加算していた** — `resolveSprint()` のボス分岐は四半期
    レビューへ遷移して `return` するため、実際にはポイントが入らない。Q1 入手 p50 が
    53 → **43** へ下がり、「ツリーを丸ごと買える」は「約1.6倍」が正しい。
35. **選択的採用の予算条件が採用費控除前で判定されていた** — 文書は「採用後も残る」と
    書いているのに、控除前の額で見ていた。控除後で判定するよう直したところ、
    当初の閾値（`RECRUIT_COST` の2倍＝採用時点で75必要）は**実測の予算 p50=45 / 最大68 に対して
    到達不能**で、`skilledSelectiveHire` が `skilledNoHire` と40ラン全部同一になっていた。
    到達可能な水準（控除後に `RECRUIT_COST` 以上）へ下げ、実際に採用が発生する条件にした
    （40ラン中4ランで経過が分かれ、勝敗の集計は 14/40 のまま）。
36. **F-5 の方針間 CV を各方針の到達ランで独立に集計していた** — 到達率が違うと seed の
    選抜差が混ざる。RI-84 の比較を共通到達 seed のコホートへ変更した。
37. **敗北したスプリントの進化ポイントを加算していた** — `resolveSprint()` は結果を記録して
    `sprintsPlayed` を増やした後に敗北判定し、敗北なら加算前に `return` する。ボスと同じ理由で
    実際には得られない。除外すると1点も得ないランが標本から外れ、Q1 入手の p10 が 7→21、
    p50 が 43→**45** になる。
38. **F-4 のペーシング標本が方針構成に依存していた** — 難易度内の全方針を連結していたため、
    方針を足し引きするだけで帯外率が動く。代表3方針へ固定した（easy 通常の絶対下限割れは
    61.6% → **64.0%**）。
39. **復職直後のメンバーを欠員として数えていた** — 復職者は `onLeave=false` でも次の `setup`
    まで `assignment='bench'` のままで、その間の採用判定で戦力から漏れていた。休職していない
    メンバーを数えるよう直した（当時の1,200ランでは勝敗に変化なし）。
40. **即時採用イベントの安全判定に採用費が入っていなかった** — `grantRecruit` は解決時に
    `tryRecruit` が `RECRUIT_COST` を引くため、予算がちょうど25のとき outcome 上は差分0でも
    実際には予算0＝`budgetExhausted` になる。採用費を引いた後の予算で評価するよう直した。
41. **安全判定に落ちた採用肢が候補集合へ戻っていた** — 上で弾いた選択肢を、後続のフィルタが
    `takeRecruit` だけを理由に再び通していた。除外を一貫させた。
42. **同一 tick 内で盤面を取り直していなかった（影響大）** — 複数の介入を試すとき、`when` の
    判定にループ開始時点の盤面を使い続けていた。先行の介入がレーンを変えても後続がそれを
    見ないため、撃つべきときに見送り不要なときに撃っていた。各 `dispatch` の直前に取り直した
    ところ、**RI-78 のカードの結論が逆転した**（使う14 vs 使わない9。以前は 14 vs 11 で
    「寄与しない」としていた）。ほかに easy 全体 210→219勝、hard 25→19勝、
    `reviewFreeze` 288→268件、`techDebt` 30→40件。
43. **休息の強化対象が常に先頭カードだった** — ドラフトやショップで後から入ったカードが
    一度も強化候補にならなかった。レベルが最も低いカードを選ぶよう変えた（12/40）。
44. **ペーシングの規定帯をレポート側で複製していた** — `src/ui/sprintTempo.ts` の
    `SPRINT_WALL_SEC` / `BOSS_WALL_SEC` から読むようにした。秒換算だけが実装へ追随して
    帯判定が旧値のまま残る、という食い違いを防ぐ。
45. **固定標本の指標が代表方針の欠けを検査していなかった** — `PT_POLICIES` で絞り込むと、
    1方針だけの値を「代表N方針（固定）」として成立判定に出しうる。欠けている方針を明示して
    未計測にするようにした。
46. **spec-mapping のリンク先が旧見出しのままだった** — RI-74 / RI-75 は改名済みで、
    対応表からバックログへ辿れなくなっていた。
47. **ショップ購入の予算予約が採用方針を無視していた** — どの方針も `RECRUIT_COST`（=25）を
    残していたが、`skilledShopBuy` は `recruit: 'skip'` なので次の採用に備える必要がない。
    予算30で8のカードが出ても買えず（買っても22残り、四半期レビューの危機条件5には十分
    余裕がある）、「ショップへ投資する」条件が実際には多くの機会を見送る保守方針だった。
    採用しない方針の予約を10へ下げたところ 13→14 になったが、`skilledNoHire` 15 は
    上回らず**結論は変わらない**。
48. **RI-80（スプリント評価の分布）を F-6 の根拠にしていた** — F-6 は「敗北画面・リザルト・
    組織診断から次の一手が読み取れるか」の基準で、評価中央値とは別物である。評価が一致しても
    敗北時の説明品質は分からない。F-6 の判定は RI-82 へ移した。
49. **RI-85（判定イベントでの決着）を F-8 の根拠にしていた** — F-8 の観測条件は「敗北が
    確定する何スプリント前から実質的な選択肢が消えていたか」で、最終イベントが選択不能で
    あることだけでは満たせない（直前まで回避策があり最後だけ judgment で決着するランも
    同じ形になる）。言えるのは「決着の瞬間に操作の余地が無い」までで、早期詰み
    （実質的な選択肢の消失時点）は RI-89 の機械的発動可否だけでは測れない。
50. **目標修正の効果をラン全体の勝率で測っていた** — 共通コホートで揃うのは最初の修正までで、
    その後の修正は方針間で統制されていない。ラン全体の勝敗には後続選択がすべて乗るため
    初回選択へ帰属できない。主指標を「初回修正の次四半期まで生存」へ変えた
    （68.8〜81.3%。勝率は参考値へ）。
51. **CV をメタプロファイル横断で集計していた** — 共通コホートのキーに `meta` を含めれば
    対応は取れるが、CV は対応差ではなく周辺分布の分散なので、`fresh` と `full` を同じ配列へ
    入れるとメタ解放による平均・分散の差が介入効果へ混入する。メタ別に分けた。
52. **訂正履歴の数値を現在値であるかのように書いていた** — 「本節の数値はすべて修正後のもの」
    としていたが、各項目の値はその修正の直後に観測したものであり、後続の修正で母集団や
    方針構成が変われば動く（実際 29 と 34 は現在値とずれている）。但し書きを入れた。
53. **AI の因果を統制していない方針で測っていた（影響大）** — 「AI 配布を切ると出荷が
    334.5→291.4」を RI-77 の因果の根拠にしていたが、`noAi` は AI 配布のほか andon の有無・
    進化ブランチ・ドラフト選好・採用まで `skilledNoHire` と違い、差を AI へ帰属できない。
    AI 配布だけを外した `noAiCtl` を立てて同一 seed・同一スプリント番号で比べたところ、
    **向きが逆だった**。AI を切ると1スプリントの出荷はむしろ増え（12セル中6セルで上回り、
    下回るセルは無い）、それでも勝率は 15→10 に下がる。「AI で出荷が増える」という
    実務感覚と実装が食い違っていることになり、RI-77 の未充足として書き直した。
54. **ビート評価が遷移先スプリントの負荷を見ていなかった（影響大）** — `stateAware` は選択肢の
    `outcome` だけを採点し `leadsTo` を認識しなかった。`elite-offer` は高負荷側の `outcome` が
    空（=0点）、通常側が信頼-4（=負点）なので、組織がどれだけ消耗していても**常に高負荷側**
    （当時のタスク量は `ELITE_TASK_MUL`=1.6倍。RI-75 以降は難易度別 `eliteTaskMul()` へ
    移行しており、現行値は同関数を参照）を選んでいた。体力（シニアHPと士気の低い方）と
    技術的負債から遷移先の負荷を評価するよう直したところ、高負荷スプリントの発生が
    代表方針で easy 13→9・normal 18→10・hard 16→9 件へ減り、勝率が多くの方針で±1〜2動いた
    （`skilledNoHire` 15→14、`skilledRestUpgrade` 13→15、`noInterventionCtl` 11→12、
    `onlyPair` 10→12）。**RI-78 のショップ・休息層の順位が入れ替わった**ため、その節は
    順位ではなく「差が±1で方向も揃わない」ことを結論とする書き方へ改めた。
55. **復職者に方針固有の編成が再適用されていなかった** — `applySetup` は `setup` フェーズでしか
    呼ばれず、`resolveBeat()` は `setup` を通らない。ベンチ配置と同じタイミングで
    `spec.ai` / `spec.formation` も掛け直すようにした。**ただしこの修正で動いた数値は無い。**
    指摘の根拠とされた「`noAiCtl` に AI 利用が残る」「`aiFullBet` が100%に届かない」は、
    調べた結果いずれも別の理由だった（下記56）。復職経路の穴自体は実在するので修正は残す。
56. **AI 統制条件の残差を「復職者の設定漏れ」と誤認しかけた** — 実際は2つとも別の原因である。
    `aiFullBet` の AI 利用率が最大97%なのは `AI_ADOPTION`（=0.85）の確率抽選のばらつきで、
    仕様どおり。`noAiCtl` に4%残るのは `assignTask` 介入が `defaultAssignee` 経由で
    個別タスクを AI へ回すためで、`spec.ai` が変えるのはロスターの `aiAssigned`（intake の
    採用率）だけ、`org.aiEnabled` は true のままだからである。**AI 配布ゼロ＝AI 利用ゼロでは
    ない**ことを RI-77 に明記した。
57. **ビート評価に信頼の残量が入っていなかった** — 予算・シニアHP・士気・負債は「適用後に
    敗北条件へ入る選択肢を除外する」判定があるのに、**信頼だけ守られていなかった**。
    信頼低下は残量に関係なく固定の重み（×0.2）で採点されるため、たとえば `urgent-demo` の
    「延期を交渉する」はレリック加点が勝って、management 信頼が11でも信頼-8を選び、
    次の四半期レビューで `minTrust <= 10` の `shutdown` を確定させていた。`BeatCtx` に
    `stakeholderTrust` を渡し、適用後10以下を除外、減少は残量で重み付け、危機域（15以下）へ
    踏み込む場合はさらに減点するようにした。`trustExhausted` が 32→27件へ減った。
58. **勝利数の突き合わせを場当たりの grep でやっていた（影響大）** — 表の形が少しでも違うと
    引っかからず、方針を1つ足しただけの回で「概要は更新したが詳細表が旧値のまま」という
    食い違いを3か所残してレビューで指摘された。`npm run playtest:check`
    （`scripts/check-findings.mjs`）で機械的に検査するようにした。**導入直後の再計測で
    即座に18件の取りこぼしを検出しており**、手作業では追い切れていなかったことがはっきりした。
59. **信頼の修正で多くの結論がまた動いた** — `onlyAndon` 17→19、`skilledRestUpgrade` 15→13、
    `skilledShopBuy` 13→14、`skilledSelectiveCards` 12→11、`skilledNoCards` 8→7、
    `noInterventionCtl` 12→11、`skilled` 9→10。結果として RI-78 のショップ・休息層は
    **無投資と完全に同数**（14対14対14、カード強化のみ13）になり、RI-84 の介入なしと初見の介入も
    同点（11対11）へ戻った。F-9 の層別一貫性は例外が1層→3層へ増え、今回は介入する方針
    （`hard` / `onlyFirefight`）にも例外が出て「介入しない条件だけがずれる」という整理も崩れた。
    **n=40（層別では n=3〜5）の指標は、ハーネスの一手を直すたびにこの幅で動く。**
60. **失敗した実行のあとに旧出力が残っていた** — `runMatrix` が例外を投げたり終端未到達の
    assertion で落ちたりすると `playtest-out/runs.json` に一度も触れないため、前回の成功結果が
    そのまま残る。続けて `playtest:report` / `playtest:check` を回すと**旧コードの結果を
    最新値として集計してしまう**。走り始める前に旧出力を消すようにした。
61. **絞り込み実行の出力を検査に使えてしまった** — `playtest:check` は入力の全ランを無条件に
    方針別集計していたので、`PT_DIFFS=easy` の出力（10ラン）を40ラン前提の文書と突き合わせて
    全件を不一致として報告しうる（`onlyAndon` なら実測9勝 対 文書19勝）。出力へコホート情報
    （難易度・seed・方針数・メタ・既定かどうか）を書き出し、既定でなければ**未計測として
    降りる**ようにした。レポートも先頭でコホートを表示する。
62. **重複方針が既定コホートとして通った** — `isDefault` を件数だけで判定していたため、
    31方針のうち1つを落として別の1つを重複指定すると既定として通り、落ちた方針は
    `playtest:check` の集計に現れないまま「一致」と表示されうる。`PT_POLICIES` で重複を
    拒否し、判定を集合の一致へ変えた。61 で入れた防護の穴。
63. **`scoreChoice` が `EventOutcome` の一部しか見ていなかった（影響大）** — 予算・信頼・
    遷移先を順に足してきたが、そのたびに**議論になった項目だけ**を足しており、
    `quality` / `testCoverage` / `aiLiteracy` / `grantCard` / `nextSprint` は最後まで
    未評価だった。またクランプを考慮していなかったため、士気100 で「士気+4・負債+2」が
    加点され、実際には負債だけ増える選択肢を選んでいた。`EventOutcome` の全項目を
    クランプ後の実効差分で採点するようにした。
64. **`rest-offer` の両選択肢が同点だった** — 「休む」は `nextSprint.taskCountMul=0.7` だけ、
    「攻め続ける」は空の `outcome` で、どちらも0点。提示順の先頭（＝常に休む）が
    無条件に選ばれ、`stateAware` 系の全方針が組織状態に関係なく毎回タスクを30%減らしていた。
    `nextSprint` と `leadsTo: 'rest'` を採点に入れた結果、高負荷スプリントの発生が
    297→507件へ増えた。
65. **63・64 の修正で F-1 の根拠が弱まった** — 単一介入と複合方針の差が
    19/16 対 14 → **17/17 対 16** へ縮み、**1勝差**になった。「単一介入が複合を明確に上回る」
    という当初の観測は、ビート評価の穴が作っていた分を含んでいたことになる。
    ほかに `skilledNoHire` 14→16、`skilledSelectiveHire` 13→16（無採用と完全同数）、
    `skilledRestUpgrade` 13→16、`skilledShopBuy` 14→16。RI-73 の F-1 判定は再検証が要る。
66. **RI-86 の分位点が条件付き分布だった** — 0点のランを除いた値（p50=45）を `n=160` の
    実測として書いていた。0点は30件あり**すべて nightmare の第1スプリント敗北**で、
    そもそも方向を選ぶ局面に到達していない。全標本の p50 は 43。どちらでも総コスト26 を
    大きく超えるため結論は変わらないが、母集団の違う値を混ぜない書き方へ改めた。
67. **信頼10以下を確定敗北として除外していた（57 の入れすぎ）** — `evaluateQuarterOutcome` は
    **ボス突破かつ全KPI達成なら `met` / `exceeded` を先に返してから** `minTrust <= 10` の
    `shutdown` を見る。つまり信頼10以下でも次のレビューで目標を達成すれば勝てるので、
    確定敗北ではない。他の4条件（予算・シニアHP・士気・負債）は `evaluateLose` が状態だけで
    即座に返すため除外が正しいが、信頼は違う。勝ち得る選択肢を落として方針比較を歪めていた
    ので、除外をやめて重い減点（-25）へ変えた。`trustExhausted` は 27→24件。
68. **士気の敗北判定にレリックの軽減を入れていなかった** — `applyEventOutcome` は**負の**士気
    差分に `moraleDamageMul`（0.6〜0.75）を掛ける。素の差分で判定していたため、士気6で
    `expectation-mgmt` を持つとき `giant-pr` の -6 は実際には -4.5 で生き残るのに、
    確定敗北として除外していた。畳んだ passives を評価コンテキストへ渡した。
69. **休息遷移に一律の回復価値を与えていた** — `skilledRestUpgrade` はカードを強化するだけ、
    `skilledRestRepay` は負債を返すだけで、どちらもシニアHPも士気も回復しない。
    `restChoiceFor()` を切り出して**実際の休息内容と同じ判断**を評価側からも参照するようにし、
    heal / repay / upgrade それぞれの効果で採点する。ショップ・休息投資の比較（RI-78）へ
    直接効く経路だった。
70. **F-7 のガードが `naive` の有無を見ていなかった** — `fresh` のランがあるかだけを確認して
    いたので、`PT_POLICIES=skilledNoHire` のような絞り込みでは未計測メッセージが出ず、
    見出しだけが残って計測済みに見えた。`naive` のランの存在を確認するようにした。
71. **旧出力の削除が型検査より後だった** — `npm run playtest` は `tsc --noEmit && vitest` の順で、
    削除は Vitest 側にあった。型エラーで落ちるとテスト本体へ到達せず削除も走らないため、
    その状態で `playtest:report` / `playtest:check` を続けると前回の成功結果を最新値として
    集計できてしまう。`scripts/invalidate-playtest-out.mjs` を実行の先頭へ置いた
    （Vitest 側の削除も `vitest` 直叩き経路のために残す）。
72. **休息の `heal` にスタミナ回復と復職を入れていなかった** — `restChoose('heal')` は
    組織値だけでなく `recoverStamina(..., REST_STAMINA_RECOVER)` も実行する。組織HP・士気が
    高くても休職者や低スタミナ要員がいれば休息の価値は大きいのに、それを0点として
    攻め続ける側を選びうる状態だった。ロスターを評価コンテキストへ渡した。
    **ただしこの修正で動いた数値は1件も無い**（1,240ランの全集計値が修正前と一致）。
    経路の穴は実在するので修正は残すが、現状この項は判定を動かしていない。
73. **獲得できないレリックにも加点していた** — `RunEngine.grantRelic()` は**既に所持している／
    枠が満杯**なら何もせず終了するのに、`grantRelic` があれば無条件に +4 していた。
    `expectation-mgmt` を持った状態で `urgent-demo` が再登場すると、レリックを得られないのに
    管理信頼-8 の選択肢が架空の加点で高得点になりうる。所持一覧と枠数を評価コンテキストへ渡し、
    実際に獲得できるときだけ加点するようにした。
74. **旧出力の削除が環境変数の解析より後だった（71 の取りこぼし）** — npm スクリプト経路は
    `scripts/invalidate-playtest-out.mjs` で守ったが、`vitest` 直叩き経路では削除が
    テストのコールバック内にあり、`PT_DIFFS=typo` のような不正値だとモジュール初期化中の
    `parseDiffs` が先に例外を投げて旧出力が残る。削除を解析より前のモジュール先頭へ移した。
75. **73・74 の修正で動いた数値は無い** — 1,240ランの全集計値（勝利387 / 敗北853 /
    総スプリント9,420 / 敗因内訳 / 勝利種別 / 診断 / 決着フェーズ / 評価分布）が修正前と一致した。
    **ハーネスの評価式に対する直近3回の修正（72・73 と本項）は、いずれも観測値を動かしていない。**
    穴は実在するので修正は残すが、評価式の主要な歪みは出尽くしつつあると読める。
76. **旧出力の削除が静的 import より後だった（74 の取りこぼし）** — 環境変数の解析より前へ
    移しても、ES モジュールは**静的 import を本体より先に**評価するので、`harness.ts`
    （やその依存）の変換・初期化で落ちると削除に到達しない。Vitest の `globalSetup`
    （`tests/playtest/globalSetup.ts`）へ移した。テストモジュールの読み込み前に走るため、
    import 時の失敗でも旧出力が残らない。`harness.ts` へ構文エラーを入れて検証済み。
77. **`playtest:check` が複数方針をまとめた行を検査していなかった** — 順位表には
    `| **`skilledNoHire`** / `skilledShopBuy` / … | **16** |` のように同じ勝利数の方針を
    並べた行があるが、「方針名がちょうど1つの行だけ」を対象にしていたため丸ごと除外していた。
    グループ内の1方針だけが変化しても旧値のまま検査を通るので、**この検査を入れた目的が
    主要な表で働いていなかった**。独立した数値セルがちょうど1つの行なら、その値を行内の
    全方針と個別に照合するようにした（グループ行へ実測10の方針を混ぜて検出を確認済み）。
78. **信頼差分を素の値で採点していた** — `applyTrust` は各信頼値を 0〜100 に丸めるので、
    残量2で -8 を受けても実際には -2 しか動かない。素の差分で減点すると実害の小さい選択肢を
    過大に嫌う。クランプ後の実効差分で採点し、閾値判定（`TRUST_SHUTDOWN` / `TRUST_CRISIS`）も
    丸めた後の値で見るようにした。
79. **休息の回復量にレリックのボーナスが乗っていなかった** — `restChoose('heal')` は
    `REST_HEAL` に `foldPassives(relics).restHealBonus`（`flow-first` で +10）を足す。
    基礎回復量だけで採点していたため、レリック取得後の休息の価値を過小評価していた。
80. **`reviewHeavy` がコーダー0人のスプリントを許していた** — 余剰コーダーをレビューへ移す
    ループだけなので、唯一残したコーダーが休職すると補充されない。`foldFormationEffects()` は
    コーダー不在で coding slot を0・速度を0.15 へ落とすため、実プレイヤーなら編成画面で
    1人戻せる局面がほぼ無人スプリントになっていた。コーダーが0人になったら実装適性の高い
    レビュー担当を1人戻すようにした。
81. **単一介入方針が未実行のセルを「0回」と表示していた** — `PT_POLICIES` で絞り込むと、
    実行していない方針も `ok=0/no-target=0(—)` として出て、「未実行」と「実行したが対象が
    一度も無かった」を区別できなかった。該当ランが無いセルは未計測と明示する。
82. **78〜81 の修正で動いた数値は無い** — 1,240ランの全集計値が修正前と一致した。
    **ハーネスの評価式・編成に対する直近4回の修正（72・73・本項の4件）は、いずれも
    観測値を動かしていない。** 個々の穴は実在し修正の価値はあるが、
    **現行の1,240ラン・31方針という標本では、これらの精度改善は結論を左右していない。**
    残る歪みがあるとすれば、この標本規模では検出できない水準にあると考えられる。
83. **難易度別の全方針合計が実測と合っていなかった** — 225/127/33（合計385）と書いていたが、
    総勝利は 387 で内訳と合わない。以前の回に**実測を取らず推測で書いた値**である。
    実測は 224/124/39（合計387）。方針別勝利数しか機械検査していなかったため通り抜けていたので、
    `scripts/check-findings.mjs` に難易度別合計の検査を足した。
    レビューは「385 が正しいなら 387 を直せ」という向きで指摘していたが、
    **合っていなかったのは難易度表の方**だった。
84. **存在しない「介入で即死する」規則をハーネスへ入れて計測してしまった** — これは
    観測の精度を上げる修正ではなく、**実装を誤解して測定対象そのものを壊した**例である。

    シニアHP・士気が敗北閾値を跨ぐ介入を全方針で見送るガード（`survivesSelfCost`）を入れ、
    1,240ラン中258ラン（21%）で計4,601回の介入を見送らせた。その結果として
    総勝利 387→382、hard の全方針合計 39→34、方針別も5件動いた。

    **しかしゲームにその規則は無い。** `RunEngine.dispatch()` は `applyImmediateLose()` を
    呼ばない（呼ぶのは `playCard()` のみ）。`evaluateLose` は `resolveSprint()` まで延期され、
    その間 `stepSprint()` が毎 tick `REVIEW_HP_REGEN` でシニアHPを回復させる。
    実機でも「HP3 で `firefight` → HP1・`status=playing` → 1 tick 後 HP1.7」を確認した。
    つまり**一時的に閾値を割っても、スプリント終了までに回復して生存し得る**。

    ガードを撤去し、集計値は撤去前（=正しい値）へ戻した。上記の 382 / 34 などは
    **実在しない規則の下での値なので、どこにも残さない**。
    再発防止に `tests/unit/sim/dispatchDefersLose.test.ts` を置いた。この機構はレビューでも
    2回続けて逆に判断されており（1回目は「即死するから見送れ」、2回目は「即死しない」）、
    コードを読むだけでは取り違えやすい。**実装の挙動を仮定でなく実行で確かめること。**
85. **敗北直前の状態が「前スプリントの終了時点」だった** — スプリント終了と敗北の間には
    ビート・ショップ・休息・setup が挟まるので、そこでの増減が丸ごと落ちていた。
    ループ各反復の処理前を控えて敗北検知時のものを残す（`lostPrevState`）ようにした。
    その結果 `seniorBurnout` の直前シニアHP は 12.6 → **19.1**、`techDebt` は 11.8 → **52.3**。
    **旧計測は「敗因が指す資源が枯れてから負ける」ように見せていたが、実際には健全な値から
    選択不能なビート1回で落ちている**（`seniorBurnout` 206件中160件が judgment）。
    RI-79 が「予兆なく終わる」とした敗因は3種ではなく、より広い。
    なお `techDebt` の敗北までの p50 も 10 と書かれていたが実測は 9 だった（併せて訂正）。
86. **採用が経路によって自滅していた** — `tryRecruit` は `budget >= RECRUIT_COST` で通し、
    差し引いた後に `applyImmediateLose()` を呼ぶ。予算がちょうど25だと残高0で
    `budgetExhausted` になる。ビートの `grantRecruit` 経路は採用費を引いた額で評価済みだったが、
    専用採用・ショップ・休息の各フェーズは引く前の額で判定していたため、
    **同じ採用が経路によって自滅したりしなかったり**していた。

    **84 と違い、これは実在する即時敗北である。** `dispatch` は敗北判定をしないが
    `tryRecruit` は呼ぶ。実測でも `budgetExhausted` 14件のうち**6件が予算ちょうど25からの採用**で、
    いずれも shop / rest / recruit フェーズだった。採用後に予算が残ることを条件へ入れて揃えた結果、
    その6件が消えて `budgetExhausted` は **14 → 8** になった。総勝利 387 → 388、
    easy 224 → 225、`noAi` 11 → 12。**84 の教訓どおり、実装するより先にコードと実測の両方で
    即時敗北が起きることを確認してから直した。**
87. **残る `budgetExhausted` 8件は別の要因である（未対応）** — 8件すべてが `beat` フェーズ・
    予算10からの `postmortem-culture(decision)` で、方針は `noAi` / `reviewHeavy` / `aiFullBet` の
    3つに限られる。この3方針は**ビート選択が `firstChoice`（提示順の先頭）**で、
    比較対象の `skilledNoHire` だけが `stateAware` である。つまり F-10 のビルド比較には
    ビルドの違いに加えてビート判断の巧拙が混ざっている。RI-90 として起票した。
88. **87 を修正した結果 `budgetExhausted` が消え、RI-77 の説明が1つ崩れた** — ビルド差分3方針
    （`aiFullBet` / `noAi` / `reviewHeavy`）のビート選択を `stateAware` へ揃えた。

    **`budgetExhausted` は 8 → 0 になった。** 1,240ランで予算枯渇の敗北が1件も出ない。
    元の14件は**すべて回避可能な自滅**で、内訳は採用で予算を使い切った6件（86）と、
    ビート統制の欠落で確定敗北を自分から選んだ8件（本項）だった。

    これにより **RI-77 の「AI を外すと予算が持たないので AI 導入が実質必須」という説明は
    根拠を失った**。予算枯渇はゲームの性質ではなくハーネスの統制不足が作っていた。
    勝率差（`noAiCtl` 10 / `noAi` 12 / `aiFullBet` 13 対 `skilledNoHire` 16）は残るので
    「解除側が不利」自体は成立するが、**その機構が予算だという説明は取り下げる**。

    他に動いた値: 総勝利 388 → 390、easy 225 → 226、normal 124 → 125、`reviewHeavy` 8 → 10、
    `beat:decision` 敗北 34 → **17**（半減）、`beat:judgment` 462 → 473。

    **F-10 の結論は変わらなかった**（勝利種別は `healthy` 205 / `noDamage` 184 / `happiness` 1 で
    実質2種のまま、診断はビルドで分かれたまま）。RI-76 は統制を直しても成立する。
89. **コーダー下限の補正が `reviewHeavy` 専用だった** — `assignBenchMembers` はレビュー適性が
    実装適性以上のメンバーを review へ置くので、coding にいた全員が休職すると**どの方針でも**
    コーダーが0人になる。`foldFormationEffects()` はその状態で coding slot を0・速度を0.15 へ
    落とすため、健常なレビュー担当が残っているのにほぼ無人のスプリントが始まる。

    ハーネスを一時計測して180ラン（6方針 × 10 seed × 3難易度）を掃いたところ29回発生し、
    うち `setup` へ到達＝実際に無人でスプリントを開始したのは
    `easy`/`normal`/`hard` の `noAiCtl`/`pt-4` と `normal` の `noAi`/`pt-9` の4件。
    **4件中3件が `noAiCtl`**、つまり RI-77 の AI 統制比較そのものに混ざっていた。
    補正を `ensureAtLeastOneCoder` へ切り出して全方針へ適用した。

    **勝率は動かなかった**（総勝利390、難易度別も `noAiCtl` 10/40 も変化なし）ので、
    RI-77 の「解除側が不利」という結論は影響を受けない。動いたのは敗因の内訳で、
    `reviewFreeze` 298→296、`seniorBurnout` 210→209、`trustExhausted` 26→28、
    `reorgRequired` 15→16、`beat:judgment` 473→470、`quarterReview` 41→44。
90. **`playtest:check` が世代不一致で終了コード0を返していた** — 検証用スクリプトなのに
    「何も比較していない」を `&&` 連結や CI が「検査に通った」として扱えた。非0にした。
    コホート不一致を `exit 0` のままにしているのは、絞り込み実行が正式にサポートした
    使い方で「その条件では検査できない」が正常な結果である一方、
    世代不一致は**再計測すれば必ず解消する異常**だからである。
91. **検査が散文の定型表現を見ていなかった** — 「良かった点」節の「熟練の 14/40」
    「使う14 vs 使わない8」は方針名がコード表記でないため `N/40` の検査に掛からず、
    旧コホートの値が3回続けてレビューで指摘された。汎用の散文解析は不可能だが、
    この文書で繰り返される言い回しは有限なので対応表として明示した。
    敗因の合計（「（実測合計N件）」）と方針別の診断件数も併せて検査対象にした。
    **追加した検査は、レビューが指摘した4件をそのまま再現して捕まえた。**
92. **検査が表形式の診断件数を取りこぼしていた（91 の取りこぼし）** — 追加した検査が
    `` `方針` は `診断` N `` の形だけを見ており、RI-76 の診断表
    （`| \`noAi\` | \`documentationKingdom\` 10 / ... |`）に掛からなかった。
    区切りに `|` も受けるようにし、**意図的に値を壊して発火することを確認**した。
    91 を入れたときにこの確認をしていれば同じラウンドで気づけた。
93. **統制条件を揃えたら RI-80 の根拠が入れ替わった** — 評価中央値の比較に `passive` を
    使っていたが、これは介入以外に進化（`none`）とビート判断（`firstChoice`）も違う。
    介入だけを外した `noInterventionCtl`（F-5 用に既に定義してあった）へ替えた。

    一致条件は 6 → **7**（10条件中）へ増え、`hard S3` の2段階差（C→A）は**消えた**。
    あの差は介入の巧拙ではなく進化順とビート判断の差だった。
    代わりに **`normal S2` で無介入の方が高い**（A 対 B）という条件が出ている。
    **結論（受入条件を満たさない）は変わらないが、根拠の数値は総入れ替えになった。**
94. **検査に敗因件数と方針別勝利種別を足した** — RI-85 の敗因表と RI-76 の勝利種別表が
    どちらも旧コホートのまま残っていた。追加した直後の実行で、
    **レビューが指摘していなかった4件（`plan/remaining-issues.md` の敗因件数）も捕まえた**。

    ただし**検査で追えない数値はまだ多い**。ペーシングの分位点・進化ポイントの分位点・
    評価の中央値一致数などはレポート側の集計ロジックを複製しないと検査できず、
    現状は手作業で揃えている。**この節の数値は「機械検査済み」と「手作業」が混在している**
    ことを前提に読むこと。機械検査の対象は方針別勝利数・難易度別合計・総勝敗・
    敗因件数・方針別の勝利種別と診断・散文の定型表現である。
95. **検査の正規表現を緩めすぎて偽陽性を出した（94 の途中経過）** — 敗因件数の照合で
    名前と件数の間に任意の4文字を許したところ、`が全29` + `6件` のように**桁の途中で切って**
    「6件と書かれている」と報告した。誤検出6件。曖昧さの無い2形式
    （`` `name`（N件） `` と `` `name` が全N件 ``）だけに絞り直した。
    **裸の「`name` N件」は対象にしない**。「`aiDependency` 10件が消える」のように
    部分集合を指す用法があり、全体の件数と区別できないためである。
    今回は2形式それぞれを壊して発火を確認してから残した。
