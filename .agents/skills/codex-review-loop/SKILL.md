---
name: codex-review-loop
license: MIT
description: 実装完了後の PR で、ユーザーが手動で行っていた Codex 再レビュー依頼と指摘クローズを代行する。初回 Codex は PR 作成時に自動実行されるため、指摘対応後の `@codex review` 投稿と、対応済みスレッドの返信・resolve を扱う。「/autopilot」「Codex レビューを回して」「Codex 指摘を対応して」など、Codex（chatgpt-codex-connector）への再依頼や指摘クローズを依頼されたときに使用する。
---

# Codex 再レビュー依頼の代行

コード修正・検証・CI・スレッド操作・コマンドの選び方はエージェント判断で進めてよい。
このスキルが固定するのは、これまでユーザーが手動で行っていた次だけである。

1. 初回以降の修正後に `@codex review` を投稿する
2. 対応済みの指摘には返信し、スレッドを解決済みにする
3. マージせず merge-ready で止めること

## 1. 初回以降だけ `@codex review` を投稿する

PR 作成時の初回 Codex レビューは自動で走る。エージェントが改めて依頼するのは、**初回以降に指摘へ対応して push したあと**だけである。

そのとき PR コメントとして、次の一文だけを投稿する。

```text
@codex review
```

- 余分な文言・チェックリスト・署名を付けない。
- PR 作成直後や、まだ一度も指摘対応していない段階では投稿しない。
- 同じ HEAD に対して連投しない。新しい push のあとなら再投稿してよい。
- Draft の Ready 化はユーザーが明示したときだけ行う。

## 2. 対応済み指摘は返信して resolve する

指摘へ対応したら（修正でも dismiss でも）、そのスレッドに返信し、解決済みにする。
返信文面や手段はエージェントに任せる。結果として返信と resolve が済んでいればよい。

## 3. ループの止め方

未解決の Codex 指摘が無く、CI も問題なければ merge-ready として報告して止める。
**ユーザーが明示しない限りマージしない。**
